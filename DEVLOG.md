# 开发记录

记录每次提出的需求、处理结果、落在哪个分支。当前开发分支：**`accountbook-20260804`**。

`accountbook-20260801`分支的内容已于2026-08-04合并进仓库的默认分支`claude/upload-project-github-ww338s`(fast-forward，无冲突)，之后从合并完的最新代码切出这个新分支继续开发。

## ⚠️ 长期规则（每次改动前必须遵守）

- **字体不要变动**，任何改版都必须沿用现在这套字体：标题/金额用 `'Noto Serif SC', serif`，正文/按钮用 `'Noto Sans SC', sans-serif`。除非用户明确要求换字体，否则不碰 `font-family`。
- **任何新按钮都必须有点按交互反馈**（按下变暗/缩小），新按钮直接复用已有的 `.save-btn`/`.icon-btn`/`.text-link-btn` 等class（都已经带 `:active` 效果），不要写没有交互反馈的裸按钮。全局已经加了 `document.addEventListener('touchstart', ...)`，让 `:active` 在iOS真机上也能可靠触发（这是iOS Safari的已知限制，没有触摸事件监听器的话`:active`经常不生效）。
- **任何样式/UI改版，先出效果图（截图或可点击的对比demo）给用户看，等用户选定/确认后再改正式代码并提交**，不要直接改完就commit。纯粹的bug修复（比如让本来就已经拍板的设计正确渲染出来）不算"改版"，可以直接修。

| 日期 | 需求 | 状态 | 分支 |
|---|---|---|---|
| 2026-07-31 | 版本记录从 v1.0.0 开始 | 已完成：新增 VERSIONS.md，打了 `v1.0.0` 快照分支 | claude/upload-project-github-ww338s（同步至 accountbook-20260731） |
| 2026-07-31 | 切换到 accountbook-20260731 上继续开发，并建立开发需求记录 | 已完成：本文件 | accountbook-20260731 |
| 2026-07-31 | 固定支出每月自动导入到后台 | 已完成：复用已有的 `RECURRING_RULES`/`ensureRecurringEntries()` 机制（原本只给工资用），发了 Excel 模板给用户填具体的固定支出条目，等用户填完回传后再把规则加进代码 | accountbook-20260731 |
| 2026-07-31 | 货币需求调整：仅设置页可切换货币；新建/编辑/复制弹窗去掉货币切换按钮，新建默认跟随设置的本位货币；切换本位货币后，原币种与当前设置不同的记录按"记账当天"的历史汇率换算显示，原币种与当前设置相同的记录始终显示原始数据（不转换、不重新计算） | 已完成：移除入账弹窗的货币按钮；新增按日期缓存历史汇率（`ledger_rate_by_date`）+ `ensureRatesForEntries()` 按需补抓每条记录当天的汇率；新增 `convForDisplay()` 统一单条记录的显示换算逻辑，`toBase()` 汇总计算也改用记录当天汇率而不是最新实时汇率 | accountbook-20260731 |
| 2026-07-31 | 集計页底部留白处理；年月选择框出几版方案 | 已完成留白问题：根因是柱状图默认停在月初(1-9号)，若数据在月中/月末就会显示大片空白，改成渲染后自动横向滚到最新一段，让实际数据直接可见。年月选择框做了3版方案（A票根式合并/B步进箭头/C日期邮戳），发了对比图给用户选 | accountbook-20260731 |
| 2026-07-31 | 給与日期规则修正为每月10日，休日则记载到最近的前一个营业日；家賃73440每月26日，休日则记载到后一个营业日 | 已完成：新增日本祝日数据源（holidays-jp API，本地缓存 `ledger_jp_holidays`）+ `isBizDay()`/`adjustToBizDay()` 通用营业日调整；`RECURRING_RULES` 里给薪资规则加 `dayOfMonth:10, bizDayAdjust:'before'`，新增房租规则 `dayOfMonth:26, bizDayAdjust:'after'`；`ensureRecurringEntries()` 改成每次都重新计算正确日期，如果之前生成的日期不对（比如祝日数据还没抓到时先按周末粗算）会自动纠正，不会一直错下去 | accountbook-20260731 |
| 2026-07-31 | 新增每月月底最后一天 -5272 的固定支出，休日则顺延到后一个营业日，支付方式ペイディ，备注iPhone，分类通信费/携帯 | 已完成：新增 `paidy` 支付方式（粉色徽标）；`RECURRING_RULES` 新增 `dayOfMonth:'last'` 规则类型 + `lastDayOfMonthStr()`；顺带修了一个潜在bug——原来判断"这个月是否已生成过"是看日期前缀，但顺延到营业日可能跨到下个月（比如月底是周日），会导致重复生成，改成用固定id判断，不受日期是否跨月影响 | accountbook-20260731 |
| 2026-07-31 | 集計页底部tabbar下方还有一道空白（用户截图圈出） | 已完成：内容变短的页面(`.page.active`)之前会用flex居中把内容高度精确算成"正好一屏"，这种"内容刚好等于可视区域、完全不能滚动"的边界情况在部分iOS版本上会让底部fixed导航栏渲染错位、留出空白。修法：让页面高度故意比一屏多1px（保证肯定可以滚动），并给 `.tabbar`/`.fab` 加 `transform:translateZ(0)` 提升为独立合成层，这是iOS Safari/PWA里修复fixed定位错位的标准做法 | accountbook-20260731 |
| 2026-07-31 | 支出/收入柱状图和环状图颜色太单一，已经开始重复 | 已完成：根因是9个收入分类的颜色是直接照抄对应支出分类的颜色（比如"工资"和"交通费"颜色完全一样、"事业副业"和"日用品"完全一样），只要当月同时有这几类记录，图表和图例就会撞色分不清。给9个收入分类重新配了一套和全部18个支出分类互不重复的颜色，用Playwright测试撞色组合验证图例8个分类现在全部颜色唯一 | accountbook-20260731 |
| 2026-07-31 | 颜色对比还是不够强烈，改用反差强烈的亮色系 | 已完成：把全部27个分类（18支出+9收入）的颜色重新设计成高饱和度亮色系（S约62-78%），色相在360°上均匀分布保证互不相近，只有"未分类"两项保留深灰色。用Playwright截图核对图例效果，颜色明显更跳、更容易区分 | accountbook-20260731 |
| 2026-07-31 | 不要用色相分布法带出的紫色系，换成其他亮色 | 已完成：上一版收入分类刚好都落在色环的紫/靛/洋红区间，看起来像"一堆紫色"。这次把紫/靛/洋红(245°-330°)整段从色相分布里排除，且让支出/收入分类交替取色相而不是各自占一段连续区间，全部27个分类改用红/橙/黄/绿/青/蓝的亮色，不含任何紫色 | accountbook-20260731 |
| 2026-07-31 | 环状图颜色不要和"支出/收入"大按钮撞色 | 已完成：根因是"支出"按钮是珊瑚红(--seal,色相6°)，"收入"按钮是墨绿(--jade,色相104°)，而分类色板里刚好有几个分类(食费/住房/现金/汽车等)落在这两个色相附近，撞色。这次从取色范围里再排除掉这两个色相各±22°的区间，全部27个分类色重新生成，用Playwright分别截图支出视图和收入视图确认环状图颜色和上方按钮不再相近 | accountbook-20260731 |
| 2026-07-31 | 顶部标题栏(胶带装饰+设置按钮+黑字提示)要显示在每个页签顶部，不只是家计簿页 | 已完成：给「集計」「為替」页头都加上了 washi-tape 装饰、设置按钮(id加2/3后缀避免重复)、黑字/赤字徽章，三个设置按钮都绑同一个打开设置面板的函数，徽章文字在 `renderLedger()` 里三份一起同步更新。Playwright截图确认三个页面头部样式一致，且从統計/為替页点设置按钮也能正常打开设置面板 | accountbook-20260731 |
| 2026-07-31 | 备份用的GitHub Token被清空了，排查 | 已完成：找到真实bug——`saveSettings()`原来是把内存里的`settings`对象整个覆盖写回localStorage；如果同时开着两个标签页/PWA实例，其中一个是在token被设置**之前**就加载的（内存里根本没有`githubToken`这个字段），之后哪怕只是在那个旧实例里随手切一下语言，保存时就会用"没有token"的旧对象整个覆盖掉磁盘上已经存好的token，等于把token冲掉了。修法：`saveSettings()`写盘前先读一次磁盘上最新的设置做合并（磁盘值打底，内存里明确改过的字段覆盖），不会再互相冲掉。用两个Playwright页面模拟"新标签页设置token→旧标签页切语言"的场景验证token不再丢失 | accountbook-20260731 |
| 2026-07-31 | Token/Gist ID 输入框加编辑按钮：备份成功后置灰锁定，点编辑才能改 | 已完成：token输入框旁加了铅笔编辑按钮；成功执行"立即备份"或"从云端恢复"后，两个输入框自动disabled置灰、显示编辑按钮；点编辑按钮解锁可重新输入，改完再次备份/恢复成功后自动重新锁定。用Playwright mock了GitHub API返回，完整走了一遍"输入→备份成功→锁定→点编辑→解锁"的流程验证 | accountbook-20260731 |
| 2026-07-31 | 集計页顶部留白、底部留白太多；柱状图改造出几版方案 | 高度问题已完成：集計页取消了强制居中，改成正常顶部对齐，图表卡片`flex:1`长满剩余空间，图表SVG高度也从固定190px改成按容器实际高度动态计算（渲染时测量`.chart-scroll`的`clientHeight`）。Playwright验证头部现在贴顶、卡片底部离tabbar只剩合理间距。过程中发现一个更本质的问题：图表变高后暴露出"大额收入(工资)和小额日常支出共用一个刻度，工资一出现小额支出柱子就被压成看不见的细线"，柱状图改造的3版方案（A支出收入分刻度/B维持结构视觉加强/C支出柱状+收入折线标记）就是针对这个问题设计的，发了对比图给用户选，选完再改代码 | accountbook-20260731 |
| 2026-07-31 | 柱状图先退回原版（撤销支出/收入下拉分刻度那版）；下方分类图例改成一行一个，旁边加对应颜色的柱状条 | 已完成：`git revert` 撤销了上一次的下拉分刻度改动，柱状图恢复成支出/收入两根柱子并排、共用刻度的原样。分类图例（`trendCatLegend`）从"横向自动换行的一堆小圆点"改成纵向一行一个，圆点换成了跟柱子颜色一致的小矩形色条，一眼能对应到图表里是哪根柱子的颜色 | accountbook-20260731 |
| 2026-07-31 | 支出/收入不同时显示，改用下拉框（默认支出）；图例改成小方块+右侧百分比进度条+百分比数字 | 已完成：重新加回下拉切换（这次默认值是`expense`），支出/收入各自算各自的刻度，柱子加粗到20px。图例每一行：小方块色标 + 分类名 + 占比进度条(颜色同色标,长度=百分比) + 百分比数字，替代了上一版的纯色条。沿用了之前验证过的"滚到当前类型最后一笔有数据的位置"逻辑，避免切到收入看不到发薪日 | accountbook-20260731 |
| 2026-07-31 | 换设备时怎么同步已保存的备份Token？能不能直接做个同步 | 已完成：没有把Token塞进Gist备份内容本身（那等于把开保险箱的钥匙锁进保险箱），而是加了"配置码"功能——把Token和Gist ID打包编码成一段文字，在这台设备点"复制配置码"，到新设备粘贴进输入框点"导入"就自动填回两个字段，不用重新生成Token也不用两个分开抄。用Playwright验证了复制→（模拟换设备）粘贴→导入的完整链路，以及粘贴格式错误、还没备份过就复制这两种情况的提示语 | accountbook-20260731 |
| 2026-07-31 | 想要"随时随地最新数据"，加真正的多设备实时同步（Supabase + 邮箱密码登录） | 代码已完成，等用户在Supabase后台执行一段SQL建表+权限规则后即可使用：接入 `@supabase/supabase-js`（CDN），设置页新增"实时云同步"区块（邮箱+密码 注册/登录/退出）。登录后：若云端已有数据会弹窗确认是否用云端覆盖本机（拒绝则反过来把本机数据推上云端），若是全新账号直接把本机数据推上去。之后每次记账（`saveEntries`/`persistCustomSubs`）都会防抖推送到云端；同时订阅 Supabase Realtime，其他设备一改这台会自动收到并刷新（用JSON对比过滤掉自己推送触发的回声，避免无意义重渲染）。GitHub Token刻意没有被同步到这里（保留原有Gist方案的隔离设计）。用Playwright注入了模拟版Supabase客户端，验证了"云端已有数据→弹确认→接受后本机数据变成云端数据"和"全新账号→不弹窗→直接推送本机数据"两条关键路径 | accountbook-20260731 |
| 2026-07-31 | 去掉"换设备同步（配置码）"功能；排查"无法注册"问题 | 已完成：配置码功能（`buildSetupCode`/`parseSetupCode`及相关UI、i18n、事件绑定）整段删除，Gist手动备份/恢复功能本身保留。顺带修了个真实的静默失败：如果 Supabase 的 CDN 库没加载成功（比如网络问题），之前点"登录/注册"按钮会完全没反应，现在会弹出明确提示"云同步服务还没加载好"，不会再看起来像卡住了 | accountbook-20260731 |
| 2026-07-31 | 登录/注册流程做完整：注册要验证密码、成功要提示并跳转登录页手动登录；登录成功要跳转对应页面 | 已完成：登录和注册拆成两个独立表单（一个"没有账号？去注册"/"已有账号？去登录"的文字链接切换），不再共用同一组输入框。注册表单加了"确认密码"字段，校验两次密码一致 + 长度≥6位。注册成功后（就算Supabase因为关闭了邮箱确认直接返回已登录状态）会主动登出，弹"注册成功，请用刚才的邮箱和密码登录"提示，切回登录表单并把邮箱自动填好，要求用户手动输入密码登录一次。登录成功后自动关闭设置面板并跳转到家计簿首页，不会停留在登录表单上。用Playwright注入模拟版Supabase客户端，完整跑通了"密码不一致→太短→注册成功→强制手动登录→登录成功跳转首页"这条完整链路 | accountbook-20260731 |
| 2026-07-31 | 接入真正的邮箱验证码流程（用户决定重新开启Confirm email而不是绕开），注册成功后要有验证码输入步骤 | 已完成：新增"输入验证码"这一步（`cloudVerifyModeBlock`）——注册后如果 Supabase 返回"未登录"（说明邮箱确认开着），就切到验证码输入界面，提示文字里带上具体发到哪个邮箱；输错验证码有提示，有"重新发送"按钮（`auth.resend`），验证成功（`auth.verifyOtp`）后直接完成登录并跳转家计簿首页。如果项目那边邮箱确认其实是关的（`signUp`直接返回session），走原来"注册成功→强制手动登录"那条路，两种情况自动兼容。用Playwright模拟"注册返回无session→验证码错→重发→验证码对"的完整链路，全部通过 | accountbook-20260731 |
| 2026-07-31 | 密码输入框加眼睛图标可显示/隐藏密码；日语注册按钮文字改回"新規登録" | 已完成：新增 `ic-eye`/`ic-eye-off` 两个图标，给GitHub Token、登录密码、注册密码、确认密码这4个密码框都加了眼睛按钮，点一下切换 `type="password"`/`"text"` 并换图标。日语的注册按钮文案从"登録"改回"新規登録"。用Playwright验证了点击后input的type确实切换、按钮文字确实更新 | accountbook-20260731 |
| 2026-07-31 | 登录/注册两个按钮没有点按交互反馈（用户截图指出），并要求以后任何新按钮都必须自带点按反馈 | 已完成：排查发现登录/注册按钮本身用的是`.save-btn`，本来就有`:active`效果；真正缺的是"没有账号去注册/已有账号去登录/重新发送/返回注册"这几个文字链接按钮（`.text-link-btn`），之前完全没写`:active`样式。补上了`.text-link-btn:active{opacity:0.5}`，并且加了个全局`document.addEventListener('touchstart',...)`空监听——这是修复iOS Safari上"没有触摸事件监听器时`:active`伪类不生效"这个已知限制的标准做法，让所有按钮的按压反馈在iPhone真机上也能可靠触发，不只是桌面浏览器好使。同时把"新按钮必须有点按交互反馈"加进了本文件顶部的长期规则。用Playwright读取实际生效的样式表规则确认`.text-link-btn:active`和`.save-btn:active`都存在且各按钮class绑定正确 | accountbook-20260731 |
| 2026-07-31 | 密码框眼睛图标不要缩短输入框、要放进框内；注册/登录成功都要有明确提示（尤其注册后要提示"验证码已发送"），不能悄无声息地切页面；确认登录后GitHub Token输入框仍为空是否是同步bug | 已完成前两项：新增`.pw-field-wrap`（相对定位容器）+ 把眼睛按钮从"输入框旁边另起一个圆按钮"改成绝对定位悬浮在输入框内部右侧，输入框本身宽度不再收缩，应用到GitHub Token、登录密码、注册密码、确认密码全部4处。注册提交后（邮箱确认流程）新增toast"验证码已发送至{email}"，登录成功、验证码验证成功后也各自新增了"登录成功"/"验证成功，欢迎回来"的toast，不再是切页面前毫无反馈。第三项排查后确认**不是bug**：GitHub Token字段从一开始就设计成只存在本机、不通过Supabase同步（云同步的数据表里根本不包含这个字段，是为了不把这种敏感凭据也塞进云端数据库），登录云同步账号后这个框保持空白是设计如此，账本里的记账记录本身应该已经正常同步；已跟用户说明并询问是否需要改成也同步 | accountbook-20260731 |
| 2026-07-31 | 确认GitHub Token只留本机不同步，并要求确认账目/自定义细分/语言货币设置是否都已进数据库 | 已完成确认：追踪`saveEntries()`/`persistCustomSubs()`两条实际保存路径，确认每次新增/编辑/删除账目、自定义细分改动都会调用`scheduleCloudPush()`防抖推送到Supabase，语言/货币设置也一起打包在同一个payload里；只有GitHub Token/Gist ID/token锁定状态和汇率/节假日缓存留在本机，且这些本来就不该同步（前者是敏感凭据，后者是可重新抓取的缓存），跟用户确认过维持现状 | accountbook-20260731 |
| 2026-07-31 | 添加子分类的页面要有明显的对勾保存按钮；新增/编辑子分类直接在小框内编辑，不要在下面另起一行；删除子分类直接在小框右上角放个×；统计页"2026年/7月/支出"这三个下拉控件样式太丑，要做好看点 | 已完成：子分类管理整个重做——点"+"或编辑铅笔后，那个小药丸框直接原地变成一个可编辑的输入框+右侧绿色对勾确认按钮（不再跳到网格下方另起一行），确认支持点对勾/回车/失焦三种方式（失焦兜底保留，避免不小心点别处丢失内容），Esc取消编辑。自定义子分类的删除×从"旁边一个独立圆按钮"改成绝对定位悬浮在药丸框右上角的小红点。三个下拉控件（年/月/支出收入切换）从"纯边框方块无箭头"重做成带箭头图标的圆角胶囊样式，年月选择用米色描边，支出/收入下拉根据当前选中类型变色（支出粉红配色/收入淡绿配色），呼应下方"支出/收入"大按钮的配色但故意避开完全相同色相。用Playwright验证了新增/编辑/删除子分类的完整链路和下拉切换后的变色效果 | accountbook-20260731 |
| 2026-07-31 | 首页年月切换：没有下个月数据时右箭头置灰，没有上个月数据时左箭头置灰 | 已完成：原来右箭头只是简单锁死"不能翻到当前月之后"，左箭头完全没有限制，可以无限翻到很久以前的空月份。改成按实际数据判断——`monthHasAnyEntries(offset)`检查对应月份是否存在任何账目，前后翻页按钮、以及卡片上的左右滑动手势都改成先检查相邻月份是否有数据，没有数据就不执行翻页，同时按钮本身用原生`disabled`置灰(新增`.ym-arrow:disabled`样式)。用Playwright模拟了"当前月有数据但上月没有→左箭头置灰"和"往回翻到有数据的月份→左箭头按该月份是否有更早数据重新判断"两种场景，行为符合预期 | accountbook-20260731 |
| 2026-07-31 | 新增两笔固定支出规则：①从下次周五起每隔两周300日元(メルペイ/趣味・娯楽>動画/备注pili)；②每月最后一天(遇休日顺延到下个营业日)600日元(Amazon/趣味・娯楽>動画/备注Amazon Prime) | 已完成：①原有的固定支出机制只支持"每月固定某天"，这次新增了一套"隔N天"的规则引擎(`RECURRING_BIWEEKLY_RULES`/`ensureBiweeklyRecurringEntries()`)，从锚点日期起每隔`intervalDays`天补一笔直到今天为止，不会补锚点之前的、也不会重复生成；今天(7/31,周五)"不算"，锚点定在下一个周五8/14。②沿用已有的月度规则引擎，直接加了一条`dayOfMonth:'last', bizDayAdjust:'after'`的规则（跟已有的Paidy iPhone那条规则同类型）。两笔都用到"趣味・娯楽>動画"这个细分——原分类库里没有"動画"这个中项目，新增为`hobby`分类下的常驻选项；新增了"Amazon"这个支付方式(橙色徽标)。用Playwright确认了`hobby`分类下能看到"動画"这个中项目 | accountbook-20260731 |
| 2026-07-31 | 集計页图例的百分比进度条截图看起来两条几乎一样长、颜色也不像对应色块——排查是否是bug | 已确认是真bug并修复：`.legend-pct-fill`是个`<span>`，`<span>`默认是行内(inline)元素，CSS里给行内元素设置`width`根本不会生效(这是CSS的基础规则)，所以93.3%和6.7%实际渲染出来都是0px宽，画面上看到的"两条差不多长的浅色条"其实只是外层灰色轨道本身，颜色对应和长度比例从来没生效过。改成`display:block`后用Playwright量了实际渲染像素——92.6%对应172px、6.6%对应12px、0.8%对应1.4px，轨道都是186px，比例和颜色都对上了 | accountbook-20260731 |
| 2026-07-31 | 年/月下拉控件重做方向定为"合并成一个胶囊，点击弹出年月选择面板"(先出的3版对比图选的C) | 已完成：`集計`页的两个`<select>`(年/月)换成一个"2026年7月"胶囊按钮，点击从底部弹出新的`ymPickerSheet`——年份步进器(‹2026年›)+12个月的九宫格，选完点"完成"才提交并关闭(不是每步都实时提交)，按天对比模式下月份格子正常显示，按月对比模式下只留年份步进器、月份格子整体隐藏。年份范围沿用原来"最近8年、不能选未来"的限制。过程中顺带发现`applyStaticI18n()`漏了给这个面板的标题和"完成"按钮赋值文字(截图看到按钮是空的)，一并补上了 | accountbook-20260731 |
| 2026-07-31 | 自定义细分：去掉单独的编辑铅笔按钮，点击自定义药丸直接进入编辑（不再用于"选中当前记账用的这个细分"）；点选细分后不要自动跳回记账表单，只记住选择，让用户自己点关闭退出 | 已完成：自定义药丸点一下直接变成可编辑输入框（复用之前做的对勾确认逻辑），不再有单独的铅笔图标；已经生成的自定义细分本来就是创建时自动选中，不需要再靠点它来选中。选中细分(不论预设还是自定义)那行代码里去掉了`setTimeout(closeCatPicker,180)`这个自动关闭逻辑，选中后面板保持打开，用户自己点‹返回或✕关闭。用Playwright验证了"选中预设细分后面板依然是open状态"和"点自定义药丸直接进入可编辑输入框(不再需要先点铅笔)"两条 | accountbook-20260731 |
| 2026-07-31 | 手机"添加到主屏幕"的PWA更新/同步比电脑浏览器慢很多；汇率页把日元→人民币换到第一个、设为默认方向 | 前者已缓解：加了`Cache-Control`/`Pragma`/`Expires`这几个no-cache的meta标签减少iOS对主文档的本地缓存倾向；更关键的是补了一个`visibilitychange`/`pageshow`监听——iOS切到后台一段时间后经常会把Supabase Realtime的WebSocket连接冻结，回到前台不一定会自动补拉数据，现在每次App重新可见时会主动拉一次云端最新数据并重启订阅，不再被动等它自己恢复(这是这次"同步慢"最可能的根因，静态资源缓存只是次要因素，无法在GitHub Pages上做更彻底的服务端控制)。后者已完成：`日元→人民币`换成默认方向和第一个按钮，`人民币→日元`挪到第二个；顺带发现并修了一个真bug——`rateSubReverse`这个i18n key代码里在用但从来没定义过，切到反向时标题栏其实一直显示的是"rateSubReverse"这个原始key名而不是翻译文字，这次一并补上了中日文案 | accountbook-20260731 |
| 2026-07-31 | 重新设计首页/主屏幕icon：先出了仿GitHub风格的全新猪形象几版(黑底白猪等)，用户反馈"有点猎奇"，迭代修好腿/耳朵/尾巴跟身体的缝隙问题，又反馈耳朵位置古怪，最后决定不用全新设计，改成在**现在正用的**图标基础上改：去掉"家計簿"文字和英文小字、图案居中放大、星星放大一号，背景+主色出了多版(黑/米白/珊瑚红/墨绿/彩虹斜向渐变等)，最终选定：彩虹斜向渐变背景(单次红→橙→黄→绿→蓝→紫扫过)+白色猪(带左上方向高光/阴影/投影的立体质感) | 已完成并应用到实际代码：整个过程都是先在scratchpad里出图给用户看、确认后才动正式代码，没有一次性直接改。最终版本从现有的192x192图标里用像素扫描定位"猪+星星"图形的边界框(排除下方的文字区域)，裁出来重新居中放大、去掉文字，星星用连通域检测单独放大35%，再用numpy生成斜向彩虹渐变背景+高光/阴影/投影效果合成成新图。已经替换进index.html的全部5处图标引用(32x32 favicon、180/152/120三个apple-touch-icon、manifest里的192x192和512x512)，Playwright验证过页面正常加载 | accountbook-20260731 |
| 2026-07-31 | 询问登录状态能保留多久、想加个"记住密码"功能；顺便问収入/支出翻译要不要改成入金/出金 | 已回复：登录状态本来就会一直保留(Supabase客户端默认把session存在localStorage并自动刷新token，除非手动退出登录或清浏览器数据，不需要额外开发)。"记住密码"没有做成"App自己存密码"（明文存密码是真实安全隐患，不能这么做），而是修了一个真实存在的问题——登录/注册的邮箱密码输入框虽然早就写了`autocomplete`属性，但外层缺一个真正的`<form>`标签(是用裸`<div>`+按钮`click`事件拼的)，导致Safari/Chrome自带的"记住密码"弹窗大概率不会触发，因为浏览器主要靠表单`submit`事件判断这是不是一次登录动作。给登录和注册分别包了`<form>`，按钮改成`type="submit"`，回车也能直接提交了，这样系统自带的密码管理器就能正常弹出"要保存这个密码吗"。収入/支出翻译问题：解释了这是日本家计簿类App的标准用词(Zaim/MoneyForward等都这么用)，入金/出金是银行存取款场景的词，用在这里更别扭，建议不改，用户同意维持原样 | accountbook-20260731 |
| 2026-07-31 | 想把"数据要改成从数据库拉取"，问是不是就不用同步了、存库时有没有记录用户名、现在是不是所有数据都读数据库 | 已回复+已改：查证后回复——数据库那边确实是按`user_id`分账号存的(有RLS权限隔离，不会混数据)，但目前架构是"本机优先、云端做同步"，不是"永远直接读数据库"。跟用户确认了两种改法的取舍(纯数据库驱动=没网就完全不能用 vs 保留本机做缓存但不再手动确认覆盖)，用户选了后者。已完成：`handleCloudSignedIn()`里去掉了登录时"云端已有数据，要不要覆盖本机"的确认弹窗，改成云端数据库永远是唯一权威来源——只要云端有数据就直接采用覆盖本机，不用户不用再做选择；全新账号(云端还没数据)才会把本机数据推上去当初始值。本机localStorage还是保留着，作为离线时的缓存/兜底，不受影响。用Playwright模拟"登录时云端已有数据"的场景，确认不再弹确认框、本机直接被覆盖成云端的数据 | accountbook-20260731 |
| 2026-07-31 | 检查之前加的几笔固定支出/收入规则是不是都正常落库了 | 排查后发现一个真实的数据丢失bug并修复：`init()`里原来的顺序是先生成本月固定收支(`ensureRecurringEntries`)，最后才去处理云同步(`initCloudSync`)。这样一来，每次重新打开App如果正好要新生成一条本月固定收支(比如月初的工资)，当时`cloudUser`还没设置好，推送云端的动作会被静默跳过；紧接着`initCloudSync`又会把云端那份"还没有这条新记录"的旧数据拉回来整个覆盖本机——等于这条固定收支既没推上数据库，本机也被覆盖丢了，且不会有任何报错提示。只有首次登录当次能正常推送(因为走的是登录成功那条推送逻辑，不受这个顺序问题影响)，之后每个月自动生成的固定收支都会中招。修法：把`initCloudSync`挪到`ensureRecurringEntries`/`ensureBiweeklyRecurringEntries`之前，先同步好云端最新状态再在这个基础上补生成本月还缺的固定收支，这样生成出来的新记录能正常推送。用Playwright模拟"云端有旧数据+本机是空的+需要新生成本月固定收支"的场景，确认修复后本机同时保留了云端旧数据和新生成的固定收支，且新记录被正确推送到了数据库 | accountbook-20260731 |
| 2026-08-01 | 数据库结构重新设计：不要单行大JSON存储，分类/子分类/支付方式要正规入库，预设和自定义用字段区分而不是写死在代码里 | 方案设计阶段（发过多版设计文档给用户确认后才动代码）：确定改成6张表——`categories`(大类)、`subcategories`(细分)、`payment_methods`(支付方式)三张表统一用"预设/自定义同表存放，`is_preset`字段区分、`user_id`为空代表预设"的模式（RLS双策略：`is_preset=true`允许所有人只读，`auth.uid()=user_id`允许本人读写自己的自定义行）；`user_settings`加`email`(建`handle_new_user()`触发器在注册时自动写入，不用App管理)和`username`(先建字段，暂不使用)字段；`entries`表改成一行一笔账目而不是一个JSON字段，`sub_key`/`payment_method`都改成必填且外键关联对应表。最终版本经过多轮用户反馈调整：分类不再在JS代码里写死当离线兜底，改成"从数据库拉取+缓存到本机"，离线时读缓存；`sub_key`/支付方式补成必填并外键化。用户确认方案后拉了新分支 `accountbook-20260801_DBUpdate` | accountbook-20260801_DBUpdate |
| 2026-08-01 | 执行数据库迁移SQL；协助排查DBeaver连接Supabase数据库（先后遇到字段填错、SSL证书PKIX校验失败、IPv6直连超时、密码错误等问题） | 已完成：生成完整迁移SQL（建5张表+RLS策略+`handle_new_user`触发器+27个分类/80个子分类/7个支付方式的预设种子数据，从原本写死在代码里的数组原样转换而来），用户在Supabase SQL Editor执行成功。DBeaver连接排查：SSL证书PKIX校验失败→引导手动下载最新PostgreSQL JDBC驱动jar替换；直连超时(Supabase直连默认走IPv6)→改用Session Pooler连接参数；再次超时怀疑本地网络/防火墙问题→建议换网络或改用Transaction Pooler端口6543，同时说明Supabase自带的Table Editor/SQL Editor本身已经足够用，DBeaver只是锦上添花；密码认证失败→引导用户重置数据库密码。用户最终确认DBeaver连接成功、数据库表结构确认无误 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 按新的数据库表结构重写App代码：分类/子分类/支付方式改成从数据库拉取+本机缓存，账目改成逐行同步，去掉整块JSON同步逻辑 | 已完成：`expenseCats`/`incomeCats`/`paymentMethods`三个数组不再写死在代码里，改成`loadCatalogData()`——先读本机缓存(`ledger_catalog_cache`)兜底展示，再异步从`categories`/`subcategories`/`payment_methods`三张表拉取最新数据覆盖缓存；账目的增/删/改各自单独调用`pushEntryUpsert`/`pushEntryDelete`/`pushEntriesBulkUpsert`同步单行到`entries`表，不再是整个JSON一起推送；Supabase Realtime订阅从"整块替换"改成按`INSERT`/`UPDATE`/`DELETE`事件逐行patch本机`entries`数组；自定义子分类的增/改/删也改成直接操作`subcategories`表而不是本机一个`customSubsMap`字段；GitHub Gist备份逻辑相应调整为从`allCatMap`里筛自定义细分打包，恢复时直接合并进`allCatMap`。过程中还顺带发现并修了一个真实的空数据崩溃bug：如果分类数据既没有缓存也没能联网拉取成功（比如全新设备第一次打开就没网），选分类相关的函数会因为`cats[0]`是`undefined`而报错崩溃，改成判空后显示"分类数据加载中，请联网后重新打开App"的提示而不是白屏崩溃。用Playwright分别验证了：数据库拉取渲染正常、离线读缓存兜底正常、账目增删改各自的推送/删除调用参数正确、自定义细分增删改调用参数正确、Realtime收到其他设备的INSERT/UPDATE/DELETE事件后本机账目列表和本地存储都正确patch、重复回声事件不会造成多余数据。全文件`node --check`语法检查通过，`div`/`span`标签数量核对配对无误 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 固定收支要在指定日期当天由服务器自动写入数据库，不依赖App是否打开，写入前判断是否休日 | 已完成：设计并提供SQL脚本，用Supabase自带的`pg_cron`定时任务，在数据库里新建`is_biz_day()`/`adjust_to_biz_day()`(跟App里`isBizDay()`/`adjustToBizDay()`逻辑一致)和`generate_recurring_entries()`(把`RECURRING_RULES`/`RECURRING_BIWEEKLY_RULES`原样搬到数据库里，用固定id防重复，绑定`zero121235@outlook.com`这个账号)，每天定时执行一次。过程中排查了两轮报错：①`create policy`不支持`IF NOT EXISTS`，脚本重跑时因为策略已存在而报错，改成先`drop policy if exists`再建；②最初设计让数据库自己用`pg_net`直接请求祝日API(`net.http_collect_response`)，结果这个函数在用户的Supabase实例上已废弃，脚本执行卡死不动。改成不在数据库里发HTTP请求，复用App本来就有的祝日抓取逻辑——`fetchJpHolidays()`成功后顺手把数据也推一份到新建的`jp_holidays`表(`pushJpHolidaysToCloud()`)，数据库这边只读表不用自己发请求，问题解决。最后用DBeaver查`pg_proc`/`cron.job`确认三个函数和每日定时任务都已正常建立 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 排查"第一次进入App时短暂显示中文和人民币"（此前已设置默认日语/日元） | 排查后发现是这次数据库重写引入的真实回归：`loadCatalogData()`原来不管本机有没有缓存，都会`await`一次到Supabase的分类数据网络请求，请求没返完不会往下走`applyStaticI18n()`(负责把语言/货币文案换成设置里的语言)。改库之前分类是写死在代码里的，这一步是瞬间完成的；改库后这一步变成了真实的网络请求，网速一般或稍慢时，用户会先看到HTML里原始写的中文/人民币占位文案，过一会儿才跳成日语/日元。修法：`loadCatalogData()`改成——如果本机已经有分类缓存，直接用缓存立即渲染，不再等网络请求，网络刷新放到后台异步进行（成功后再补一次`applyStaticI18n()`+`renderAll()`静默更新最新数据）；只有真的什么缓存都没有（比如全新设备第一次用）才会像以前一样老老实实等网络请求。用Playwright模拟"本机已有缓存+网络故意设置3秒延迟"的场景验证修复后首屏能立刻用正确的语言渲染，不再等网络 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 金额数值显示要按三位一逗号加千分位分隔 | 已完成：先出对比图给用户确认。所有金额显示统一走`fmtCur()`这一个函数，改成用`Number(amount).toLocaleString('en-US', {...})`生成带千分位逗号的数字字符串，一改全生效——首页本月结余/收入/支出、账目列表每一笔、集計页图例金额全部自动带上逗号(例如289346→289,346)。新增/编辑账目的金额输入框本身不受影响，不加逗号(避免打字时被逗号干扰)。用Playwright验证大额数字(289,346)分隔位置正确 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 数据都已经改成走数据库了，GitHub Gist手动备份/恢复功能可以去掉了 | 已完成：设置页里"云备份（GitHub Gist）"整块区域(Token/Gist ID输入框、立即备份/从云端恢复按钮、编辑锁定图标、说明文字)连同对应的`scheduleGistBackup`/`pushBackupToGist`/`pullBackupFromGist`/`buildBackupPayload`/`extractCustomSubsForBackup`/`renderBackupStatus`/`renderBackupLockState`等全部JS函数、`gistSyncing`/`gistBackupTimer`状态变量、中日文i18n词条、事件绑定一并删除；`saveEntries()`里原来触发防抖备份的调用也一起去掉。用Playwright确认设置面板打开正常、Gist相关DOM元素全部不存在、旁边的语言/货币/实时云同步几块设置不受影响 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 排查"刷新页面之后老是自动变回中文/人民币"（不是缓存问题，是持续性的） | 排查后发现是数据库迁移脚本里的真实bug：`user_settings`表的`lang`/`currency`列建表时给了硬编码默认值`'zh'`/`'CNY'`，`handle_new_user()`注册触发器和一次性补录语句也都硬编码插入这两个值，导致用户的设置行从一开始就是错的（跟实际使用的日语/日元不符）。App每次登录同步都会把这行"看起来是设置过、其实是我写错的默认值"当成权威数据拉回来覆盖本机正确设置，所以每次都变回中文/人民币。修法：提供SQL直接改正现有账号的这一行，同时去掉列的默认值和NOT NULL限制、触发器不再顺手带上错误默认值，避免以后再出现同样问题；App代码本身不用改（`remoteSettings.lang || settings.lang`这种写法本来就能正确处理NULL）。过程中还顺带发现另一个真实bug：`initCloudSync()`(已登录状态下重新打开App这条路径)拉到云端修正后的设置后，只更新了内存里的`settings`对象，没有像登录流程`handleCloudSignedIn()`那样再调一次`applyStaticI18n()`刷新界面文字和语言/货币按钮的选中状态，导致哪怕数据库已经修好，界面上设置面板的高亮按钮还是停留在刷新前的旧状态。补上了这次遗漏的`applyStaticI18n()`调用。用Playwright模拟"本机缓存是旧的zh/CNY+数据库已经是正确的ja/JPY"的场景，确认修复后设置面板能正确显示日本語/日元为选中状态 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 精简"实时云同步"区域的说明文字，去掉"（测试中）"标签（因为Gist已删除，数据库同步现在是唯一的数据保存方式） | 已完成：中日文字段标题从"实时云同步（测试中）"/"リアルタイム同期（テスト中）"改成"实时云同步"/"リアルタイム同期"；下方说明文字去掉了对已删除的Gist手动备份/恢复功能的过时对比措辞，改成准确描述"账目、分类、设置都会自动实时同步到数据库" | accountbook-20260801_DBUpdate |
| 2026-08-01 | GitHub Pages部署后CDN缓存要等一阵子才刷新，用户手动清缓存不现实，需要App自己能提示"有新版本" | 已完成：新增`APP_BUILD`版本号常量(每次实质性部署手动改一下)+`checkForAppUpdate()`——定期(每3分钟)或App从后台切回前台时，用带时间戳的参数悄悄重新拉一次自己这个页面(绕过缓存)，从返回内容里读出最新的`APP_BUILD`跟当前运行的比对，不一样就在顶部弹出一条"有新版本可用/立即刷新"的绿色提示条，点了才会`location.reload()`，不会自动强制刷新打断正在填的账目。用Playwright模拟"服务器返回的新页面版本号不同"的场景验证提示条正确出现、点击刷新按钮能正常触发 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 為替页的"简易换算"卡片改成用户提供的参考样式(顶部汇率提示文字+金额/换算为两行，每行左边数字右边国旗+币种，中间绿色圆形上下箭头切换方向) | 已完成：先出对比图确认样式，再改代码。顶部新增`converterRateLine`，复用已有的`displayRate()`/`rateUnitLabel()`直接拼出"1 JPY = 0.0470 CNY"这种格式；两行换算框改成"标签+圆角边框行(数字输入+国旗emoji+币种代码)"，中间加`converterSwapBtn`圆形按钮，点击复用原有方向切换逻辑(把原来写在按钮click里的代码提成`setRateDirection(dir)`公共函数，上方的"日元→人民币/人民币→日元"分段按钮和这个新按钮共用同一份逻辑，不重复维护)。原来按方向切换显示"人民币(CNY)"/"日元(JPY)"整句文字的`converterCNYLabel`/`converterJPYLabel`废弃，改用固定的国旗+币种代码，不再需要按语言翻译整句货币名。用Playwright验证了默认方向下国旗/代码/金额换算结果正确、点击切换按钮后双方向都能正确互换 | accountbook-20260801_DBUpdate |
| 2026-08-01 | "有新版本"提示条样式不好看(纯色通栏、挡住右上角徽章)，改成可折叠的右边缘小标签；点更新后要转圈提示"更新中"，刷新完提示"更新完毕"停留3秒；顺便优化App打开时数据加载慢的问题 | 已完成，两部分改动一起上线。**提示样式**：先用可分享链接的Artifact交互原型反复调整（位置/大小/透明度/颜色/按钮位置/文字换行都按用户在原型上确认的效果来），最终定案：折叠态是贴右边缘的26×41px半透明毛玻璃小标签(红色`--seal`，带脉冲小圆点)，点击展开一张同样半透明的卡片(标题+"更新"小按钮+两行说明文字)，卡片左侧有收起箭头；点"更新"后卡片内容换成转圈图标+"更新中..."，触发`location.reload()`前用`sessionStorage`打一个标记，刷新完成后读到这个标记就弹出"更新完毕"提示并3秒自动消失；点更新后标签和卡片都彻底消失(不留折叠态)。替换了原来写死在index.html里的顶部通栏(`update-banner`)实现。**加载优化**：排查发现`initCloudSync()`/`handleCloudSignedIn()`里拉分类、账目、设置三个云端请求原来是依次`await`(等于三次网络往返串行叠加)，改成`Promise.all()`并行拉取，三个请求同时发出只等最长的一个；另外`init()`原来要等云同步全部跑完才第一次`renderAll()`，改成本机数据加载完就先渲染一次首屏(用缓存/本机数据)，云同步和固定收支生成完成后再正确渲染一次，用户不用整个等云端返回才看到内容。用Playwright模拟"本机有缓存数据+云端请求故意延迟1.5秒"的场景，确认本机数据渲染出现的时间点比云端数据到达早了约1.5秒(跟延迟设置吻合)，验证首屏不再被网络卡住 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 為替页换算卡片"换算后"标签去掉，整体留白太多需要调整布局 | 已完成：删掉"换算为"/"換算後"这个字段标签(连同对应的`converterResultFieldLabel`元素、i18n词条、`applyStaticI18n()`里的赋值)，两行输入框直接挨着中间的切换按钮；顺带收紧了间距——汇率提示行、输入框行、切换按钮的上下margin都调小，视觉上更紧凑不空旷 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 用户截图对比集計页"支出/收入"两种视图：收入视图分类少、图例内容短的情况下，底部标签栏(家計簿/集計/為替)位置看起来往上移了，问能不能让它始终固定在最下方 | 已加固处理，但受限于沙盒环境无法在真机iOS上复现验证。这类问题的根因是之前(7/31)就记录过的同一类iOS怪癖——内容高度如果和可视区域高度完全一致(不多不少、无法滚动)，部分iOS版本会把`position:fixed`的底部导航栏渲染错位。之前的修法是让页面故意比一屏多留1px，这次发现"内容非常短"(比如收入只有一个分类、图例只有一行)的极端情况下，光靠flex拉伸+1px余量在某些情况下可能还是不够保险。这次两处加固：①`.page.active`的最小高度富余量从1px加大到24px；②集計页负责撑满剩余空间的图表卡片增加380px的保底最小高度，不完全依赖flex-grow的实时计算。用Playwright验证了"收入视图只有1个分类"这种短内容场景下卡片和页面高度都有明显撑满，Chromium环境下tabbar本身位置正常(但这不能100%代表iOS Safari/PWA独立模式下的真实渲染情况，需要用户在真机上确认这次是否解决) | accountbook-20260801_DBUpdate |
| 2026-08-01 | 分类选择页不需要显示✕号；点返回按钮要记住已选分类；点空白处退出要清空所有信息，下次新建分类回到默认 | 已完成：去掉分类选择页(`catPickerSheet`)标题栏上的✕关闭按钮(连同对应事件绑定)，只保留‹返回按钮；‹返回按钮走的`closeCatPicker()`本来就不清空`selectedCat`/`selectedSub`，天然满足"记住已选分类"；点空白处退出走的`closeAllSheets()`(遮罩层`overlay`的点击事件也绑定在这个函数上)新增清空`selectedCat = null; selectedSub = null;`，下次点"+"新建时`ensureSelectedCat()`会因为没有已选分类而自动回退到第一个默认分类。用Playwright验证了✕按钮已不存在、切换分类后点返回能保留选择、点遮罩层关闭后再次新建分类确实回到默认第一项 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 用户截图反馈：真机上展开的"有新版本"卡片和左侧收缩箭头之间脱开了一段距离，跟原型不一样 | 找到并修复了一个真实bug：写正式代码时把原型里"标题文字和更新按钮之间"的`gap:20px`误抄成了外层`.update-expand-wrap`(收缩箭头和卡片的容器)的间距，导致箭头和卡片之间凭空多出20px空隙，看起来像是脱离的两个东西。改成`gap:0`让两者紧贴。顺带说明截图里箭头上方那个虚线圆圈✕按钮不是这次功能的一部分——那是App里另一个已有面板自带的关闭按钮样式(`.icon-btn`本来就是虚线圆边框+✕)，凑巧在同一张截图里挨得比较近，容易被误认为是新功能的一部分。用Playwright量了修复后箭头右边缘和卡片左边缘的像素距离，确认是0 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 收缩箭头应该对齐卡片顶部而不是垂直居中 | 又是一处写代码时漏掉的细节：`.update-expand-wrap`用的是`align-items:center`，跟之前原型阶段特意确认过的"箭头固定在卡片顶部"要求不符(箭头41px矮、卡片70px高，居中会导致箭头上下都留白，顶部对不齐)。改成`align-items:flex-start`。用Playwright量了修复后箭头和卡片的顶部坐标，确认完全对齐 | accountbook-20260801_DBUpdate |
| 2026-08-01 | 数据库迁移相关工作告一段落，另拉一个不带`_DBUpdate`后缀的正常日常分支，后续更新都推送到这里；首页环状图点击年月文字也会弹出本月明细，应该只有环状图本体能触发 | 已完成：从`accountbook-20260801_DBUpdate`最新状态切出`accountbook-20260801`分支并推送，后续开发在这个分支继续。环状图bug：`#homeDonutCard`整卡绑了一个点击弹出本月明细的处理器，原来只排除了`.ym-arrow`(年月左右箭头按钮本身)，没排除年月文字(`.ym-picker`容器里的`.ym-label`)，点文字部分会穿透触发弹窗。排除条件从`.ym-arrow`改成`.ym-picker`(箭头+文字一起排除)，环状图本体和下方"点击查看本月明细"提示文字保持可点 | accountbook-20260801 |
| 2026-08-01 | 账目明细里+/-号跟金额数字混在一起分不清，要突出显示 | 已完成：先出多轮方案图确认（圆形/方形色块、符号放大、箭头图标等），最终定稿——20px圆形浅色底徽标，符号用SVG描边画的粗+/-（不用文字字符，避免不同字体粗细渲染出来看不清），颜色跟金额同色系(支出用--seal浅底深符号、收入用--jade浅底深符号)，紧贴在金额数字左边(3px间距)。新增`signBadgeHtml(type)`函数生成徽标HTML，替换掉原来直接拼在金额字符串前面的纯文字`+`/`-`。用Playwright确认徽标尺寸20px、颜色跟支出/收入分类对应正确 | accountbook-20260801 |
| 2026-08-01 | 支付方式的メルペイ/楽天/Amazon能不能用各家真实品牌Logo | 已完成Amazon和楽天，メルペイ暂缺素材：从开源图标库找到能免费嵌入的官方Logo矢量图——楽天用Simple Icons(CC0协议)，Amazon用Font Awesome Free(CC BY 4.0协议)，都合法可用；メルペイ是日本本土小众服务，这两个库都没收录，这个环境也连不上Mercari官网，暂时维持原来的"メ"字母徽标。代码新增两个真实Logo的SVG符号(`ic-pm-amazon`/`ic-pm-rakuten`)，App原有图标都是线条描边风格(stroke，`.hd-icon`默认`fill:none`)，这两个是实心品牌图形，新增`BRAND_LOGO_COLORS`映射表按图标id单独指定`fill`颜色覆盖默认描边样式。数据库那边`payment_methods`表里`amazon`/`rakutenpay`两行的`icon`字段要改成`pm-amazon`/`pm-rakuten`、`badge_bg`/`badge_text`改成NULL(改用图标而不是色块字母)，SQL已发给用户在Supabase执行。用Playwright模拟更新后的数据库行确认两个真实Logo能正确显示对应品牌色 | accountbook-20260801 |
| 2026-08-01 | Amazon/楽天真实Logo跟文字没对齐，看起来偏下 | 已修复：用Playwright截图放大核对，两个Logo各自的路径在其SVG viewBox里其实是居中的，不是素材本身的问题；根因是实心填色图形和App原有的描边线条图标的"视觉重心"不一样，沿用同一套`vertical-align`数值会让实心图形看起来偏低。给这两个品牌Logo单独加了`transform:translateY(-2px)`上移微调，截图对比现金图标确认对齐明显改善 | accountbook-20260801 |
| 2026-08-01 | メルペイ的图标补上（用户提供官方素材压缩包，指定用其中的Mercari品牌图标代表メルペイ） | 已完成：用户提供的官方素材压缩包(`mercari_service_logos.zip`)里都是Mercari(フリマ本体App)品牌资产而非メルペイ(支付服务)专属素材，两者是同集团下的不同品牌/不同Logo；跟用户确认后按用户意愿直接用Mercari的六边形"m"图标(`new_mercari_icon_iOS.png`)代表メルペイ这个支付方式。因为是实心多色填色图(红色六边形+白色m+蓝色圆点)不是简单单色矢量图形，跟Amazon/楽天的处理方式不同：用Python裁掉四周空白后压缩到96×96并转成base64内嵌的PNG(`<img>`标签)，新增`BRAND_LOGO_RASTER`映射表(区别于矢量图标用的`BRAND_LOGO_COLORS`)，`pmIconHtml()`优先检查这个映射表。用Playwright在选择支付方式列表(20px)和账目明细小徽标(12px)两种尺寸下都截图确认清晰可辨认。数据库`payment_methods`表`merpay`行的`icon`字段要改成`pm-merpay`、`badge_bg`/`badge_text`改成NULL，SQL已发给用户执行 | accountbook-20260801 |
| 2026-08-01 | ペイディ的图标也补上（用户直接贴图指定用这张，只要图标不要文字） | 已完成：Paidy在Simple Icons/Font Awesome/gilbarbara-logos等开源图标库里都没收录，用户直接在对话里贴了官方Logo图。贴图是内联粘贴而非文件上传，一开始拿不到原始像素文件；后来发现Claude Code的会话transcript(JSONL)里其实完整保存了每张贴图的原始base64数据，从transcript里按图片大小定位并提取出真实的原图文件，不用再让用户重新上传。用Python按"品红/洋红色像素"做bbox裁剪(排除黑色的"paidy"文字部分，第一版裁剪没做颜色过滤导致文字边缘的抗锯齿像素被误裁进来)，抠出纯图标(带透明背景，原图本身就是PNG透明底)，居中放到正方形画布缩到120×120，跟メルペイ一样走`BRAND_LOGO_RASTER`这条渲染路径新增`pm-paidy`键。中途发现一个操作失误：第一次把base64贴进代码时被终端输出截断了(15124字符的原文只贴进去12701字符)，导致图标数据损坏；改用Python脚本直接从源文件读取完整字符串做替换，避免再手抄大段base64出错，并解码校验字节数与原文件一致。用Playwright截图确认Amazon/楽天/メルペイ/ペイディ四个真实Logo放在一起显示都正常清晰。数据库`payment_methods`表`paidy`行的`icon`字段要改成`pm-paidy`、`badge_bg`/`badge_text`改成NULL，SQL已发给用户执行 | accountbook-20260801 |
| 2026-08-01 | 用户真机确认Amazon/楽天真实Logo现在反而偏上了，之前加的`translateY(-2px)`上移微调改过头 | 已完成：把之前(7/31那次)给`BRAND_LOGO_COLORS`路径加的`transform:translateY(-2px)`直接去掉，恢复成跟`BRAND_LOGO_RASTER`(メルペイ/ペイディ)一样不额外调整位置，跟Playwright截图对比现金/メルペイ/ペイディ都在同一行确认新的对齐效果正常 | accountbook-20260801 |
| 2026-08-01 | 剩下的三个预设支付方式(现金/信用卡/银行转账)也要改成彩色图标，跟其他几个真实Logo画风统一 | 已完成：这三个不是具体品牌，没有"真实Logo"可用，先出两版配色预览图(贴合App暖色调 vs 更鲜艳的Fintech常见色)截图发给用户选，用户选定后又要求信用卡单独改成红色，再出一版预览图确认(现金jade绿、信用卡红、银行转账金色)才写入正式代码。新增三个实心多色符号`ic-pm-cash`/`ic-pm-credit`/`ic-pm-bank`(色块直接写死在symbol内部的path/rect上，不是用CSS统一上色)，新增`MULTI_TONE_PM_ICONS`列表标记这三个走"stroke:none"的彩色渲染路径(区别于`BRAND_LOGO_COLORS`单色fill覆盖、`BRAND_LOGO_RASTER`内嵌PNG两条已有路径)。用Playwright截图确认7个支付方式(现金/信用卡/银行转账/Amazon/楽天/メルペイ/ペイディ)放在一起画风统一。数据库`payment_methods`表`cash`/`credit`/`transfer`三行的`icon`字段要分别改成`pm-cash`/`pm-credit`/`pm-bank`，SQL已发给用户执行 | accountbook-20260801 |
| 2026-08-02 | 想加一个记录资产余额(银行卡等)的地方，讨论后决定不做：多账户手动记录资产的想法在讨论到"记账流水不会实时从资产扣款"这个问题后，用户决定放弃(那就算了) | 未实施，作废。曾经出过完整设计预览(资产Tab页面mockup+`assets`表结构SQL)但用户最终放弃，不写入代码 | - |
| 2026-08-02 | 用户提议的柱状图(集計页`trendChart`)样式修正：柱子宽度/间距、标签/刻度文字有意见，一共出了9版方案(A现状对比/B网格线+刻度/C背景轨道+顶部数值/D胶囊软萌/E和风虚线描边/F极简大字号/G并排分组/H叠纸阴影/I周末高亮)供选，最终用户觉得都不满意，决定先不改 | 未实施：9版方案截图分3轮发给用户看，用户回复"没什么合适的，先不改了"，未写入代码，`trendChart`维持原样 | - |
| 2026-08-02 | 需要一个能看"累计结余"(不止当月)的地方，且要能自选统计的起止年月区间；讨论后确定放在集計页(不是首页)，做成一张新卡片 | 已完成：出了3版放置方案预览图(首页大数字下加副标题/首页三栏统计/独立小卡片可点开看趋势)，用户选首页方案但改口要挪到集計页并要可选年月区间，再出一版集計页新卡片设计(起止年月胶囊+大数字净结余+区间总收入支出)确认后实现。集計页`日别比较/月别比较`分段下新增"区间累计结余"卡片，新增`cumRangeSheet`起止年月选择面板(复用已有`year-stepper`+`month-grid`样式，用一个"起始/截止"分段切换编辑哪一头，避免另外做两套选择器)。默认起始=有记录以来最早一笔账目的年月(不是随便写死的日期，按用户要求)，默认截止=当前年月；区间起止如果选反了(起始晚于截止)会自动交换而不是报错打断。计算逻辑复用已有`toBase()`按记账当天历史汇率换算，跟本月结余的算法一致。这个区间选择是当次会话的临时状态，不落库/不进本机缓存，每次重新打开App都会回到"从有记录以来"这个默认区间。用Playwright验证了默认区间正确(最早一笔账目年月→当前年月)、修改起始年月后再次打开截止年月不受影响(两边分开的草稿状态)、零账目情况下不报错、中日文文案都正确 | accountbook-20260801 |
| 2026-08-02 | "区间累计结余"卡片层级放错了：日別/月別比较分段是柱状图那组的东西，新卡片不该夹在两者中间 | 已修复：纯粹调整HTML里几个`<div>`块的先后顺序，把`cumulative-range-card`从"日別/月別分段"和"今日对比卡片"中间挪到最前面(紧跟在页面标题下面，日別/月別分段之上)，逻辑和渲染函数完全不用改。用Playwright确认挪动后功能不受影响、外观上"区间累计结余"现在是独立一段，不再夹在日別/月別分段中间 | accountbook-20260801 |
| 2026-08-02 | "累计结余"卡片文字精简：去掉"区间/期間"这个前缀，下面两个统计直接叫"累计收入"/"累计支出" | 已完成：起止年月胶囊已经明确表达了这是一个可选区间，标题/统计标签里再重复"区间/期間"字样显得啰嗦，`cumRangeTitle`/`cumRangeIncomeLabel`/`cumRangeExpenseLabel`三个i18n词条中日文都精简成"累计结余/累计收入/累计支出"("累計残高/累計収入/累計支出")，纯文案改动，逻辑不变 | accountbook-20260801 |
| 2026-08-02 | 日别对比的柱状图默认停留位置要固定在"当天"，不要靠数据推算 | 已完成：`renderTrendChart()`原来的自动定位逻辑是"滚到当前选中类型(支出/收入)最后一笔有数据的那一段"(7/31为了让工资那种月中才发生的收入不被滚过头加的)，现在改成——如果是"按日对比"且看的正是当前这个真实月份，直接找`buckets`里`key`等于`todayStr()`的那一格定位过去，不管那天有没有数据；只有不是当前月(翻看历史月份)或者切到"按月对比"时才退回原来"最后一笔有数据"的逻辑。用Playwright模拟"今天是月初、但月底25号也有一笔支出"的场景，确认定位停在月初(今天)而不是被数据拉去月底 | accountbook-20260801 |
| 2026-08-02 | 点击柱状图的某一根柱子，需要弹出这天/这个月的具体金额和分类颜色图例 | 已完成：给`trendChart`每一根柱子上加了一个覆盖整列高度的透明点击层(`.trend-bar-hit`，不用非得精准点在色块上，点柱子上方空白处也算数)，点击后：①用一个半透明的印章红底色矩形(`#trendSelHighlight`)高亮整列，②在图表下方新增的`trendPointInfo`面板里显示"日期/月份 + 总金额(支出红/收入绿) + 每个分类的色块小方块+分类名+金额"明细列表，复用了已有`colorForKey`/`labelForKey`/`fmtCur`等函数，颜色跟柱子里的色块、下方汇总图例保持一致。用Playwright验证了点高柱子(某天两个分类堆叠)和点矮柱子(验证高亮矩形在柱子上方空白处可见)两种情况，金额、分类名、颜色都对得上 | accountbook-20260801 |
| 2026-08-02 | 柱状图加横向网格线和纵轴数值(之前9版样式对比时的方案B，单独拿出来实现) | 已完成：`renderTrendChart()`新增左边距(`leftPad`)给纵轴数值腾地方，从0到当前最大值分4档画横向虚线网格+对应数值标签(超过1000自动显示成"1.2k"这种缩写)，原来单独画的底部基准线直接被网格线的0刻度取代，不用两份重复。柱子/点击热区/默认定位到今天/点击高亮矩形这几处横坐标计算都相应加上`leftPad`偏移，逻辑本身不变。用Playwright截图确认网格线、数值刻度(0/525/1.1k/1.6k/2.1k)、点击后的高亮和金额明细几个功能都正常共存 | accountbook-20260801 |
| 2026-08-02 | 点某天柱状图后，下面不要同时显示"整段时间汇总"的图例(容易和刚点开的单日明细搞混)；没有数据的日期改成点了没反应 | 已完成：①`showTrendPointInfo()`弹出单日/单月明细面板时顺手把下面`trendCatLegend`(整段可见范围的汇总图例)隐藏，`renderTrendChart()`每次重新渲染(换月/切支出收入)时会先重置成显示状态，不会一直卡在隐藏；②画每根柱子的点击热区(`.trend-bar-hit`)时先判断`b[valField]>0`，当天/当月是0(没有这个类型的记录)就直接不生成这个热区，点了没反应，不算"选中"。用Playwright验证了：默认图例可见→点有数据的柱子后图例隐藏+明细面板出现→切换支出/收入类型后图例恢复可见+明细面板收起；没有数据那天的柱子确认拿不到点击热区元素 | accountbook-20260801 |
| 2026-08-02 | 按月对比也要跟按日对比一样的点击/无数据不可选逻辑；支出收入下拉框日别/月别要各自独立，默认支出，切换日别月别就恢复默认 | 前半部分确认无需改动：点击热区/隐藏汇总图例/无数据不可点这些逻辑本来就写在`renderTrendChart()`共用的部分，不区分`currentDim`，月别视图天然就是一样的效果，用Playwright验证了月别下点7月(有工资收入)的柱子同样弹出明细+隐藏图例，无数据的月份同样点不出反应。后半部分是真的bug：`trendChartType`是日别/月别共用同一个变量，切换"按日对比/按月对比"分段时不会重置，导致比如在日别里选了"收入"，切到月别还停留在"收入"而不是回到默认"支出"。修复：`dimSegment`的点击事件里切换`currentDim`的同时把`trendChartType`重置回`'expense'`。用Playwright验证了日别选收入→切月别→变回支出，月别选收入→切回日别→也变回支出 | accountbook-20260801 |
| 2026-08-02 | 用户截图反馈：首页账目列表每天分组的"日期+当天总额"字小色淡不明显，出方案改样式 | 已完成：先出4版方案(现状对比/字号加大加粗变深/加盈亏染色/日期做标签块+衬线大字号)截图给用户选，选中"日期做浅色标签块+总额用衬线大字号"这版才写代码。`.day-header`：日期部分包一层`.day-date-chip`(浅灰底圆角小标签)，总额部分改成`'Noto Serif SC'`衬线字体、16px、加粗，并按当天盈亏套`expense`(印章红)/`income`(青瓷绿)两个已有的颜色class，跟账目卡片里金额的配色逻辑保持一致。`buildDayGroupedHtml()`是首页列表和"本月明细"弹窗共用的同一个函数，改一处两边同时生效。用Playwright截图确认新样式渲染正确、颜色跟当天盈亏对应无误 | accountbook-20260801 |
| 2026-08-02 | 用户截图反馈：柱状图某天有大额支出(比如房租)时，其他日子的小额支出柱子被压成看不清的细线；讨论多个方案(最小高度保底/平方根刻度/顶部截断)后，用户提出让纵轴刻度跟着当前实际看到的柱子自适应，数据大就变大 | 已完成：`renderTrendChart()`大重构——纵轴刻度不再按"整个月"的最大值算，改成只看`.chart-scroll`当前视口范围内实际露出来的那几根柱子的最大值(`visibleMaxVal()`按柱子横坐标跟`scrollLeft`/`clientWidth`的重叠关系筛选)；柱子/网格线/点击热区的绘制逻辑抽成`drawBars(maxVal)`内部函数，方便用不同刻度重复调用而不用整个函数重写一遍。横向滚动时监听`scroll`事件，防抖150ms(避免手指划动过程中柱子跟着不停跳动重画)，滚动停下来后按新视口重新算刻度、只有真的变了才重画。下面的分类汇总图例本来就是整段可见范围(不看滚动位置)的占比，这次顺手把它的计算从"跟着柱子一起画"里拆出来，改成只算一次，不会因为柱子重复重画而跟着变。用Playwright模拟"月初几百元小额支出+月中一笔房租大额支出"的场景，验证了：默认停留位置(今天附近)刻度贴合小额数据、滚动到房租那天刻度自动放大贴合、再滚回月初刻度又自动缩回小尺度；点击柱子看明细、切换支出收入类型/日别月别等此前功能重构后全部照常工作 | accountbook-20260801 |
| 2026-08-02 | 刻度自适应改数值时要有"逐步变大/变小"的动画过渡,不能瞬间跳变 | 已完成：把柱状图/网格线从"每次重新计算刻度就整个innerHTML重建"改成"只建一次DOM元素,后续改刻度只是更新同一批元素的`y`/`height`(柱子)和`y1`/`y2`/文字内容(网格线)属性"，配合新增的CSS `transition:y 0.35s ease, height 0.35s ease`，浏览器会在属性变化时自动补间动画，不用手写JS动画循环。第一次绘制时直接算好正确的初始高度(不是先画成0再变化)，避免打开页面时柱子从0"长"出来的多余动画。用Playwright在滚动触发刻度重算后的多个时间点(180ms/260ms/340ms/420ms/600ms)采样某根柱子的实际渲染高度，确认高度是从147px平滑过渡到6px左右(147→134→64→23→6.5→3.3)而不是瞬间跳变，验证动画确实生效；点击柱子看明细、图例隐藏/显示、日别月别切换等功能重构后全部照常工作 | accountbook-20260801 |
| 2026-08-02 | 纵轴数值刻度要固定显示在最左侧不跟着横向拖拉滚动，只有数值内容随可见区间变化 | 已完成：纵轴数值文字从原来画在会横向滚动的SVG里，挪到旁边新增的`trendAxisFixed`这个不滚动的元素里(`position:absolute`叠在`.chart-scroll`左侧，背景色跟卡片一致，盖住底下滑过去的柱子)。横向网格虚线仍然留在SVG里跟柱子一起滚动(不然刻度线对不上对应的柱子高度)，只是"数值文字"这一层单独拆出来固定住。改刻度时`applyScale()`同步更新网格线的`y1/y2`和固定侧栏文字的`top`/文字内容，两边坐标是同一套SVG像素单位、1:1对应，不需要额外换算。用Playwright验证滚动前后固定侧栏文字的水平位置(`left`)几乎不变(只有因为数字位数不同、右对齐导致的几像素差异)，而文字内容从小数值(0/225/450/675/900)正确变成滚动后视口对应的大数值(0/10.0k/20.0k/30.0k/40.0k)；点击柱子看明细、图例隐藏显示、支出收入类型/日别月别切换等此前所有功能重构后全部照常工作 | accountbook-20260801 |
| 2026-08-02 | 柱子没显示在当前画面内时不要渲染出来，等拖拉滚动到它出现在画面里再按当前刻度把它画出来 | 已完成：`applyScale(maxVal, scrollLeft, clientWidth)`新增视口重叠判断——柱子横坐标跟当前`scrollLeft`/`clientWidth`不重叠(不在可见范围内)就直接把它的`height`设成0(不画出来)，重叠(在可见范围内)才按当前刻度算真实高度；这个判断复用了原本`visibleMaxVal()`里筛选可见柱子用的同一套坐标重叠逻辑。滚动停下来后(防抖150ms)不再只在刻度数值变了才重画，而是每次都重新跑一遍`applyScale`，这样柱子进入/离开可见范围时能及时补画/隐藏，不依赖刻度是否刚好也变化。因为柱子DOM元素本来就是"只建一次、后续改属性"而不是重建，配合已有的CSS `transition:height 0.35s ease`，新进入视口的柱子会自然从0"长出来"，不需要额外写动画。用Playwright验证：滚动到月初时月末大额支出的柱子`height=0`(未渲染)，滚动到月末后该柱子从0过渡到最终高度(采样0→23→66→106→136px)，同时月初的柱子回落到`height=0`；点击柱子看明细等回归用例全部照常通过 | accountbook-20260801 |
| 2026-08-03 | 滚到没有数据的日期/月份区间时，纵轴刻度会掉回兜底值1，显示成很丑的"0/0/1/1"；给了3个方案(沿用上一次刻度/刻度栏留白/加提示文字)，用户选择第一个 | 已完成：`visibleMaxVal()`原来在当前视口一根有数据的柱子都没有时会返回兜底值1；改成这种情况下返回`null`，由调用方(初次渲染和滚动事件里)判断——`null`就沿用`currentMaxVal`已有的数值，不去更新它，只有视口里出现有数据的柱子时才真正更新刻度。这样滚动到两笔数据中间的空白区间时，刻度会保持上一次经过的、有意义的数值(比如0/225/450/675/900)，而不是突兀地变成0/1。用Playwright构造"月初有一笔900日元、月末有一笔4万日元、中间完全没数据"的场景验证：先滚到月初让刻度定住900档，再滚到中间空白区间，刻度数字原样不变，没有出现0/1这种异常值；点击柱子看明细、图例、日别月别切换、柱子视口外不渲染等此前所有回归用例全部照常通过 | accountbook-20260801 |
| 2026-08-03 | 柱子从"视口外不渲染"变成"滚进视口出现"这个过程太慢，但已经在看着的柱子因为刻度变化而升降高度可以慢没关系 | 已完成：给每根柱子加了一个"上一次是否在视口内"的记录(`barWasInView`)，`applyScale()`每次改柱子的`y`/`height`之前先判断这次是"从视口外第一次冒出来"还是"本来就在视口里、只是刻度变了升降高度"——前者用快速过渡(`0.15s`)，后者沿用原来的慢速过渡(`0.35s`)，通过给柱子的`rect.style.transition`按情况动态赋值实现，不用改CSS class本身。用Playwright验证：造一根一直留在视口里的柱子(因为旁边冒出更大数值,刻度要跟着变大)和一根原本在视口外、滚动后进入视口的柱子，前者的渲染高度要到差不多500ms(150ms防抖+350ms过渡)才完全定住，后者大概300ms(150ms防抖+150ms过渡)就已经到达最终高度，明显比前者快；点击柱子看明细、空数据区间刻度沿用、视口外柱子不渲染等回归用例全部照常通过 | accountbook-20260801 |
| 2026-08-03 | 用户截图反馈：滚动横向进度条时，柱子有时会紧贴着左侧固定刻度栏、看起来像被刻度切掉了一截；纵坐标应该是视图的边界，不该跟柱子叠在一起 | 已完成：根源是固定刻度栏`trendAxisFixed`原来是`position:absolute`盖在可滚动内容上面(视觉上"冻结"的效果)，但滚动到某些位置时,某根柱子左边缘刚好滑到这层盖住的34px范围内，就会被半遮住、看着像切了一刀。改成用真正的flex布局分栏——`.chart-scroll-wrap`变成`display:flex`的行容器，`trendAxisFixed`是它左边固定34px宽的一个真实分栏(不再是绝对定位覆盖)，`.chart-scroll`占剩余宽度、独立滚动，这样刻度栏跟可滚动内容是并排关系，内容不可能滚到刻度栏底下被挡住。相应地SVG内部原来给刻度预留的`leftPad`空间也去掉了(现在刻度在外面单独一栏，柱子不用再让出这块空间)。用Playwright截图验证滚动到多个位置(包括之前会出问题的位置)刻度栏跟柱子都是清清爽爽分开的，没有再出现重叠/切割；点击柱子看明细、刻度自适应/沿用、视口外柱子不渲染、快慢过渡动画等回归用例全部照常通过 | accountbook-20260801 |
| 2026-08-03 | 用户截图反馈：首页"本月明细"弹窗里，按分类筛选后显示的分类小标签(比如"食费")上带一个×号，需要去掉 | 已完成：`monthDetailFilterChip`这个筛选标签原来右边有个`monthDetailFilterClear`按钮(点击清除筛选、恢复显示全月)，直接从HTML里删掉这个按钮，标签变成纯展示"图标+分类名"，不再可点击清除；相应地删掉了`.filter-chip button`这条只服务于这个按钮的CSS规则和它的点击事件绑定，`.filter-chip`的左右padding改成对称(不用再给按钮留位置)。清除筛选后想看全月明细，关掉弹窗重新点"查看本月明细"(不带分类筛选)进入即可，跟原来的入口逻辑一致，没有丢失功能。用Playwright点击首页环形图的"食费"图例进入筛选明细，截图确认标签上确实没有×号了 | accountbook-20260801 |
| 2026-08-03 | 记账表单选分类时，自定义子类点一下会直接进入改名编辑、没法把它选中用来记账；希望点击能选中，编辑功能挪到别处 | 已完成：先出3个方案截图给用户选(镜像编辑/删除双角标、长按改名、单一"⋯"菜单角标)，选中方案C。自定义子类的胶囊本体现在点一下就是"选中"，跟普通子类完全一样；右上角原来的✕删除角标换成一个灰色"⋯"菜单角标，点开弹出一个小菜单，"编辑"进改名输入框(原来的`openEditSubInput`逻辑不用改)，"删除"是原来的删除逻辑。全局绑了一个点击穿透监听，点菜单以外的地方自动收起。开发过程中用Playwright测出一个真实问题：子类列表在弹层里滚动位置靠下时，菜单默认往下展开会被弹层底边裁掉一截("删除"选项看不全)——加了个检测，渲染完菜单量一下实际位置，装不下就翻到往上展开(`.flip-up`)，修完之后验证不会再被裁切。验证了点胶囊选中/点⋯开菜单/点编辑改名/点删除/点外部收起菜单全部符合预期 | accountbook-20260801 |
| 2026-08-03 | 选中子类后停留在选择面板，需要手动点‹返回记账页；问有没有方案可以选完自动跳回去 | 已完成：7月31号那次改动特意去掉了"选中细分自动跳回记账表单"，是因为当时自定义子类"点一下=进编辑"和"选中"共用同一个点击，选中就自动跳走会打断正在进行的改名。这次编辑/删除已经挪进"⋯"菜单、点胶囊本体现在只代表选中，冲突已经不存在，可以放心恢复：点任意子类(预设或自定义)胶囊本体，选中态先短暂高亮180ms再自动`closeCatPicker()`跳回记账表单；"⋯"菜单里点编辑/删除不会触发这个跳转，不影响改名/删除操作。用Playwright验证了4种场景：点预设子类自动跳回、点自定义子类自动跳回、菜单里点编辑仍停留在选择面板、菜单里点删除仍停留在选择面板 | accountbook-20260801 |
| 2026-08-03 | 明细列表(首页账目列表/本月明细弹窗)排序要修正：今天及之前(已发生)的按日期倒序置顶，本月里还没发生的收支(比如提前生成的下个月房租/工资)保留显示但优先级要靠后 | 已完成：先出3个方案(只调顺序/加分隔线/分隔线+调淡未发生部分)截图给用户选，选中"只调顺序"，不加任何视觉样式改动。两个列表共用的`buildDayGroupedHtml()`原来排序是纯按日期倒序(`b.date.localeCompare(a.date)`)，导致晚于今天的未发生收支会跟已发生的混在一起，甚至排到今天前面。改成先分两组：日期<=今天的一组按原来的倒序规则排(今天在最上面，越早的越往下)；日期>今天的一组整体排到最下面，组内按日期升序排(越快发生的排越靠前)。用Playwright在首页账目列表和"本月明细"弹窗两处各构造"今天+过去几天+未来几天"的数据验证，日期顺序都符合预期(今天→08-02→08-01→未来日期由近到远) | accountbook-20260801 |
| 2026-08-03 | 首页环状图下面图例的金额数字颜色不分收支，都是一个颜色，截图反馈需要按支出/收入显示对应颜色 | 已完成：`renderHomeDonut()`渲染图例时给容器`#homeLegendList`按当前`homeDonutType`加`expense`/`income`这个class，CSS里`#homeLegendList.expense .legend-row .amt{color:var(--seal)}`/`.income{color:var(--jade)}`跟账目列表金额的配色逻辑保持一致(整个环状图当前只会显示单一收支类型，图例里所有金额统一同一个颜色即可，不用逐行判断)。用Playwright验证了切换支出/收入后图例金额颜色分别是印章红/青瓷绿 | accountbook-20260801 |
| 2026-08-03 | 新建/编辑记账页加"标签"功能：支出下预设一个默认标签"固定支出"，收入暂不加默认标签；标签支持新建编辑，仅自定义标签能编辑删除，系统预设不可操作(参照子类的处理方式)；环状图要支持按标签统计，未打标签的归入"默认"；切换统计维度后图例和点击进明细都要跟着重新算 | 已完成，这次改动量比较大：①新建`tags`表(主键用`id`不用`key`，业务侧稳定标识改叫`code`，避免跟"主键"概念混淆——这是这次专门定的新表命名规范，不动`categories`/`subcategories`等旧表的`key`字段，那是另一件风险大很多的事，不在这次范围内)，`entries`表加`tag_code`列，SQL和历史数据回填(把`RECURRING_RULES`里房租/话费/Prime/追番这几个"预设支出"回填成"固定支出"标签)整理成文件发给用户在Supabase自己跑。②客户端仿照自定义子类的模式实现`expenseTags`/`incomeTags`/`allTagMap`，自定义标签的新增/改名/删除复用"⋯"菜单交互(编辑/删除)，预设标签换成不可点的锁标角标；标签是可选的单选胶囊，再点一下已选中的会取消选中(变回"无标签")，这点跟子类不一样(子类永远有一个默认选中项)。③记账表单在"支付方式"下面加"标签"字段，选中/新建自动跳回原有逻辑不受影响。④首页环状图新增"分类/标签"维度下拉框——先出方案讨论了放哪里(用户否决了放在支出/收入分段上层的方案，指出这样层级不合理，因为标签本身还要分支出/收入两套；最后按用户截图指的donut右侧空白处来做，不占额外一行、层级上明确在支出/收入下面)。标签维度下未打标签的账目归到"默认"这一档，用调淡的灰色跟真正打了标签的部分区分开。⑤点击图例进"本月明细"的筛选逻辑从只支持分类改成同时支持分类和标签两个维度(`monthDetailFilter`从单一的`catKey`变成`{dim,key}`)，标签维度的筛选标签用统一的标签图标(新增`ic-tag`/`ic-lock`两个svg符号)。用Playwright覆盖了：标签选中/取消选中、自定义标签增删改(含新建自动选中的行为)、预设标签不可操作、维度切换后图例和总额跟着变、"默认"档颜色变淡、点标签图例和点"默认"图例都能正确筛出本月明细，以及所有之前的回归用例 | accountbook-20260801 |
| 2026-08-03 | 用户指出categories/subcategories/payment_methods这几张老表也是用key当主键，容易跟"主键"概念混淆；要求全部一起做数据库规范化优化，主键统一改id，业务标识改叫code | 已完成，这次范围比tags那次大得多，同步改了数据库和几乎整个客户端读写分类/细分/支付方式的代码：①SQL方面，一开始漏算了categories/subcategories/entries之间原本就有的外键约束(subcategories.cat_key/entries.cat_key都外键指向categories.key)，直接删主键被这些外键拦住报错；修正为先用`pg_constraint`动态找出所有指向这三张表的外键统统删掉，改完名字后重新建回去，外键改成指向新的`code`列(不是`id`)——跟客户端的引用方式保持一致，`id`只是数据库内部主键，业务上认哪个分类/细分/支付方式还是靠`code`。②客户端把`categories`/`subcategories`/`payment_methods`三张表相关的几十处代码全部同步：`rowsToCatArray`/`allCatMap`/`payMethodMap`/`findSub`按`code`重新键值化，`entryToDbRow`/`dbRowToEntry`的`cat_key`/`sub_key`改成`cat_code`/`sub_code`，分类选择页/细分选择页里所有`data-key`属性和点击处理改成`data-code`，自定义细分的增删改跟着标签那次一样换成按`id`匹配(客户端生成uuid，不用等insert返回回填)，`RECURRING_RULES`/`RECURRING_BIWEEKLY_RULES`里硬编码的`catKey`/`subKey`也都改成`catCode`/`subCode`。是否要把`entries.payment_method`也顺手改名成`payment_method_code`求彻底一致，跟用户确认后选择不改(它本来就不带"key"字样，没有混淆主键的风险)。用Playwright用全新的`code`/`cat_code`/`sub_code`schema把分类选择、细分增删改、记账保存、首页环状图、支付方式选择整条链路重新测了一遍，另外把之前几个关键回归用例(柱状图相关)的mock数据也同步改成新schema重新跑通过。SQL执行阶段用户反馈了两次问题：第一次是"整段一起跑触发事务回滚导致id列没加上"，改成一条一条跑；第二次是前面说的外键依赖报错，出了v2修正版。数据库迁移成功后，用户手机上短暂出现分类/支付方式显示错乱，排查后是本机缓存的旧版分类/支付方式数据还没被新schema的数据覆盖(App自己的后台刷新+CDN发布都有延迟)，让用户彻底关闭重开App后自行恢复正常，不是代码本身的bug | accountbook-20260801 |
| 2026-08-03 | 环状图标签维度里，没打标签的账目归到的那一档，中文/日文分别叫"默认"/"デフォルト"，用户反馈这个措辞不好，讨论后改成"其他"/"その他" | 已完成：`tagDefault`这个i18n文案从"默认"/"デフォルト"改成"其他"/"その他"。中间有一轮讨论——用户一开始想直接叫"非固定支出"，因为现在确实只有"固定支出"一个标签时这个名字更贴切；但指出如果以后再加别的标签(比如"旅行")，"没打标签"这一档就不再等于"非固定支出"了，"其他"这种通用措辞在标签数量增加后也不会显得不准确，用户认可后采用。用Playwright验证了标签维度图例正确显示"其他"/"その他"，颜色调淡、点击进明细筛选等其它行为不受影响 | accountbook-20260801 |
| 2026-08-03 | 首页环状图右上角的维度切换下拉框，切到"标签"档位后按钮上显示的文字反馈不对，几轮反馈后明确为：应该显示tags表里实际的标签名称，而不是"标签"/タグ这种通用档位名 | 已完成：新增`updateDonutDimTagOptionLabel()`，在`renderHomeDonut()`一开始调用，把下拉框里"标签"这个`<option>`的文字动态改成当前收支类型下tags表里实际的标签名(比如"固定支出"/"固定費")；只有一个标签时才这样显示，标签数量为0或大于1时退回通用的"标签"/"タグ"文案（避免以后标签变多时这个选项文字失去意义）。这个措辞问题反馈时不够清晰，来回澄清了好几轮才用选择题问卷方式确认下来。用Playwright验证：只有1个标签("固定支出")时选项显示"固定費"，切到收入类型(0个标签)时正确回退显示通用的"タグ" | accountbook-20260801 |
| 2026-08-03 | 反馈新建/编辑页面预设标签"固定支出"选不中(锁死)；"固定支出"日语翻译应为"固定費用"(不是"固定費") | 已完成两处：①用Playwright复现了根因——本机`ledger_catalog_cache`缓存的是`categories/subcategories/payment_methods`大改造(id/code规范化)**之前**的旧结构(字段叫`key`不叫`code`)，`loadCatalogData()`原来的逻辑是"有本机缓存就先直接拿来渲染,云端刷新放后台悄悄做"，如果那次后台刷新因为网络问题没成功，旧结构缓存(标签对象没有`code`字段)就会一直被使用，导致点标签胶囊时`tags.find(tg=>tg.code===code)`永远匹配不上、状态更新不了，看起来就像''点了没反应/被锁死''。修法：给缓存数据加版本号`CATALOG_CACHE_VERSION`(改动了字段结构就手动+1)，`loadCatalogCacheFromLocal()`发现缓存版本对不上就直接当作没有缓存处理,强制走一次云端拉取,不会再拿旧结构数据硬渲染。用Playwright在localStorage里预先塞一份旧结构(`key`字段)缓存模拟这个场景，验证修复后能正确重新拉到新结构数据、标签胶囊可以正常点选。②发SQL(`update tags set ja='固定費用' where code='fixed_expense'`)让用户在Supabase里把预设标签的日语字段改掉，这是数据内容问题不是代码问题 | accountbook-20260801 |
| 2026-08-03 | 环状图维度下拉框继续调整：选项内容要读tags表的全部真实数据(不能是写死的通用"标签"文字)，"分类"要作为固定的默认选项、显示内容还是原来那个不分标签的环状图 | 已完成，这次是把下拉框从"分类/标签两选一模式"重新设计成"分类(默认,不筛选) + 当前收支类型下每一条标签各自一个选项"：①新增`renderDonutDimOptions()`,每次渲染环状图都用`expenseTags`/`incomeTags`里的实际数据重建`<option>`列表(不再有任何写死的"标签"文案)，固定把"分类"放在第一位当默认值。②选中某个具体标签后，环状图不再是"每个标签一个扇形"的整体对比图，而是"只看打了这个标签的账目,还是按分类细分"(比如选"固定支出"就能看到固定支出里房租/话费/餐饮等各占多少)，这样"分类"选项和标签选项用的是同一套按分类分组的图表逻辑，只是数据源有没有被标签筛过；同时废弃了之前"未打标签账目统一归到'其他'档"这个概念(不再需要,因为现在没有"把所有标签混在一张图里对比"这个视图了)，相关的`TAG_DEFAULT_COLOR`/`tagDefault`/`dimTag`死代码和i18n一并删掉。③"本月明细"点击进入的筛选逻辑从"分类"或"标签"二选一，改成`{catCode?, tagCode?}`两个字段可以同时生效(点图例=分类+当前标签筛选一起带进去，筛选标签胶囊同时显示标签名和分类名)。用Playwright验证：下拉框选项等于tags表实际内容、选中标签后图例只剩该标签下的分类占比、点图例进明细同时按分类和标签筛出正确的1条记录且筛选胶囊文字正确、切回"分类"能恢复原来不筛选的整体视图 | accountbook-20260801 |
| 2026-08-03 | 截图反馈上一版做错了：选中标签后不应该是"按分类细分该标签下的账目"，而应该是"这个标签 vs 其他"两档，看这个标签占当月总支出/收入的比例 | 已完成，把选中标签后的统计维度改掉：不再按分类分组，而是把当月同类型的全部账目分成两档——打了这个标签的("固定费用"，取标签本身的名字)和没打的("其他")，两档金额相加等于当月总支出/收入(跟"分类"模式的总额一致，只是切法不同)。相应地：颜色从"每个分类专属色"改成"标签用主色/其他用调淡的灰色"两色对比；"本月明细"筛选逻辑加了`tagExclude`标记区分点的是"这个标签"还是"其他"(其他=标签不等于当前选中值，覆盖未打标签+打了别的标签两种情况)，筛选胶囊文字对应显示标签名或"其他"；重新加回`tagOtherBucket`("其他"/"その他")这个i18n文案(上一版当作死代码删掉了，这次证明其实还需要)。测试过程中发现一个坑：Playwright测试用的mock分类如果用了`housing`这种跟`RECURRING_RULES`里自动生成规则(房租/话费等)重名的code，会被App自己每月自动生成的固定支出条目污染测试数据、算出来的数字对不上，换成测试专用的分类code后确认数据链路正确：选中标签后图例正确显示"标签名 vs 其他"两档金额、总额=两档相加=当月总额，点两个图例分别进明细都能筛出正确的1条记录且筛选胶囊文字正确 | accountbook-20260801 |
| 2026-08-03 | 截图反馈日语状态下"选择分类"弹窗的标题和"分类"字段标签显示的还是中文；顺带指出环状图下拉框"分类"选项的日语应该统一用"カテゴリ"(片假名),不要用"分類"(汉字) | 已完成，这是个之前就存在、这次才被日语状态测试暴露出来的bug：分类选择弹窗(`catPickerSheet`)里的标题`catPickerTitle`("选择分类")和分类区块标签`catFieldLabel2`("分类")这两个元素的文字虽然在HTML里有id，但`applyStaticI18n()`一直没把它们纳入语言切换的更新范围，导致不管App语言设成什么，这两处永远显示写死在HTML里的中文占位文字；同一个弹窗里"细分"那行(`subFieldLabel`)倒是正常跟着语言走，这次一并补上遗漏的两行。另外把上一版新加的`dimCategory`日语文案从"分類"统一改成"カテゴリ"，跟App其它地方(`catFieldLabel`等)保持用词一致。用Playwright验证：环状图下拉框"分类"选项、分类选择弹窗标题、弹窗内"分类"标签，三处日语状态下都正确显示"カテゴリ"/"カテゴリを選択" | accountbook-20260801 |
| 2026-08-03 | 指出"カテゴリを選択"作为弹窗标题不地道——标题不该用句子/带助词动词的短句，中日文都一样，应该是纯名词 | 已完成：分类选择弹窗标题从"选择分类"/"カテゴリを選択"改成纯名词"分类"/"カテゴリ"，跟`catFieldLabel`用词完全一致；顺带发现标题下面紧跟着还有一行同样内容的字段标签(`catFieldLabel2`，也是"分类"/"カテゴリ")，两个纯名词标题现在会变成完全重复的两行文字，所以直接删掉了这个多余的字段标签(标题本身已经说明这个区块是分类选择，网格上方不再需要额外重复一遍)。用Playwright截图确认弹窗顶部现在是"‹ カテゴリ"标题+紧接着分类网格，不再有重复文字 | accountbook-20260801 |
| 2026-08-03 | 反馈完全关闭App重新打开后没有弹出更新提示，检查更新的时间点需要调整 | 已完成：之前`checkForAppUpdate()`只在"App开着不关"的场景下触发——定时器每3分钟一次、切回前台(`visibilitychange`)、以及从bfcache恢复(`pageshow`的`persisted`)，唯独冷启动(完全关闭再重新打开，走的是全新的页面加载而不是这几种场景)没有调用它；如果这次冷启动碰巧加载到的是浏览器HTTP缓存里的旧版本，就要等最多3分钟才会看到更新提示。这次在`init()`最后加了一次`checkForAppUpdate()`调用，跟`fetchRate`/`fetchJpHolidays`一样是fire-and-forget不阻塞首屏渲染。用Playwright拦截了App自己检查更新时发的请求、伪造一份`APP_BUILD`更高的假页面模拟"服务器已有新版但这次冷启动加载到旧版"的场景，验证冷启动加载完成后(不等定时器、不触发切前台)就能立刻看到更新提示 | accountbook-20260801 |
| 2026-08-03 | 反馈"固定费用"环状图选中标签这一档的颜色太难看 | 已完成：查出根因是打了标签这一档之前直接用调色板第一个颜色`palette[0]`(`#d1665a`)，这个色值跟`--seal`(支出红,FAB按钮/支出分段按钮/支出金额全在用这个颜色)完全一样，混在一屏里显得又脏又不好看，跟本类型统计不清。改用App本来就有的金色强调色`#e3b23c`(装饰胶带用的那个)，不撞支出红也不撞收入绿。用Playwright重跑了之前的标签环状图测试确认数字逻辑没受影响，另外截图确认圆环现在显示为清晰的金色，跟上方红色的"支出"分段按钮明显区分开 | accountbook-20260801 |
| 2026-08-03 | 反馈汇率取不到更新数据；截图显示为替页顶部大数字的"更新日時"卡在2026-07-31，但下面近30天走势图的折线已经画到2026-08-01了 | 已完成，根因：`fetchRate()`拉汇率有4个源按优先级依次试(jsdelivr/cf-pages/frankfurter两个)，之前是"谁第一个成功就直接用谁"，完全不比较各个源返回数据里的日期新旧——jsdelivr的`@latest`是跟着npm发包节奏更新的，经常会比cf-pages/frankfurter慢个几天；一旦它先成功返回(哪怕数据是旧的)，后面几个可能已经更新的源就再也不会被用上了。而趋势图用的是另一个函数(`fetchOneDayRateFromCdn`，按具体日期去请求历史快照)，不吃这个"先到先得"的限制，所以能先一步拿到8月1日的数据，导致顶部大数字和下面走势图对不上。修法：4个源全部试一遍，比较各自返回的`date`字段，取最新的那个，不再是谁先响应就用谁。用Playwright mock了"jsdelivr先响应但日期是07-31，cf-pages响应更晚但日期是08-01"这个场景，验证修复后最终采用的确实是cf-pages更新的那份数据 | accountbook-20260801 |
| 2026-08-03 | 截图反馈汇率趋势图两个问题：①横坐标日期文字重叠糊在一起(比如"08-01"和"08-04"叠在一起)；②进入为替页默认停在最早的数据，看不到最新走势，得手动划到最右边 | 已完成两处：①`renderRateTrendChart()`原来的横坐标取点逻辑是"每隔N个点标一个,最后一个点再强制标一次"，如果这个强制加的最后一个点跟前面按规律标的最后一个常规点靠得太近(比如就差1个点),两个日期文字就会紧贴在一起糊成一团——这正是截图里"08-01"和"08-04"重叠的原因。改成先按老逻辑选出候选的标注点，再过滤一遍：如果某个点跟下一个候选点的横向距离小于最小间距(22px)，就跳过它、把位置让给后面那个，这样永远优先保留最新的日期不被挤掉，也不会有两个文字叠在一起。②`chart-scroll`滚动容器渲染完之后没有主动定位，默认停在最左边(最早的数据)；补上跟集計页柱状图同样的"渲染完自动滚到最右边(最新数据)"逻辑。用Playwright灌了30天模拟汇率数据（最后几天故意做出陡峭跳变复现截图场景）验证：横坐标相邻标签间距全部≥22px不再重叠，且默认滚动位置就停在最右边(scrollLeft=scrollWidth-clientWidth) | accountbook-20260801 |
| 2026-08-03 | "过去8年"汇率趋势只取到3个点的数据，应该有8个 | 已完成，根因分两部分：①`jsdelivr`/`cf-pages`这两个历史汇率源都是同一个开源项目(fawazahmed0/currency-api)的镜像，这个项目的数据只从项目开始收录那天算起，更早的年份两个源都查不到；②最新一年(今年)如果查询的目标日期(比如"今年12月31日"或"今天")还没被数据源发布，也会查不到。两部分加起来导致8年里只有中间那几年能查到数据。修法：①`fetchOneDayRateFromCdn`加了第三个兜底源`frankfurter.dev`(背后是欧洲央行的历史牌价，覆盖年份长得多)，专门顶住偏早的年份；②新增`fetchNearDateRateFromCdn`，目标日期查不到就往前逐天找最近一个有数据的日子(避免年底12-31刚好是节假日没发布这种小概率丢点)；③如果做完以上兜底，最新一年/最新一个月还是查不到(大概率是这个年份/月份确实还没被数据源收录)，就直接用当前已经拿到的最新汇率顶上，不会让"今年"这个点直接消失。用Playwright模拟了"2019-2022查不到、2023-2025正常、2026(今年)哪个源都查不到"的场景，验证修复后8年全部都能出现在图上 | accountbook-20260801 |
| 2026-08-04 | 编辑和复制记账记录的弹窗里，支出/收入这个切换需要置灰锁死不能改 | 已完成：给记账表单的支出/收入分段按钮外层套了个`id="typeSegment"`容器，新增`setTypeSegmentLocked(locked)`统一控制——新建记录(`openSheet()`)时解锁，编辑(`openEntrySheetForEdit`)和复制(`openEntrySheetForCopy`)已有记录时锁死：按钮本身加原生`disabled`属性(彻底挡掉点击，不只是视觉上的)，配合`opacity:0.5`让它看起来是灰的。用Playwright验证：新建记录时支出/收入能正常点切换；编辑/复制已有记录时按钮`disabled`为true、点了也确实切不动；关掉编辑/复制弹窗后再新建一笔，状态正确恢复成可点 | accountbook-20260804 |
| 2026-08-04 | 分享了另一个会话里做的"四季主题皮肤"设计稿(8款季节配色，每款有独立的卡片形状/图标画法/背景插画)，要求先加一个主题(着せ替え)切换页面，做出真实功能，并确认是否需要开新表存主题信息 | 已完成v1(先做换色，卡片形状/生成式背景插画留到下一阶段)：①发现App本来就用CSS变量管理整套配色(`--paper`/`--ink`/`--seal`/`--jade`/`--accent`等定义在`:root`)，这跟设计稿里每款主题的`vars`token刚好一一对应，于是新增8个`:root[data-skin="x"]`覆盖块，直接照抄设计稿里定好的色值，`document.documentElement`打上`data-skin`属性即可整体换肤，不用碰任何组件代码。②设置页新增"主题"行(跟"选择分类"一样的picker-row+独立选择页模式)，`themeSheet`里3×3网格展示9个选项(现状+8款四季配色)，每个用四色圆环(纸/支出色/收入色/点缀色)做预览，点击即时生效。③持久化复用现有`settings`机制(不需要新建表)：`themeSkin`作为`settings`的新字段跟着`lang`/`currency`一起走本机+云端同步；云端这边`user_settings`表加一列`theme_skin`就够(SQL发给用户在Supabase自己跑)；`pushSettingsUpsert()`做了防御——新列还没建好时upsert失败会自动退回不带`theme_skin`的旧payload重试，不会拖累语言/货币的同步。日文主题名重新斟酌成更自然的说法（比如"枫拾"译作"紅葉狩り"、"极光"用片假名"オーロラ")。用Playwright验证：默认显示"現状"、切换到"夏・花火"(深色系)后全局`--seal`/`--paper`等变量正确更新、设置页主题行文字同步更新、刷新页面后主题保持(本机持久化生效)。卡片形状/图标容器画法/生成式SVG背景这些设计稿里更丰富的部分本次没做，后续需要再确认是否要投入 | accountbook-20260804 |
| 2026-08-04 | 反馈栏目名称应该叫"着せ替え"（发了LINE的着せ替え设置页截图作参照）；先把8款四季主题全部撤下，只留"基本"(原来叫"デフォルト"，改名"基本")这一个；选择页不需要显示当前选中状态 | 已完成：①设置页的字段名和主题选择页标题都改成"着せ替え"（跟参照截图的LINE设置项一致）。②`THEME_SKINS`只保留`current`这一项，中日文都叫"基本"；对应的8个`:root[data-skin="x"]`四季配色CSS覆盖块也一并删掉了(以后要重新上直接把数据加回`THEME_SKINS`就行，DEVLOG和之前的设计稿里色值都还留着)。③选择页的卡片去掉了"selected"高亮样式(边框/背景变色那套)，改成跟参照截图里LINE着せ替え商店一样的中性展示，不标哪个是当前生效的。用Playwright验证：字段名和弹窗标题都显示"着せ替え"，网格里只有1张"基本"卡片且没有任何选中态样式 | accountbook-20260804 |
| 2026-08-04 | "着せ替え"改成独立按钮放在设置图标旁边(图标用衣服)，点进去显示每个主题的首页缩略图+名字，下方一个勾选圆圈+统一的保存按钮，一行3个 | 已完成：①三个页面(家计簿/集計/為替)头部的设置齿轮图标旁都加了一个新的衣服图标按钮(新增`ic-shirt`)，直接点击打开主题选择页，不用先进设置——原来塞在设置弹窗里的那一行整个去掉了。②主题选择页改成3列网格，每张卡片是"缩略图+名字+选中圆圈"：缩略图不是真的渲染一份首页(维护成本太高)，用该主题的4个关键色拼一个"支出色横条+三色环状图"的迷你版，看一眼就能认出配色调性。③交互改成"先选后存"：点卡片只是把圆圈打勾(暂存选择，不会立刻换肤)，底部统一一个"保存"按钮，点了才真正应用配色+写盘，方便挑选时把几个主题都点一遍比较，不会每点一下全屏跳一次色。用Playwright验证：独立按钮不经过设置弹窗就能直接打开主题页、卡片带缩略图、初始默认打勾"基本"、点卡片后保存前配色不变、点保存后才真正生效并关闭弹窗 | accountbook-20260804 |
| 2026-08-04 | 反馈缩略图要显示真实的首页截图，不要用预设的示意图 | 已完成：把上一版"手画的支出色横条+三色环状图"简化示意图换成真实DOM克隆——直接把首页的`.header`/`.ledger-card`/`.chart-card`这三块真实节点原样搬进缩略图容器（去掉id避免跟原节点重复），外层套一层设了该主题完整颜色变量的容器，因为App整套配色本来就是走CSS变量，同一份真实标记换个变量取值就能正确重新上色，再用`transform:scale()`缩小裁切成缩略图大小——这样看到的就是这个主题下首页实际会长什么样（标题、真实的本月收支金额、环状图卡片开头），不是重新画的示意图。过程中发现一个真实bug：缩略图外层原来用的是`<button>`，但克隆进来的`.chart-card`本来就带按钮和下拉框，`<button>`套`<button>`/`<select>`是不合法的HTML嵌套，浏览器解析时会把结构拆散导致后面的名字和选中圆圈跑到卡片外面；改成`<div role="button" tabindex="0">`+手动补键盘事件解决。缩略图内容本身设置了`pointer-events:none`，不会不小心点到克隆进来的真实按钮。用Playwright验证：缩略图区域正确显示真实的"家計簿"标题和当月实际收支金额（截图可见和背后首页数据一致），且8项功能测试（打开/选中/保存/关闭）全部照常通过 | accountbook-20260804 |
| 2026-08-04 | 要求缩略图取一张快照图片放上去 | 已完成，把上一版"运行时克隆真实DOM+套颜色变量+缩放裁切"的做法换成更简单直接的方式：用Playwright对着首页(标题+余额卡+环状图卡这一段，配了几笔像真实使用场景的示例数据：房租、话费、餐饮、工资)截了一张图，转成base64图片直接存进`THEME_SKINS`的`thumb`字段，缩略图就是普普通通一个`<img>`标签。这样不用再操心克隆节点的非法HTML嵌套、缩放计算这些运行时复杂逻辑，以后新增主题时截一张对应配色下的首页图存进去就行。顺带把外层卡片从`<div role="button">`改回原生`<button>`(不再需要担心里面嵌套着真实的按钮/下拉框，用回语义正确的按钮更省心)。用Playwright验证：缩略图显示的是截图里的示例数据(¥204,462)而不是运行时当前的真实数据，8项原有功能测试(打开/选中/保存/关闭)照常全部通过 | accountbook-20260804 |
| 2026-08-04 | 拉取最新版本，把"红黄配色"(夏 · 花火)这一款主题加回到程序的着せ替え页面，选中后能真的应用到App | 已完成：①之前d279c18把8款四季配色连同`:root[data-skin]`覆盖块一起撤下只留"基本"，这次按用户要求只把"夏・花火"这一款重新加回`THEME_SKINS`，色值token沿用297cf90最初定的那套(深藏青底+橙红/金黄强调色)，其余7款仍按原计划留到下一阶段。②缩略图按当前"静态截图"惯例重新生成：起本地server用Playwright加载首页、localStorage里预置几笔示例数据(房租/餐饮/游戏/工资)+`themeSkin:'summerB'`，截了一张跟"基本"缩略图同尺寸(716×1364)的深色主题首页图存进`thumb`字段。③端到端验证：点独立的"着せ替え"衣服图标按钮(不是设置弹窗里，这个入口在f17ca52就已经移出去了)→选择页正确显示"基本"/"夏・花火"两张缩略图卡→点"夏・花火"卡片(暂存)→点"保存"→`document.documentElement`的`data-skin`属性变成`summerB`、全局配色实时切换、`localStorage.ledger_settings`正确写入`{themeSkin:'summerB'}`、出现"テーマを保存しました"提示。中途发现两个坑：一是设置sheet里已经没有主题入口了(那行HTML在f17ca52就被拿掉，只是JS里没删干净的注释还写着旧结构，一并顺手更新成准确描述)，得改成点头部的衣服图标按钮；二是选完卡片后配色不会立刻变，还要点独立的"保存"按钮才真正生效(f17ca52引入的"先选后存"交互)，两处都是先按旧假设测试失败后才定位到的 | claude/four-seasons-theme-design-eptb2c |
| 2026-08-04 | 反馈"夏 · 花火"目前只是换了颜色，设计稿里的烟花动画和其它效果都没应用上，要求补上 | 已完成：之前那次是v1"只做换色"范围内的实现，这次把设计稿里summerB专属的动态效果接进真实App，只在`data-skin="summerB"`且当前停在记账页(`#pageLedger`)时生效：①卡片/图标按钮/FAB加发光描边，标题从纸卡纸边描边效果换成夜里霓虹发光——CSS规则原样照抄设计稿里`.phone[data-mood="summerB"]`那一套，选择器换成App真实类名(`.ledger-card`/`.chart-card`/`.entry`/`.icon-btn`/`.fab`/`.papercut`)。②设置齿轮图标(三个页面头部各一份)和底部记账/集計/為替三个tab图标，换成之前照用户给的参考图描摹(potrace)出的矢量版本+径向渐变着色，新增`applyThemeIcons()`动态改`<use>`的`href`，跟着`applyThemeSkin()`一起触发。③首页背景插画(星空静态图+canvas烟花粒子动画)搬进`#pageLedger`第一个子元素`#fwBgLayer`：星空是90颗随机大小/亮度的星星+月亮+景深光斑的静态SVG；烟花是真实物理模拟(不是预制形状做CSS动画)——火箭升空、5种真实不同的爆炸类型(菊/牡丹/柳/冠裂/点滅)、邻近色配色、拖尾渐变、同屏最多3发，这些都是设计稿里跟用户来回打磨了十几轮才定下来的参数，原样照搬过来。这块动态效果只在停在记账页时挂载，新增`updateFireworksBg()`跟着`applyThemeSkin()`和`switchToPage()`一起调用——切到集計/為替或换回别的主题，会调用`stopFireworksCanvas()`取消动画帧+断开`ResizeObserver`并清空容器，不会留着在看不见的地方空转耗电。用Playwright验证：选中"夏・花火"后设置图标/tab图标立刻变成矢量渐变版、`#fwBgLayer`里出现`<canvas>`且能截图看到真实烟花轨迹、切到集計页后canvas数量变回0、切回记账页后重新挂载，全程无JS报错 | claude/four-seasons-theme-design-eptb2c |
| 2026-08-04 | 反馈"夏 · 花火"的背景插画需要在记账/集計/為替三个页面都生效，不能只有首页有 | 已完成：把只挂在`#pageLedger`的背景层改成三个页面(`#pageLedger`/`#pageStats`/`#pageRate`)各放一份`.fw-bg-layer`空容器，CSS的定位规则从`#pageLedger`专属选择器改成通用的`.page`；JS的`updateFireworksBg()`重写成"跟着当前.page.active走"：用`document.querySelector('.page.active .fw-bg-layer')`找到该显示哪个容器，如果发现animation已经挂在别的(不再是active的)容器上，先在那边`stopFireworksCanvas()`清理掉再挂到新容器，保证任何时刻最多只有一份canvas在跑，不会切页面时新旧两份叠着算。用Playwright验证：记账→集計→為替→记账来回切换，每一步`.fw-bg-layer canvas`在整个DOM里的总数始终是1(不多不少)，且截图确认集計/為替页确实能看到星空+发光卡片背景，全程无报错 | claude/four-seasons-theme-design-eptb2c |
| 2026-08-04 | 反馈"夏 · 花火"分类icon的圆圈没有加上发光特效 | 已完成：之前只给记账明细列表的图标圆圈(`.entry .icon`)加了发光，漏了另外两处同样是"分类icon圆圈"的地方——新建/编辑记账表单里"分类"这一行的预览圆圈(`#catPickerIcon`)和点开分类选择页后的图标网格(`.cat-btn .ic-wrap`)。这两处的背景色/文字色都是JS直接内联设置(`style.background`/`style.color`)而不是走CSS变量，所以之前的写法没法用`var()`拿到具体颜色；这次改成额外内联设一个`--ic-color`自定义属性(不影响原有的background/color)，三处发光CSS统一用`color-mix(in srgb, var(--ic-color, var(--seal)) ...)`取实际的分类色做发光颜色，同时把发光强度也从`8px -2px 55%/35%`调到跟设计稿原版一致的`10px -1px 60%/40%`(之前偏弱)。用Playwright灌入带真实分类色的`ledger_catalog_cache`验证：明细列表图标圆圈、记账表单分类预览圆圈、分类选择网格三处的`box-shadow`计算值都正确取到对应分类的颜色(比如"住居"色`#4FD1A5`对应的发光就是青绿色)，截图确认视觉上圆圈周围能看到清晰的同色发光环 | claude/four-seasons-theme-design-eptb2c |
| 2026-08-04 | 保存主题时弹出的提示语用词要跟标题一致，不要用"テーマ" | 已完成：日语的保存提示`themeSavedToast`从"テーマを保存しました"改成"着せ替えを保存しました"，跟弹窗标题`themeSheetTitle`统一用"着せ替え"这个词；中文这边标题和提示本来就都是"主题"，不用改。用Playwright验证保存后弹出的提示文字正确显示"着せ替えを保存しました" | accountbook-20260804 |
| 2026-08-04 | 反馈汇率还是不对：下面趋势图已经取到8月5日的数据，上面顶部大数字不管怎么刷新都停在8月4日，要求改用同一个源、两处要同步 | 已完成：根因是顶部"当前汇率"和下面的趋势图走的是两条完全独立的抓取链路——顶部只查`RATE_SOURCES`里4个"最新"接口，下面走的是按天历史快照+frankfurter兜底+就近取值(之前几次改进都是加固这条链路，更容易拿到最新一天)，两边各查各的，下面明显更容易拿到新数据，就会出现顶部卡在旧日期上。修法：`fetchRate()`里`fetchRateHistory()`跑完之后，多一步"用趋势图实际算出来的最后一天数据校正顶部"——只要`rateHistory`最后一项的日期不比目前顶部的日期旧，就直接拿这一条覆盖`rateData`（连带更新本机缓存），保证顶部数字和趋势图最新一个点永远同源、不会出现两边打架。用Playwright完整mock了"4个最新源全部只到08-04，按天历史快照能查到08-05"这个场景验证：顶部"更新日時"正确显示2026-08-05，不再卡在08-04 | accountbook-20260804 |
| 2026-08-05 | 截图反馈：支出环状图的"その他"档明细里混进了一笔收入(給与)，要求支出环状图和明细只统计支出、收入只统计收入 | 已完成：根因是`renderMonthDetailList()`原来只按月份筛，再叠加可选的`catCode`/`tagCode`(`tagExclude`)筛选，从没按`entry.type`筛过——"その他"这类排除式筛选(`tagCode!==固定支出`)收入记录天然满足这个条件(收入压根不会有支出专属的标签)，就被漏筛进来了。修法：`openMonthDetail()`的3个调用点(环状图整卡点击、按分类点图例、按标签点图例)统一带上`type: homeDonutType`(即环状图当前显示的支出/收入)，`renderMonthDetailList()`里把这个`type`筛选放在最前面、月份筛选之后、分类/标签筛选之前，跟`catCode`/`tagCode`筛选一样一起过滤。用Playwright构造"一笔打了固定支出标签的支出+一笔没打标签的支出+一笔工资收入"同月混在一起的场景验证：切到支出环状图按标签维度点"その他"图例，明细里只有未打标签的那笔支出、不含工资；整卡点击支出环状图，明细两笔都是支出、不含工资；切到收入环状图整卡点击，明细只有工资一笔、不含两笔支出；另外重跑了之前的标签维度回归测试(`test_tag_vs_other_donut.js`)确认没有引入回归 | accountbook-20260804 |
| 2026-08-05 | 截图反馈两处："夏 · 花火"背景顶部状态栏和两侧留白没填充上；為替页"レートを更新"按钮看不清颜色 | 已完成两处：①背景没铺满是因为`.fw-bg-layer`用的是`position:absolute`，而它挂在`.page`里，`.page`又是`.app`(`max-width:480px`居中+安全区内边距)的子元素，absolute只能填满`.app`那个居中窄栏，顶部状态栏那一截和两侧留白天然填不到。改成`position:fixed`直接钉在视口上，不受`.app`宽度/内边距影响，才能真正铺满整个屏幕（宽屏下两侧、以及顶部安全区都能看到星空/烟花了）。②`为替`页"レートを更新"按钮看不清，根因是`.save-btn`全局默认`background:var(--ink);color:#FFFDF8`——这在其它主题成立是因为`--ink`一直是深色，但summerB的`--ink`故意反过来是浅米色(`#F0E9D8`，用作深色底上的浅色字用)，直接拿去当按钮底色就变成浅底浅字糊成一片。加了`[data-skin="summerB"] .save-btn`覆盖，改用`--seal`强调色打底+白字+发光描边，跟FAB/图标按钮统一。用Playwright在比`.app`最大宽度更宽的视口(600px)下截图验证背景确实铺满两侧和顶部；為替页截图确认刷新按钮变成清晰可辨的珊瑚红底白字 | claude/four-seasons-theme-design-eptb2c |
| 2026-08-05 | 要求新建/编辑/复制记账表单再加一个"付与ポイント数"(获得积分)字段，数据库也加对应字段 | 已完成：这是每笔账目自身的一个可选属性，跟`note`/`tag_code`同级，不需要新建表，直接在`entries`表加一列`points`。表单里紧跟在标签(`tagGrid`)后面、备注/日期那一行之前新加一个字段：`pointsFieldLabel`(zh"获得积分"/ja"付与ポイント数")+一个`inputmode="numeric"`的文本输入`pointsInput`，留空或填0都当作没填(存null，不占地方)。三处表单入口(`resetEntryFormFields`新建时清空、`applyEntryToForm`编辑/复制时回填`entry.points`)保持跟`note`一致的写法；保存时(`saveBtn`点击)解析`pointsInput`存到`savedEntry.points`。云同步这边`entryToDbRow`加`points: entry.points||null`、`dbRowToEntry`加`points: row.points||null`；因为`points`这一列要等用户手动跑迁移SQL才会在数据库里出现，`pushEntriesBulkUpsert`比照之前`theme_skin`的做法加了退避：先带`points`字段整批upsert，失败就把每条记录的`points`字段删掉再重试一次，避免这一个还没建好列的字段拖累账目其它字段的同步。用Playwright验证：表单默认语言(日语)下字段标签正确显示"付与ポイント数"；填25分保存后本地`ledger_entries`里对应记录`points`正确存成25；重新打开编辑该记录`pointsInput`正确回填25；复制该记录`pointsInput`同样带过去25；另外重跑了标签维度筛选、其他/收支类型筛选、着せ替え独立入口三组已有回归测试全部通过，没有引入回归。迁移SQL已给到用户：`alter table entries add column if not exists points integer;` | accountbook-20260804 |
| 2026-08-05 | 首页/统计页残高卡加积分徽标；统计页收支柱状图加积分柱子+图例；后来反馈积分只应该跟收入挂钩，支出档不该出现积分 | 已完成：①`本月结余`/`累计残高`两张卡在残高数字右侧加翡翠绿描边胶囊(对勾图标+`+N`)，独立汇总当前区间积分、不参与`income-expense`的残高计算。②统计页收支趋势图每根柱子旁加一根积分柱(独立算刻度，不跟金额共用纵轴)，图例和点击明细里都带积分小计。③随后发现bug：积分本该只跟收入挂钩，但当时不分类型统计，导致同一天有收入又有支出时，支出档的柱状图/图例/明细里也会混进那笔收入带来的积分。修复：记账表单切到"支出"时隐藏`获得积分`输入框并清空已填值，保存时强制只有`type==='income'`才写`points`；`getDailyBucketsForMonth`/`getMonthlyBucketsForYear`分桶只累加收入记录的积分；趋势图的积分柱子/图例/点击明细统一加`isIncome`判断，切到"支出"档完全不显示积分相关内容。用Playwright验证：同一天分别造一笔支出和一笔带积分的收入，"支出"档柱状图/图例/明细都不出现积分，切到"收入"档才正确显示；记账表单切支出/收入时`获得积分`字段正确隐藏/显示 | accountbook-20260804 |
| 2026-08-05 | 讨论了"集计"页加支付方式环状图+积分单独走势图的设计方案(纵向追加 vs 顶部収支/ポイント分段导航)，用户倾向两个标签页的方案，设计稿已发待确认；同时问能否在Windows上把App封装成Windows/iOS应用，选定"测试模式+免费Apple ID+Sideloadly/AltStore"路线 | 集计页两个新图表(支付方式环状图、独立积分走势图)还没写代码，只出了设计稿等用户拍板。iOS打包这块已落地基础设施：新增`package.json`(`@capacitor/core`+`@capacitor/ios`+`@capacitor/cli`，`build`脚本把根目录`index.html`原样拷进`www/`不做额外构建)、`capacitor.config.json`(`appId: com.cidstevez01.jiazhangbu`，`webDir: www`)、`.gitignore`排除`node_modules/`/`www/`/`ios/`(原生工程不提交，每次CI全新生成)、`.github/workflows/ios-test-build.yml`——手动触发(`workflow_dispatch`，不跟每次push跑，iOS编译在macOS runner上慢且按10倍分钟数扣费)，跑在`macos-14`上：`npm install`→`npm run build`→`npx cap add ios`→`npx cap sync ios`→`pod install`→`xcodebuild`用`CODE_SIGNING_ALLOWED=NO`编译出未签名的`.app`→手动打包成`Payload/App.app`结构的`.ipa`→当作Actions构建产物上传，不需要任何Apple开发者账号或证书密钥(签名这一步交给用户本地的Sideloadly/AltStore，用免费Apple ID在装机时现场签)。本地(Linux沙盒，没有Xcode/CocoaPods)验证过`npm install`/`npm run build`/`npx cap add ios`三步能跑通、生成的`ios/App/App.xcworkspace`和`www/index.html`内容正确；`pod install`/`xcodebuild`这两步依赖macOS环境，只能等推到GitHub后在真实的macOS runner上跑通验证 | accountbook-20260804 |
| 2026-08-05 | 确认设计稿：累计残高卡挪到"収支/ポイント"标签切换上面(两个tab共用，不重复放)，収支tab里新加支付方式内訳环状图，ポイント tab里新加积分内訳(按分类)环状图+独立的还元推移柱状图；不需要单独的"累计ポイント"卡(已有的胶囊徽标够用)。要求实装 | 已完成：①`#pageStats`重构成"累计残高卡(页面级，不变)→`#statsTopTabs`収支/ポイント分段→`#statsMoneyPane`/`#statsPointsPane`两块内容"，两块渲染逻辑都挂在`renderStats()`里一起跑(不做懒渲染，切标签只是纯CSS显示/隐藏，不会因为没渲染过而空白)。②`支付方式内訳`(収支tab新增)：复用累计残高卡同一个统计区间(`cumRangeStart`~`cumRangeEnd`)，不用再加一层月份选择器；支出/收入切换、按`paymentMethod`分组，颜色取`payment_methods`表的`badge_bg`(没有就退回`CHART_PALETTE`)，跟首页环状图是同一套donut+legend视觉语言。③`积分内訳(按分类)`(ポイントtab新增)：同样用那个统计区间，只统计`type==='income' && points`的记录按`catCode`分组，颜色直接复用`colorForKey`(分类本来就有颜色)。④`还元推移`(ポイントtab新增)：`renderTrendChart()`的简化单系列版本——不用再判断支出/收入(积分现在只可能来自收入)，没有堆叠，独立的月份导航状态(`pointsStatsDayYear`/`pointsStatsMonthYear`，跟収支tab那个趋势图各自看各自的时间段互不干扰)，点柱子能看当天/当月按分类拆开的积分明细(复用`getDailyBucketsForMonth`/`getMonthlyBucketsForYear`新加的`pointsByCat`字段)。用Playwright端到端验证：収支tab支付方式环状图切支出/收入正确分组显示金额和占比；ポイントtab积分环状图正确按分类聚合(salary 75%/sidejob 25%)；还元柱状图点某天弹出的明细正确显示分类拆分；日别↔月别切换、两个tab来回切换、空数据状态(甜甜圈显示灰环+"この期間の記録はまだありません")均无JS报错 | accountbook-20260804 |
| 2026-08-05 | 反馈"新建/编辑/复制"记账表单里"获得积分"字段消失了，指出从来没要求改这个入力页面——8月5日"pt只会存在收入下"那次bug修复只是让用户反馈的是集计页图表bug，结果多改了表单本身(切到支出档隐藏字段+保存时限制只有收入能存points) | 已完成：撤回表单侧的限制，只保留统计页图表那部分的修复(集计页积分柱子/图例/明细依然只在"收入"档显示，不受这次改动影响)。删掉`updatePointsFieldVisibility()`函数以及`openSheet`/`applyEntryToForm`/`segExpense`/`segIncome`四处调用点，"获得积分"输入框恢复成不分支出/收入、始终显示；`saveBtn`保存逻辑的`points`判断去掉`currentType==='income' &&`这个条件，恢复成不分类型只看填没填数字。用Playwright验证：新建记录默认"支出"档下"获得积分"输入框正常显示(不再隐藏) | accountbook-20260804 |
| 2026-08-05 | 纠正积分的业务归属：应该是"支出之后才会返积分"(比如信用卡消费返现)，不是挂在收入记录上；切到收入档就不用显示这个字段了 | 已完成：把这几天一直按"积分只跟收入挂钩"实现的逻辑整体翻转成"只跟支出挂钩"。①记账表单重新加回`updatePointsFieldVisibility()`(在`openSheet`/`applyEntryToForm`/`segExpense`/`segIncome`四处调用)，但方向反过来——切到"支出"档才显示"获得积分"输入框，切"收入"隐藏+清空；`saveBtn`保存时的`points`判断改成`currentType==='expense'`才写入。②`getDailyBucketsForMonth`/`getMonthlyBucketsForYear`分桶里，`points`/`pointsByCat`的累加从`if(e.type==='income')`分支挪到`else`(支出)分支。③首页"本月结余"卡、统计页"累计残高"卡的积分徽标汇总条件从`e.type==='income'`改成`e.type==='expense'`。④统计页收支趋势图的积分柱子/图例/点击明细，判断条件从`isIncome`改成`!isIncome`(现在切到"支出"档才会看到积分柱子)。⑤集计页"ポイント"标签下新增的"积分内訳(按分类)"环状图，过滤条件从`e.type!=='income'`改成`e.type!=='expense'`。用Playwright端到端验证：记账表单默认"支出"档正常显示积分字段、切"收入"隐藏；统计页用"同一天一笔带积分的支出+一笔普通收入"的数据验证，"支出"档柱状图/图例/明细正确显示积分、"收入"档完全不显示；ポイント标签下的环状图和还元推移柱状图都正确按支出记录聚合 | accountbook-20260804 |
| 2026-08-05 | 真机(iOS)试装成功后反馈两处：①顶部状态栏下面有一块黑色空白，适配没做好；②要求把顶部标题+图标固定，不要随内容滚动，三个页面都要 | 已完成：①黑色空白是`capacitor.config.json`里`contentInset:"automatic"`导致的——这个模式会让WKWebView自动让出安全区、不在状态栏下方绘制内容，露出原生容器默认的黑色底色；网页CSS这边其实早就用`env(safe-area-inset-top)`自己处理安全区留白了(PWA模式下一直工作正常)，两边"顶"在一起才导致状态栏底下多出一截黑边。改成`contentInset:"never"`让WKWebView整屏绘制、把安全区完全交给页面自己的CSS处理，同时补了`backgroundColor:"#101B33"`兜底避免加载瞬间的色差。②`.header`(标题+着せ替え/设置/黒字印章图标那一行)加`position:sticky; top:var(--safe-top)`固定在安全区下方，背景补成`var(--paper)`纸色，不然滚动的卡片经过头部下方时会跟透明的头部区域叠在一起分不清前后。过程中踩了个坑：直接把`position:sticky`写进`.header{}`规则完全不生效，排查后发现是`.page > *:not(.fw-bg-layer){position:relative;z-index:1;}`这条选择器带两个class、优先级比单class的`.header`高，把position悄悄吃掉了——改成用同样优先级的`.page > .header`单独覆盖position/z-index，靠这条规则在源码顺序里排在后面赢过它。用Playwright验证三个页面(家计簿/集計/為替)滚动后头部都正确悬停在顶部、背景遮住了滚过去的卡片内容，无花屏/无报错 | accountbook-20260804 |
| 2026-08-05 | 装上一版之后反馈：顶部状态栏那一整条（时间/信号/电量图标那一行）还是没固定住，往下滚动的内容(截图里是"支出"分段按钮)会从状态栏底下"透"出来，跟系统图标叠在一起 | 已完成：根因是`contentInset:"never"`让网页整屏绘制解决了黑边问题，但代价是原生状态栏本身默认是**透明覆盖**在网页上面的，不是一块不透明的实色条——所以只要网页内容滚动到最顶上，就会直接从状态栏底下透出来，跟系统时钟/电量图标叠在一起，跟之前修的`.header`是不是sticky没关系(header本身位置是对的，是它下面滚动经过的其它内容透上去了)。加了`@capacitor/status-bar`插件，新增`updateNativeStatusBar()`函数——在原生App环境下(用`window.Capacitor?.isNativePlatform()`判断，网页/PWA模式直接跳过)调用`StatusBar.setOverlaysWebView({overlay:false})`让状态栏改成不透明实色条，颜色跟着当前主题的`--paper`纸面色走(`setBackgroundColor`)，文字颜色也跟着深浅色主题切换(`setStyle`，夏·花火深色主题用浅色文字，基本主题用深色文字)；挂在`applyThemeSkin()`里，跟着换肤和App启动一起触发，主题切换时状态栏颜色也会同步跟着变。本地(Linux沙盒)验证过`npm install`装插件、`npx cap add ios`能正确识别到这个原生插件(`Found 1 Capacitor plugin for ios: @capacitor/status-bar@6.0.3`)；实际的不透明效果要等下一次真机安装才能最终确认 | accountbook-20260804 |
| 2026-08-05 | 反馈上一次编译出的iOS测试包图标不对(用户排查Sideloadly登录问题过程中发现) | 已完成：根因是`npx cap add ios`生成原生工程时从没配置过自定义App图标，用的是Capacitor自带的默认占位图标。修法：从`index.html`里现有PWA manifest嵌的512×512图标(白色小猪存钱罐+彩虹渐变背景+星星，`记帳本`这个manifest里本来就有)提取出来，用Lanczos重采样放大到1024×1024存进`resources/icon.png`作为图标源文件；新增`@capacitor/assets`依赖，workflow里`cap add ios`之后、`cap sync ios`之前插一步`npx capacitor-assets generate --ios`，自动把`resources/icon.png`铺进`ios/App/App/Assets.xcassets/AppIcon.appiconset`(现代Xcode的"单尺寸"图标目录格式，一张1024图就够，系统自动缩放，不用再手动生成一堆不同尺寸)。本地(Linux沙盒)验证过完整跑通`npm install`→`cap add ios`→`capacitor-assets generate --ios`，生成的`AppIcon-512@2x.png`打开确认就是真实的小猪存钱罐图标，不再是占位图；这次没有重新触发CI出新ipa验证(用户还在处理Sideloadly那边的Apple ID登录/Team ID问题，装机这一步本身还没跑通)，等那边解决后下次编译会自动带上正确图标 | accountbook-20260804 |
| 2026-08-06 | 装上一版之后反馈还是没修好："顶部没有同样颜色的实心条"，滚动内容(截图里还是"支出"分段按钮)依然穿到状态栏区域，要求再查一下 | 已完成，这次是之前修法本身踩了个iOS平台限制的坑：上一版加的`StatusBar.setOverlaysWebView()`/`StatusBar.setBackgroundColor()`这两个API经查证**在iOS上根本没有实现**(官方文档和插件issue都写明这两个方法只有Android/Web有效，iOS原生状态栏压根不存在"设背景色"这个概念)，等于上一版的修复代码在iOS真机上是空跑，怪不得用户装完毫无变化。既然原生API这条路走不通，改成纯CSS方案：在`<body>`最前面加一个全局唯一的`.status-bar-bg`元素，`position:fixed;top:0;left:0;right:0;height:var(--safe-top);background:var(--paper);z-index:17`——用固定定位的实色块把状态栏那一截region永久钉住，颜色跟着当前主题的纸面色走(换肤时CSS变量自动更新，不需要额外JS)，层级卡在header(z-index:16)之上、弹窗/下拉菜单(z-index:20+)之下，不随页面滚动、不会被任何卡片内容盖过去。同时清理了`updateNativeStatusBar()`里两个iOS上无效的死代码调用，只保留iOS/Android都支持的`setStyle()`(切状态栏图标黑白色)。本地(Linux沙盒Playwright)验证：新元素`position:fixed/top:0/z-index:17`且背景色正确取值当前主题`--paper`，三个页面滚动后头部/背景无报错无花屏；沙盒浏览器没有真实刘海屏,`env(safe-area-inset-top)`恒为0,这一步测不出真机上实际的高度和遮挡效果,需要用户装上新包后拿真机截图确认 | accountbook-20260804 |
| 2026-08-06 | ①要求打包成App之后加上"固定竖屏"功能；②顺带问之前做的App内更新提示功能在原生App里能不能生效 | ①已完成：竖屏锁定是iOS原生工程`Info.plist`里`UISupportedInterfaceOrientations`(iPhone)/`UISupportedInterfaceOrientations~ipad`(iPad)这两个key决定的，但`ios/`目录不提交进仓库、每次CI跑`npx cap add ios`全新生成，没法直接改仓库里一个不存在的文件，改成在workflow里`cap add ios`生成工程之后、`cap sync ios`之前插一步用macOS自带的`PlistBuddy`命令行工具把这两个key的数组都清空重写成只剩`UIInterfaceOrientationPortrait`一项(先`Delete`再`Add`，`Delete`外面包一层`|| true`防止模板万一没有这个key时报错中断)。本地(Linux沙盒)没有Xcode/PlistBuddy，这步依赖macOS runner，要等下次CI真跑一遍`cap add ios`才能确认patch脚本执行无误。②回答用户问题(不涉及代码修改)：说明`checkForAppUpdate()`用的是`fetch(location.pathname+'?_v='+时间戳)`重新拉"自己这个页面"比对`APP_BUILD`，这套机制是给网页版(GitHub Pages/PWA)设计的——原生App里`index.html`是编译时打包进`.ipa`的本地静态文件，不走网络，`location.pathname`拉到的永远是自己，`APP_BUILD`比对条件永远不成立，所以这个更新提示条在原生App里**不会生效**；原生App目前没有OTA更新机制，每次改动都得重新出一版`.ipa`手动装 | accountbook-20260804 |
| 2026-08-06 | 约定以后的发布流程：先推到当前测试分支(如`accountbook-20260804`)验证，验证通过再合并主分支`claude/upload-project-github-ww338s`发布，不要每次都先合主分支再编译 | 已完成(流程约定，不涉及代码)：验证了`workflow_dispatch`只要求workflow文件本身存在于要编译的那个分支上，不要求一定是默认分支——之前几次"合并了才能编译"是因为workflow文件当时还没在任何分支存在过、GitHub那边完全没注册这个workflow(404)，现在文件已经在主分支注册过，之后可以直接对`accountbook-20260804`这类测试分支发起`workflow_dispatch`编译验证，不用每次都先合并。同时顺手把主分支比测试分支多出来的几个提交(`.claude/skills`相关，跟App代码无关)快进合并回`accountbook-20260804`，保证两边基线一致 | accountbook-20260804 |
| 2026-08-06 | 反馈IPA需要做版本号管理，问能否做到App内自动更新；顶部实心色块挡住了"夏·花火"主题本来做的星空/烟花背景，问能不能直接用那个背景填满状态栏区域；后又简化要求为"只做按钮及图标的位置固定" | 分两部分，App内自动更新讨论后达成方案：保留Sideloadly手动装机不变，新增版本号+接入AltStore自带的更新源机制(不改状态栏那块背景，用户简化需求后确认现有的`.status-bar-bg`纯色遮挡方案已经足够挡住内容穿帮，不用额外还原花火动画背景，这部分未改代码)。已完成：①**版本号**：新增仓库根目录`VERSION`文件(语义版本号)，`ios-test-build.yml`新增"算版本号"步骤(`VERSION`文件内容+`${{ github.run_number }}`当build号，每次编译必定不同)，`Info.plist`新增patch`CFBundleShortVersionString`/`CFBundleVersion`；`index.html`里`index.html`构建产物(仅CI里的`www/index.html`，仓库里这份源文件不变)会把设置页新加的`__NATIVE_APP_VERSION__`占位符替换成真实版本号，`revealNativeVersionRow()`只在`Capacitor.isNativePlatform()`为真时才把这一行从`display:none`切换出来，网页/PWA模式完全不受影响(Playwright验证网页模式下该行仍是隐藏状态、文字仍是未替换的占位符原样)。ipa文件名和Actions产物名也带上版本号(`jiazhangbu-unsigned-v{版本}-{build}.ipa`)方便区分。②**AltStore更新源**：新增`.github/workflows/ios-release.yml`，跟`ios-test-build.yml`平行独立、手动触发，只在"测试分支已验证、决定要发布"时手动点一次(通常合并进主分支之后跑)——除了走一遍完整编译流程，额外生成一份AltStore更新源格式的`altstore-source.json`(app信息+版本+下载地址+文件大小，同时写`versions`数组和顶层字段兼容新旧AltStore客户端)，用`gh release`发布/覆盖一个标签固定为`ios-latest`的GitHub Release，把ipa、`altstore-source.json`、App图标一起挂上去——固定标签保证下载链接每次发布都不变，用户只需要把`.../releases/download/ios-latest/altstore-source.json`这个地址在AltStore里添加成"更新源"一次，以后每次点这个新workflow发布，AltStore里就会自动出现"Update"提示，不用再手动复制链接。**Sideloadly完全不受影响**：`ios-test-build.yml`本身除了多算版本号、patch版本信息之外，产物依然是普通的未签名`.ipa`文件，跟以前一样直接下载拖进Sideloadly签名安装。本地(Linux沙盒)验证过两份workflow yaml语法(`python3 -c yaml.safe_load`都能正常解析)、`index.html`的JS改动语法检查通过、Playwright确认版本行在网页模式下正确保持隐藏；`gh release`/`PlistBuddy`/实际AltStore加源效果依赖macOS runner和用户真机，要等下次实际跑`ios-release.yml`并在AltStore里添加更新源才能最终验证 | accountbook-20260804 |
| 2026-08-06 | 提出一个新方案试一版：状态栏顶部实心条平时正常完全遮盖，但在滑动过程中让这块实心变透明，停下来再变回实心 | 已完成，先出一版试试效果：`.status-bar-bg`加`transition:opacity 0.2s ease`，新增`.is-scrolling{opacity:0}`；新增一段`initStatusBarScrollFade()`监听`window`的`scroll`事件(`{passive:true,capture:true}`，因为主页面滚动是普通的document/body原生滚动，没有额外包一层可滚动容器)，滚动时立刻给实心条加上`is-scrolling`淡出，每次滚动事件都重置一个170ms的定时器，停止滚动170ms后自动摘掉这个class、淡回实心。网页/PWA模式下这块条本来高度就是0(没有真实状态栏)，加这个监听肉眼看不出区别，不需要额外判断是否原生环境。用Playwright验证：触发滚动后立即检测到`is-scrolling`class(实心条变透明)，停止滚动300ms后(超过170ms的静止阈值)该class被正确移除(实心条淡回不透明)，过程无报错 | accountbook-20260804 |
| 2026-08-06 | 集计页"ポイント"标签下的积分内訳环状图要求从按分类分组改成按支付方式分组 | 已完成：`renderPointsDonut()`分组key从`e.catCode || e.category`改成`e.paymentMethod || 'cash'`，配色/图例标签从`colorForKey`/`labelForKey`(分类那套)换成`payMethodColor`/`payMethodLabelForKey`(复用上面支付方式内訳环状图同一套，两张图里同一种支付方式颜色能对上，方便对照)；标题文案(HTML默认文字+中日文i18n)从"积分内訳(按分类)"/"ポイント内訳(分類別)"改成"积分内訳(按支付方式)"/"ポイント内訳(支払い方法別)"。这个图没有维度下拉框，只有配色/分组逻辑需要改，改动范围很小。本地(Linux沙盒)做了JS语法检查确认无误；沙盒里分类/支付方式基础数据依赖网络，无法端到端跑通entries数据走UI验证图表实际渲染效果，逻辑上直接照抄已经在用的`renderPayMethodDonut()`同一套分组/配色写法，风险低 | accountbook-20260804 |
| 2026-08-06 | 截图反馈改成按支付方式分组后，图例只有一行(比如只用了一种支付方式)时卡片下面留了一大截空白，要求图例卡片高度改成自适应内容 | 已完成：根因是`#pageStats .chart-card{flex:1 1 auto; min-height:380px}`这条规则给集计页所有图表卡片统一设了380px保底高度，这是当初为了让趋势柱状图能撑满剩余空间特意加的，但套用到"支付方式内訳"/"积分内訳"这两张环状图+图例卡片上就不合适——分组数通常很少，图例内容用不到这么高，多出来的空间只能空着。新增一条更高优先级的`#pageStats #payMethodDonutCard, #pageStats #pointsDonutCard{flex:0 0 auto; min-height:0}`，只针对这两张环状图卡片取消保底高度和flex伸展，让它们按内容实际高度收缩；趋势图那两张卡片不在这条新规则里，继续保持撑满剩余空间。用Playwright对比修复前后：积分卡片(空数据状态)从380px降到312px，实际有数据、图例只有一行时会更矮；顺手确认"支付方式内訳"卡片(收支标签下同款卡片)使用同一条新规则、逻辑一致 | accountbook-20260804 |
| 2026-08-06 | メルペイ支付方式图标背景是白色方块，要求改成透明背景 | 已完成：メルペイ没有矢量素材，一直用内嵌的官方PNG(`BRAND_LOGO_RASTER['pm-merpay']`)，这张图本身背景就是不透明白色，之前套进`.pay-badge`(账目列表的支付方式徽标)、`.sub-pill`(选支付方式的按钮)这些有色底(纸色/金色)的容器里就会露出一块方形白边。本地用Pillow处理这张PNG：从四条边界向内洪水填充找出"背景白色区域"(核心阈值RGB≥250)，只对这块连通区域(以及向内膨胀2像素、覆盖抗锯齿边缘环)做透明化处理——边缘按"离纯白的距离"算出新alpha并反预乘颜色，避免留白边发虚；图案内部被红色包围的白色"m"字母因为不连通到外部背景区域，不受影响、保持不透明白色。处理时发现这张PNG源文件本身有个已存在的问题(不是这次改动引入的)：PIL加载时报"broken data stream"，图案里能看到几条很淡的横纹解码瑕疵，猜测是原始素材裁剪/转base64时数据有轻微截断；这次不处理这个问题——图标实际显示尺寸很小(18-24px)，肉眼几乎看不出来，且用户这次只反馈了背景色问题，需要的话之后可以换一张更干净的素材。用Playwright验证：替换后的base64能正常解码成96x96 PNG并加载(`Image.onload`成功)，套进彩色/纸色背景上对比截图确认不再有白色方块 | accountbook-20260804 |
| 2026-08-06 | 真机反馈滑动淡出方案效果不对：滑动过程中顶部会露出背景，停下来才变实心；实心条透明度好像根本没变化；而且顶部有两块实心条(状态栏那条+头部)，都要一起修 | 已完成，根因是上一版只给`.status-bar-bg`(真正盖状态栏那一细条)加了滑动淡出，`.header`(标题+图标那一整块，纸色同款实心底，紧接在状态栏条正下方)完全没有跟着变——两块颜色一样但只有上面那条跟着滑动变透明，下面的头部却全程不透明，滑动时看起来就是"一截透明一截不透明"很不协调，容易被看成"没有统一在变透明、还是露着别的东西"。这次给`.header`也加上跟`.status-bar-bg`同一套`transition:opacity 0.2s ease`+`.is-scrolling{opacity:0}`，`initStatusBarScrollFade()`里`window`的scroll事件同时给状态栏条和(所有页面共3份)头部一起切换`.is-scrolling`，滑动时两块同步淡出、停下来170ms后同步淡回实心，看起来是同一整条在变化。用Playwright验证：触发滚动后状态栏条和当前激活页面的头部同时带上`is-scrolling`(算出的`opacity`确实中途在0~1之间过渡)，停止滚动300ms后两者都正确摘掉这个class、淡回不透明 | accountbook-20260804 |
| 2026-08-06 | 反馈状态栏条和头部这两块实心条之间还有空隙 | 已完成，根因：未滚动的初始状态下头部的位置是由`.app`的`padding-top`(`safe-top+18px`)决定的，比`.status-bar-bg`的下边缘(`safe-top`)整整低18px——sticky只在滚动到需要"追上"才生效，页面刚打开、还没滚动时头部停在它自然的位置上，两块实心色块之间就露出这18px的空隙(能看到底下的东西透出来)。修法：给`.header`加`margin-top:-18px`把它整体往上顶，让它从一开始就跟状态栏条无缝贴合，不用等滚动触发才贴上；顶掉的18px换算成`padding-top`补回头部内部(原来6px改成24px)，标题文字相对头部自己顶边的视觉间距完全不变，只是这份留白从"头部外面透明的缝"挪到了"头部自己不透明的背景内"，头部整体占的高度、下面卡片的位置都没变。用Playwright模拟`--safe-top:50px`(沙盒里真实值恒为0测不出这个bug)验证：修复前状态栏条下边缘和头部上边缘之间有18px空隙，修复后缝隙为0px，滚动前后都是0px(不依赖sticky触发)，无报错 | accountbook-20260804 |
| 2026-08-06 | 想试一下把顶部状态栏那块实心条改成透明看看效果 | 已完成(试验性质，不确定是否保留)：`.status-bar-bg`的`background`从`var(--paper)`改成`transparent`，只改这一块，`.header`(标题+图标那块)维持不透明，因为里面有实际内容需要保持清晰。只推到测试分支，没有触发编译，等确认要不要保留这个效果再说 | accountbook-20260804 |
| 2026-08-06 | 完全透明不要了，改成背景虚化(近似透明但毛玻璃模糊)，先出原型图看效果 | 已完成：先复制一份`index.html`到本地临时改`.status-bar-bg`/`.header`的background为`color-mix(in srgb, var(--paper) 55%, transparent)`配合`backdrop-filter:blur(14px) saturate(140%)`(iOS原生导航栏同款毛玻璃效果)，用Playwright在浅色主题和"夏·花火"深色主题下各截了一张滑动到有内容经过顶部时的原型图发给用户确认(未改真实代码/未提交)。用户确认"推吧"后，把同样的改动应用到真实`index.html`：完全透明会让滑动经过的内容原样穿帮(等于没修状态栏那个bug)，完全不透明又挡住了花火主题的星空/烟花背景，半透明+模糊两头兼顾——经过的内容变成模糊色块看不清细节，花火背景的轮廓还能隐约透出来。用Playwright验证`getComputedStyle`确认两个元素`backdropFilter`都正确应用成`blur(14px) saturate(1.4)`，无报错；只推到测试分支，没有触发编译 | accountbook-20260804 |
| 2026-08-06 | 真机截图反馈：透明度还要再透一点；状态栏条和头部这两块能不能合成一条；顶上那条实心条盖到了图标(装饰纸条被糊掉一截) | 已完成，这次直接把两块合成一个元素：之前`.status-bar-bg`(position:fixed，盖真正的状态栏细条y:0~safe-top)和`.header`(position:sticky，紧接在下面)是两个各自独立的`backdrop-filter`区域，拼接处会有一条看得出来的接缝，而且`.washi-tape`(左上角装饰纸条，设计成"从卡片顶边探出去8px"的效果)是相对`.header`定位的，`.status-bar-bg`用更高的z-index叠在上面正好把纸条探出去的那一截糊掉了。改法：删掉`.status-bar-bg`这个单独的元素/CSS，把`.header`自己的sticky粘住位置从`top:safe-top`改成`top:0`，`margin-top`/`padding-top`在原来"抵消`.app`的18px顶部留白"基础上再往上多顶`safe-top`这么多，让头部自己的实心底一路延伸到真正的屏幕顶端(标题文字视觉位置不变，顶掉的空间换算成padding补回来)——现在整块只有一个`backdrop-filter`，没有接缝。`.washi-tape`的`top`从写死的`-8px`改成`calc(var(--safe-top) - 8px)`，跟着"卡片顶边"这个视觉锚点一起挪到safe-top那条线，不再被谁盖住。透明度从`color-mix(...55%,transparent)`降到`32%`，更透一些。滑动淡出的JS(`initStatusBarScrollFade`)也简化成只用管头部一个元素，不用再同步两个。用Playwright验证：未滚动状态下头部顶边刚好在y=0(不再有单独的状态栏条元素)，滚动时头部正确淡出、停下后淡回，无报错；截图确认视觉上是一整块连续的毛玻璃区域，没有断层 | accountbook-20260804 |
| 2026-08-06 | 要求能比较清晰看到背后的"夏·花火"星空/烟花背景，先出原型图确认 | 已完成：先复制一份`index.html`到本地把`.header`的`backdrop-filter`从`blur(14px)`降到`blur(5px)`、纸色不透明度从`color-mix(...32%,transparent)`降到`15%`，用Playwright在"夏·花火"深色主题下截了未滚动(星星清晰可见)和滚动经过卡片(内容软化模糊但星星仍在)两张原型图发给用户确认，用户回复"推送一下我验证"后应用到真实`index.html`同一处规则。只推到测试分支，没有触发编译 | accountbook-20260804 |
| 2026-08-08 | 想再试一版完全透明看看 | 已完成(试验性质)：`.header`的`background`改回`transparent`，去掉`backdrop-filter`模糊，只推到测试分支，没有触发编译，等用户看完决定要不要保留 | accountbook-20260804 |
| 2026-08-08 | 反馈网页版(GitHub Pages)的更新提示条好像不生效 | 已完成，根因不是机制坏了，是维护疏漏：查了`pages-build-deployment`这个GitHub自动workflow的部署记录，确认Pages一直是从`accountbook-20260804`分支自动部署、每次push都成功重新构建，服务器上的代码是新的；但`checkForAppUpdate()`靠对比`APP_BUILD`这个常量判断"是否有新版本"，这个值需要每次实质性改动手动改一下，最后一次改是8月5日的`20260805a`，之后8月6日到8月8日这一长串改动(积分环状图/卡片高度/メルペイ图标/状态栏头部合并/毛玻璃好几轮/其他会话的图标改版等)我都忘了同步改这个值——服务器和浏览器两边的`APP_BUILD`其实一直是同一个字符串，比对结果永远"相等"，更新条自然弹不出来。这次把`APP_BUILD`改成`20260808a`补上；之后每次实质性改动要记得同步改这个值，不能只顾着iOS这边忘了网页版的更新提示机制 | accountbook-20260804 |
| 2026-08-08 | 反馈滑动页面时"夏·花火"背景的烟花特效会卡顿，要求给个优化方案 | 已完成，先出方案再实现：卡顿根因是烟花动画自己跑一个独立的`requestAnimationFrame`循环，每帧要算几百个粒子的物理(位置/拖尾/衰减)、画Canvas(每个粒子的渐变拖尾+`shadowBlur`发光，`shadowBlur`在iOS上尤其吃性能)，这些JS计算和绘制跟页面滚动抢占同一条主线程，滚动时两边一起挤占CPU就会卡。方案：滑动时直接暂停这个rAF循环(画面定格、不消耗主线程)，停止滑动后自动恢复——复用了已有的滚动监听(跟状态栏毛玻璃淡出是同一个`initStatusBarScrollFade`)，不用新增额外的scroll事件监听。`startFireworksCanvas()`里给`fwEngine`加了`pause()`/`resume()`两个方法(`pause`只是`cancelAnimationFrame`，`resume`重新`requestAnimationFrame`且重置`last`时间戳，配合已有的`dt`钳制`Math.min(...,0.05)`，暂停一段时间后恢复不会因为时间差过大导致物理一下跳很远)；`reduced-motion`分支的静态版本没有动画循环，`pause`/`resume`不存在时用`&&`短路跳过，不会报错。顺手把画布像素比上限从1.5降到1，减少每帧要处理的像素量。用Playwright(monkeypatch `requestAnimationFrame`计次)验证：正常状态下rAF持续在跑，触发滚动后100ms内rAF次数完全不增长(已暂停)，停止滚动、过了170ms的静止阈值后rAF重新持续增长(已恢复)，无报错 | accountbook-20260804 |
| 2026-08-08 | 实心条透明度改成10%看看 | 已完成(试验性质)：`.header`的`background`从完全透明改回`color-mix(in srgb, var(--paper) 10%, transparent)`，配合恢复`backdrop-filter:blur(5px) saturate(150%)`，只推到测试分支，没有触发编译 | accountbook-20260804 |
| 2026-08-08 | 调成5%我看看 | 已完成(试验性质)：`.header`的纸色不透明度从10%降到`color-mix(in srgb, var(--paper) 5%, transparent)`，模糊维持`blur(5px)`不变，只推到测试分支，没有触发编译 | accountbook-20260804 |
| 2026-08-08 | 截图反馈两处：①从来没要求收支标签下加"支付方式内訳"环状图(只有积分标签要)，要求去掉；②支出/收入趋势柱状图数据不对，切到"收入"看不到柱子、纵轴刻度显示成"1/1/1/0/0"这种数字 | 已完成两处：①删掉`statsMoneyPane`里的`payMethodDonutCard`整块(HTML卡片+`payMethodTypeSegment`切换按钮+`renderPayMethodDonut()`函数+它的调用点+相关i18n文案`payMethodDonutTitle`/`payMethodOtherBucket`)，保留`payMethodColor`/`payMethodLabelForKey`这两个颜色/标签辅助函数(积分内訳环状图今天刚改成复用这两个函数，不能删)。②根因：`renderTrendChart()`按日对比时默认把视口滚动定位到"今天"这一根柱子，但没检查"今天"在当前选中的类型(支出/收入)下是否真的有数据——收入通常只集中在发薪日，切到"收入"档如果今天没有收入记录，就会定位到一段完全没有收入柱子的空视口，`visibleMaxVal()`在空视口里找不到数据返回`null`，`currentMaxVal`掉回兜底值`1`，纵轴刻度就会算出`round(0×1)=0, round(0.25)=0, round(0.5)=1, round(0.75)=1, round(1)=1`这种"0/0/1/1/1"的丑数字，柱子高度全部按0画、完全不显示——明明这个月有收入(图例汇总不受视口影响，照常显示"给与 100%")，柱状图区域却像是完全没数据，让人以为数据丢了。修法：只有"今天"对当前选中类型确实有数据(`buckets[todayIdx][valField] > 0`)时才用它当默认定位点，没有就跟"浏览历史月份"一样退回"当前类型最后一笔有数据的那一段"；`renderPointsTrendChart()`里同样的定位逻辑有一模一样的问题，一并修了。用Playwright复现：修复前用"1号发薪、8号买东西"这组测试数据切到收入档，纵轴显示`["0","0","1","1","1"]`、0根柱子可见；修复后纵轴正确显示`["0","2.0k","4.0k","6.0k","8.0k"]`、柱子正常显示，无报错 | accountbook-20260804 |
| 2026-08-08 | 反馈删掉支付方式内訳环状图后，"按日对比/按月对比"分段控件直接贴在"收支/积分"标签下面，两个长得一样的胶囊控件叠一起很难看，要求重新设计；经过多轮原型图(下划线标签/迷你虚线胶囊/合并卡片/圆形图标/纯文字/迷你分段胶囊/居中 vs 撑满等方案对比)，最终确认：合成一张卡片(日别月别切换+今天对比数字+月份选择+趋势柱状图全部在一起)+圆形图标按钮(呼应页头图标语言，选中态实心填色)+居中对齐 | 已完成：HTML结构上把原来`statsMoneyPane`里三个各自独立的块(`dimSegment`分段、`compareRow`对比数字、单独的趋势图`chart-card`)合并进同一张`.chart-card`(`id="compareCard"`)，日别/月别按钮从`.segment`胶囊换成`.dim-toggle-row`(两颗36px圆形按钮，2px虚线描边，居中，选中态`background:var(--seal)`实心+描边转实线)；`compareRow`的CSS/JS从专门维护的`.compare-row`/`.compare-item`样式改成直接复用已有的`.stats-row`/`.stat`(跟"累计残高"卡是同一套结构和样式，`renderCompare()`里生成的`class`从`compare-item`改成`stat`)，顺带删掉了不再用的`.compare-row`/`.compare-item*`这套CSS；`.stats-row`自带的`border-top:2px dotted`正好用作dimSegment和对比数字之间的分区线；月份选择器`ym-picker`挪进卡片中段，新增`#compareYmPicker`规则改成左对齐+点状分隔线跟上面区块隔开(不再是单独卡片里居中摆放)。日/月按钮的文案从"按日对比/按月对比"缩短成单字"日/月"塞进小圆圈里。用Playwright验证：合并后卡片结构正确渲染、真实测试数据下趋势图正常显示柱子，点击"月"按钮后`dimMonthBtn`正确切到active、标题正确变成"本年每月收支"，原有的日/月切换点击逻辑(`$('dimSegment').querySelectorAll('button')`)不用改就能兼容新markup，无报错 | accountbook-20260804 |
| 2026-08-08 | 透明度改成1%看看 | 已完成(试验性质)：`.header`纸色不透明度从5%降到`color-mix(in srgb, var(--paper) 1%, transparent)`，模糊维持`blur(5px)`不变，只推到测试分支，没有触发编译 | accountbook-20260804 |
| 2026-08-08 | 模糊程度降低一点 | 已完成(试验性质)：`.header`的`backdrop-filter`模糊半径从`blur(5px)`降到`blur(2.5px)`，纸色不透明度维持1%不变，只推到测试分支，没有触发编译 | accountbook-20260804 |
| 2026-08-08 | 真机截图反馈两处：①日/月圆形按钮位置没摆好，左右间距/居中/上下位置都有问题；②支出趋势图(按月对比)一个月份显示两条柱子，怀疑数据没改好 | ①用Playwright量了圆形按钮的实际坐标，横向确实是精确居中的(卡片中心x=195，两个按钮组合起来的中心也是x=195，左右留白完全相等)，不是居中逻辑本身的bug；但圆形按钮上下留白确实很紧凑——`margin-bottom`原来只有2px，贴着下面的点状分隔线，卡片顶部也没有额外留白，只靠卡片自身18px的padding撑着。加了`margin:4px 0 16px`(上4px下16px)，圆圈之间的间距也从14px放宽到18px，整体不再显得挤。②经查证不是bug：柱状图第二条细一点的绿色(jade)柱子是"积分"柱——这是这次会话更早之前就做好的既有功能，在"支出"档下，如果这个月/这天有账目带积分(比如信用卡消费返现)，会在金额柱子旁边多画一根积分柱子，用独立的刻度撑到差不多高度(所以视觉上跟金额柱子差不多高，但两者单位完全不是一回事，积分柱子高度不代表跟金额可比)。用测试数据(一笔带60积分的支出记录)复现确认：`.trend-bar-seg`(金额柱)和`.trend-pt-bar`(积分柱，`fill:rgb(122,155,110)`即`--jade`)确实都各渲染了1个，是设计如此，不是这次改动引入的新问题；这次合并卡片改版之前这根积分柱子本来就存在，只是排到组件里位置更显眼、用户之前没太注意到 | accountbook-20260804 |
| 2026-08-08 | 明确纠正：不接受上一条"积分柱子是既有设计"的解释，要求收支趋势图彻底不要跟积分混在一起("point是point，收支是收支")；顺带要求日/月圆形按钮位置继续优化、整体布局再调整 | 已完成，这次直接把积分从收支趋势图里整个拆掉，不再讨论是否"设计如此"：`renderTrendChart()`里跟积分相关的代码全部删除——`ptBarW`/`ptGap`两个柱宽/间距变量、`groupW`的计算(简化回`barW+barGap`，不用再给积分柱留位置)、图例区`grandPoints`统计和积分那一行、`visiblePtMaxVal()`整个函数、`.trend-pt-bar`的柱子绘制、`applyScale()`里`ptBarEls`那一段缩放逻辑和多余的`ptMaxVal`参数、点击柱子弹出的明细`showTrendPointInfo()`里的`ptHtml`积分行；柱子的x坐标/文字居中位置也跟着从"金额柱+积分柱两块并排"简化回"只有金额柱一块"的坐标算法。积分现在只会出现在"积分"标签页自己的图表里(`renderPointsDonut`/`renderPointsTrendChart`，这两个函数完全独立、没有被牵动)。日/月圆形按钮的位置沿用上一条已经做的间距调整，没有再改。用Playwright复现原来报告的场景(一笔带60积分的支出记录，切到按月对比)验证：`.trend-bar-seg`还是1个(金额柱)，`.trend-pt-bar`变成0个，图例里也不再出现"积分"字样；切到"积分"标签页确认`pointsDonutTotal`仍正确显示60，没有被误伤 | accountbook-20260804 |
| 2026-08-08 | 5px降到1px看看 | 已完成(试验性质)：`.header`的`backdrop-filter`模糊半径从`blur(2.5px)`降到`blur(1px)`，纸色不透明度维持1%不变，只推到测试分支，没有触发编译 | accountbook-20260804 |
| 2026-08-08 | 反馈"今天支出/今天收入"两栏里"今日收入"左边空出一大截，要求重新设计布局 | 已完成：根因是`#compareRow`用的是`.stats-row`/`.stat`(`flex:1`均分)那套通用样式，两栏内容默认左对齐——支出/收入两边数字位数不一样长时(比如一边¥0一边¥130,771)，两栏各自的文字宽度参差不齐，中间就会看起来像莫名多出一大截说不清用途的空白，尤其数字短的那栏更明显。改法(只加在`#compareRow`这个实例上，没动`.stats-row`/`.stat`这套被"累计残高"卡等其它地方共用的基础样式，避免连带影响)：两栏内容改成各自居中(`text-align:center`)，不管数字长短两栏视觉上都对称；再给收入这栏加一条竖着的点状分隔线，精确卡在两栏中间(`border-left`配合`padding-left`/`margin-left`各12px抵消掉一半的24px间距)，把中间这条缝从"看不出用途的空白"变成"看得出是故意留的分隔线"。用Playwright测试了数字位数差异很大的场景(¥30 vs ¥1,289,346)，两栏保持居中对称、分隔线位置不受数字长短影响 | accountbook-20260804 |
| 2026-08-08 | 还是改回原来左对齐 | 已完成：撤销上一条的居中+分隔线改动，`#compareRow .stat`恢复成`.stats-row`/`.stat`基础样式默认的左对齐，不再有额外的`text-align:center`和分隔线 | accountbook-20260804 |
| 2026-08-08 | 截图发了之前独立带边框卡片盒子的样式，要求"再放回去看看长什么样" | 已完成(试验性质)：`#compareRow .stat`加回`background:var(--card);border:2px solid var(--grid);border-radius:18px 8px 18px 8px;padding:14px`(就是之前删掉的`.compare-item`那套盒子样式)，间距从`gap:24px`收紧到`12px`，去掉外层`.stats-row`自带的点状分隔线(`border-top:none`)，改用盒子自己的边框做区隔。用Playwright截图确认效果跟用户发的截图一致，已推到测试分支 | accountbook-20260804 |
| 2026-08-08 | 反馈盒子样式"现在这样不合适"，要求稍微变动一下 | 已完成(试验性质)：盒子样式从`2px实线描边+不对称圆角(18px 8px 18px 8px)`改成`1.5px虚线描边+统一16px圆角`，跟上面日/月圆圈按钮同一套虚线视觉语言，减轻"大卡片套小卡片"的实线重边框观感；`padding`从14px微调成13px 14px。已推到测试分支 | accountbook-20260804 |
| 2026-08-08 | 要求推一版正式app、检查0804累计的更新内容不要有遗漏，并把App对外的名字改成"PigBang"(涉及命名的地方都用这个)，明确app内软件名目前不用动 | 已完成改名部分：先确认过范围——之前有另一会话尝试把改名范围扩大到`index.html`的`<title>`/`apple-mobile-web-app-title`(改成"PiggyBloom")，被撤销(`fd7a3f5`→`caf2bf0`)，跟这次"app内软件名可以不动"的要求一致，所以这次改名严格只动"对外/原生壳"相关的命名，不碰`index.html`里任何面向用户的文案：①`capacitor.config.json`的`appName`从"记账本"改成"PigBang"(`appId`不动，纯技术标识符不算"名字")；②`.github/workflows/ios-test-build.yml`的ipa文件名/上传的artifact名从`jiazhangbu-*`改成`pigbang-*`；③`.github/workflows/ios-release.yml`的`IPA_FILENAME`环境变量、AltStore更新源JSON里app的`name`字段("记账本"→"PigBang")、source的`name`字段("记账本 更新源"→"PigBang 更新源")、GitHub Release的`--title`、最后上传的artifact名，全部同步成PigBang(`bundleIdentifier`等技术ID不动)；④`ALTSTORE_GUIDE.md`里描述AltStore里会看到的App名字的几处也同步改成PigBang(不然发布后教程文字和AltStore里实际显示的名字对不上)；`README.md`描述的是网页版/PWA本体，跟"app内软件名"是同一件事，按要求不动。全部改动过syntax检查：`capacitor.config.json`过`json.load`，两个workflow的yaml过`yaml.safe_load`，`ios-release.yml`里内嵌的python heredoc额外单独提取出来过了`compile()`；`index.html`的内联JS也重新跑了一遍`node --check`确认没有被误改动。检查0804分支：`git log`确认从`accountbook-20260804`到`claude/upload-project-github-ww338s`一共26个commit，逐条过了一遍(状态栏/头部合并、图标改版、毛玻璃透明度多轮试验、支付方式内訳环状图移除、日/月圆形按钮重设计+合并卡片、积分柱子彻底从收支趋势图移除、烟花滚动卡顿修复、`APP_BUILD`过期修复、今日支出收入两栏box样式改回+两轮描边调整、一次改名到PiggyBloom又撤销)，没发现遗漏或半成品，确认可以合并发正式版 | accountbook-20260804 |
| 2026-08-08 | 截图反馈"集計"页收支趋势图下面的分类图例(住宅/食費/通信費...那一排)要求加个功能：点这个图例能弹出这个分类下的出入金明细，积分那根柱子的图例不用管 | 已完成：`renderTrendChart()`里给`trendCatLegend`每一项加`clickable`class+`data-key`(分类code)，点击后复用首页环状图图例已有的`openMonthDetail(filter, monthKey)`+`monthDetailSheet`机制(不用另外写一套明细列表UI)，传`{catCode:key, type:trendChartType}`当筛选条件；日别对比时传当月`'YYYY-MM'`，月别对比时传当年纯`'YYYY'`——`renderMonthDetailList()`内部原本就是用`e.date.startsWith(monthKey)`做日期匹配，纯年份字符串一样能当前缀匹配用，不用改这部分逻辑，只是`monthKeyLabel()`原来只认`'YYYY-MM'`格式(标题会显示成"2026年NaN月")，补了一个分支处理只有年份没有月份的情况。"point的柱状图可以除外"这点天然满足：积分页(`renderPointsDonut`按支付方式分组、`renderPointsTrendChart`只有单色柱子)本来就没有这种分类图例，唯一有图例的地方就是收支趋势图这一处，不用额外排除代码。用Playwright模拟测试数据(住宅两笔+食費一笔当月，另加一笔上月住宅)验证：日别对比点"住宅"图例只弹出当月2笔住宅记录(食費和收入都被正确排除)；切到月别对比再点"住宅"图例，正确弹出跨两个月共3笔住宅记录(验证了整年范围筛选生效)，无报错 | accountbook-20260804 |
| 2026-08-08 | 要求新增支付方式"suica"，用户直接贴图指定用官方那个绿底企鹅图标 | 已完成客户端部分：用户是内联贴图不是文件上传，从会话transcript(JSONL)里按发图时间定位、提取出原始PNG(240×240，带透明外角的圆角方块图标，6224字节)，参照メルペイ/ペイディ的先例算了sha256核对提取出来的字节跟transcript里的原始base64解码结果完全一致(避免之前ペイディ那次手抄大段base64被终端截断坏掉的问题重演，这次是脚本直接从文件读完整字符串写入，插入后也重新读回文件里的base64核对了一遍长度和解码后字节)。图标本身已经是官方定稿的圆角方块+企鹅设计（不是那种带留白等裁剪的独立logomark），直接原样保留没有裁剪背景，只是走已有的`BRAND_LOGO_RASTER`映射新增`pm-suica`键(跟メルペイ/ペイディ同一条渲染路径，`pmIconHtml()`不用改)。用Playwright在实际UI尺寸(约20px的支付方式选择胶囊)截图确认企鹅图标清晰可辨、不糊。数据库这边`payment_methods`表还没有`suica`这一行，SQL已发给用户在Supabase执行(新增一行而不是改现有行) | accountbook-20260804 |
| 2026-08-09 | 要求"明细"(monthDetailSheet,点首页环状图/图例/趋势图柱子弹出的那个明细弹层)上方加个搜索框，模糊检索备注、分类、支付方式 | 已完成：`monthDetailSheet`里筛选标签(`monthDetailFilterChip`)和列表(`monthDetailList`)之间新增`.detail-search`搜索框(新增`ic-search`线条图标，跟`ic-gear`等其它图标同一套画风)，输入内容存到新state`monthDetailSearchQuery`，`renderMonthDetailList()`在原有的按类型/分类/标签筛选之后，追加一层子串匹配(小写化后`includes`)：备注直接匹配`e.note`；分类匹配`catLabel(allCatMap[e.catCode])`(找不到分类对象退回旧的`e.category`字符串字段兼容老数据)；支付方式匹配`payLabel(payMethodMap[e.paymentMethod])`——三者只要命中一个就保留这条记录，不要求同时匹配。匹配的是"当前显示语言"下的分类/支付方式名称(不是同时匹配中日双语)，跟用户在列表里实际看到的文字保持一致。搜索框在`openMonthDetail()`每次重新打开明细时清空(避免下次打开残留上次的搜索词)，新增一个"✕"清空按钮(只在有输入内容时显示)。placeholder文案走`applyStaticI18n()`同一套机制，中/日文案都补了。用Playwright端到端测试：按备注子串("房租")、按分类名("食費"，命中该分类下2笔)、按支付方式名("Suica")分别搜索都只返回预期的记录；验证过大小写不敏感("Suica"和"suica"结果一致)；搜索无匹配时显示已有的"这段时间还没有相关记录"空状态文案(没有另外新增一套文案)；清空按钮点击后正确清空输入框、恢复显示全部记录，无JS报错 | accountbook-20260804 |
| 2026-08-09 | 纠正上一条：搜索框加错地方了，不是那个点了才弹出来的"本月明细"弹层，是首页(记账页)本身一直显示着的那份明细列表(`#listArea`)上方 | 已完成，把上一条加在`monthDetailSheet`里的搜索框整个撤掉(HTML输入框、`monthDetailSearchQuery`状态、`renderMonthDetailList()`里的过滤逻辑、`openMonthDetail()`里的重置、两个事件监听器全部移除，`monthDetailSheet`恢复成改动前的样子)，改到首页`.ledger-card`/`.chart-card`下面、`#listArea`上面新增同一个搜索框(`ledgerSearchInput`)，`.detail-search`这套CSS和`ic-search`图标都是复用的，没有重复定义。过滤逻辑挪到`renderLedger()`里：只影响下面这份列表本身(逐条过滤`entries`)，不影响上面月度收支汇总的数字(那是单独按当月`entries.forEach`累加算的，跟列表过滤是两回事，特意确认过不会被搜索词带偏)；搜索无结果时复用`emptyChart`文案，跟真的一条记录都没有的空状态(`emptyLine1`/`emptyLine2`)区分开。用Playwright验证：确认`#monthDetailSearch`已经不存在、新搜索框确实在`#listArea`前面；按备注/分类/支付方式搜索都只返回预期条数；搜索时月度收支汇总数字保持不变(按全部账目算，不受搜索影响)；无匹配显示空状态文案；清空按钮恢复全部列表，无JS报错 | accountbook-20260804 |
| 2026-08-09 | 出了4版搜索框样式原型(现状实线/胶囊圆角凹陷/虚线画风/收起成图标点击展开)截图给用户选，选定虚线画风，并反馈搜索框跟下面明细列表离得太远(比明细条目之间的间距明显大) | 已完成：`.detail-search`边框从`1px solid var(--grid)`+`background:var(--card)`实心卡片改成`1.5px dashed var(--grid)`+透明背景，跟`.cat-picker-row`等虚线元素统一画风；间距方面根因是两层叠加——`.detail-search`自己的`margin-bottom:14px`加上第一个`.day-header`自带的`padding-top:14px`，两个14px叠在一起变成近30px的空隙，比列表内部账目之间的`8px`(`.entry`的`margin-bottom`)大了将近4倍。修法：`.detail-search`的`margin-bottom`降到`8px`，另外加一条`#listArea .day-group:first-child .day-header{padding-top:0}`单独清掉第一个日期分组头顶部的padding(不影响列表中间其它日期分组之间原有的14px呼吸感，只处理"紧跟在搜索框后面"这一个位置)。用Playwright量了实际像素：搜索框底边到第一条日期分组顶边的间距、和列表内部两笔账目之间的间距，两个数字都精确等于8px，观感上完全统一节奏了 | accountbook-20260804 |
| 2026-08-09 | 反馈不要透明底色，跟上面环状图卡片的间隔可以再大一点 | 已完成：`.detail-search`背景从`transparent`改回`var(--card)`实心(虚线描边保留不变)；间距方面`.page.active`是`display:flex`(flex子项之间margin不会合并、是直接相加的)，所以给`.detail-search`加`margin-top:10px`会跟上面`.chart-card`自带的`margin-bottom:18px`直接相加变成28px，比原来单纯18px的卡片间距明显大一圈，但没有大到跟页面其它卡片间距(18~22px)脱节；下面到列表的`margin-bottom:8px`维持不变。Playwright量了像素确认：环状图卡片到搜索框28px，搜索框到列表明细8px，两头间距按需求方向拉开了 | accountbook-20260804 |
| 2026-08-09 | 要求给明细起个小标题"出入金"(左)+搜索按钮(右)，点击按钮才弹出输入框(样式不变)，先出一版看看 | 已完成：`#listArea`上方新增`.card-title`标题行(复用统计页趋势图卡片同款小标题样式)，左边`出入金`文案走i18n(`ledgerSectionTitle`，中日文都用同一个词，"出入金"本身就是日语记账常用词)；右边新增30px圆形虚线图标按钮`section-search-btn`(跟日/月圆形切换按钮同一套视觉语言)，默认`.detail-search`输入框整个`display:none`收起，点按钮展开(`display:flex`+自动聚焦)、按钮同时变成实心seal红高亮(跟`.dim-toggle-row`选中态同一个模式)；再点一次收起，收起的同时清空搜索词+恢复完整列表(不留着"收起了但筛选还生效"这种反直觉状态)。输入框内部搜索逻辑(备注/分类/支付方式模糊匹配)完全没动，只是外面套了层显示/隐藏。用Playwright验证：初始收起态、点击展开(聚焦+按钮高亮)、展开态搜索仍正常过滤、再次点击收起(清空+按钮取消高亮+列表恢复全部)，全流程无报错；截图确认收起/展开两种状态视觉效果都符合预期 | accountbook-20260804 |
| 2026-08-09 | 反馈两个问题：①不用在标题下面单独出一行输入框，直接原地从搜索按钮的位置展开成输入框，位于"出入金"右侧即可；②输入框弹出后软键盘展开会把搜索框顶到屏幕外面，得手动上拉才能看到，需要修正 | 已完成两处：①把原来"标题行+下面单独一个`.detail-search`行"的两行结构合并成一行——`.detail-search`本身就是标题行里紧跟在"出入金"后面的第二个flex子项，收起态是30px圆点(只显示放大镜图标，`border-radius:50%`)，点击后加`expanded`class让它原地展开成`width:100%`的横条输入框(`border-radius`同步过渡成12px圆角矩形)，宽度/圆角都走CSS transition，没有另起新行也没有整体布局跳动。搜索图标本身从独立的`.section-search-btn`按钮改成塞进`.detail-search`内部的`.search-icon-btn`，展开后图标缩小(30px→16px)但还在最左侧，点它依然是收起(逻辑不变，只是从"切换整行的display"改成"切换.expanded这个class")。②根因是这个App是Capacitor包出来的iOS原生壳、内部用的是WKWebView——WKWebView不会像Safari浏览器分页那样在输入框聚焦时自动把它滚动到软键盘上方，键盘弹出后如果输入框位置偏下就会直接被键盘盖住看不见(这个问题在①改完、搜索框上移到页面更靠上的位置后已经缓解不少，但WKWebView这个"不自动滚"的行为本身还在，偏下的场景比如小屏机型依然会犯)；修法：输入框`focus`事件里延迟320ms(等键盘弹出动画基本完成)后手动调一次`scrollIntoView({behavior:'smooth', block:'center'})`兜底，不管输入框原来在哪个位置，聚焦后都会自己滚到可见区域中间。用Playwright验证：收起态30px圆点、点击后精确展开到`width:100%`(测试viewport下=394px)、输入框正确聚焦、搜索过滤逻辑不受影响、猴子补丁`scrollIntoView`确认聚焦后320ms内确实被调用了一次、再次点击收起且清空搜索词恢复完整列表，全流程无报错 | accountbook-20260804 |
| 2026-08-09 | 真机截图反馈：搜索框展开后"出入金"标题的"出"字被顶到屏幕外面看不见了，整体布局很乱 | 已完成，根因是上一条改动引入的真实bug：`.detail-search.expanded{width:100%}`跟同一个flex行里"出入金"标签这个兄弟元素抢位置——`width:100%`是要吃满整个父容器的100%宽度，而不是"剩余空间"的100%，加上`.detail-search`基础规则还带着`flex-shrink:0`(不允许收缩)，两者叠加的结果是"标签宽度 + 搜索框100%父容器宽度"必然超出这一行的实际可用宽度，导致整行横向溢出；本地Playwright测试当时用的视口比较宽(430px)+没有专门检查横向溢出，没有暴露出来，真机(WKWebView，且窗口更窄)加上搜索框聚焦触发的`scrollIntoView`一起作用，就把溢出的这一行沿水平方向滚动过去、"出"字被滚出了可视区域左边。修法：展开态改成`flex:1`(在flex布局里表示"跟标签分完剩下的空间"，不是"占满整个容器")配合`min-width:0`(允许在需要时正常收缩，不会被内容撑开继续溢出)，`width:100%`和它导致的溢出问题一起去掉。这次额外补了个之前没做的检查：用Playwright在更窄的375px视口(iPhone SE同等宽度)+日语/夏花火深色皮肤+比较长的内容组合下，专门测了`document.documentElement.scrollWidth`是否超过`clientWidth`(整页有没有横向溢出)、标题行自己的`scrollWidth`/`clientWidth`、以及标题文字左边界是否还在可视区域内(`getBoundingClientRect().left>=0`)，收起/展开/输入文字/重新聚焦触发scrollIntoView这几个阶段全部确认零横向溢出，「出入金」四个字完整可见不再被顶飞 | accountbook-20260804 |
| 2026-08-09 | 要求点击页面其它位置时，展开的搜索框要自动收起变回搜索按钮 | 已完成：把收起逻辑从"只有再点一次搜索图标"抽成独立函数`collapseLedgerSearch()`(展开对应`expandLedgerSearch()`)，避免两个入口各写一遍以后改一处漏一处；新增一个挂在`document`上的全局click监听，展开状态下只要点击目标不在`#ledgerSearch`这个容器内部(`!$('ledgerSearch').contains(ev.target)`)就调用收起，覆盖点列表/图表卡片/标签页/底部tab栏等任何"外部"位置；容器内部的点击(输入框本身、清空的✕按钮、搜索图标)天然被`contains`判定为"内部"会跳过，不会误收起。收起时依然会清空搜索词、恢复完整列表，跟点搜索图标收起是完全一致的行为。检查过页面上其它几处已有的"点外面收起"逻辑(⋯菜单/月份选择器弹层)用的都是同一套"检查点击目标"模式、没有用`stopPropagation`，这次保持一致没有加，避免意外挡住那些监听收不到点击事件。用Playwright验证：点搜索框内部的清空按钮不会误收起(展开态保持)；点搜索框以外的中性区域(页头标题文字)会收起且清空搜索词、列表恢复全部；展开状态下点底部"统计"标签切换页面也会顺带收起搜索框，无报错 | accountbook-20260804 |
| 2026-08-09 | 要求把"点页面其它地方收起"改成"检测到输入法消失就收起" | 已完成：撤掉上一条加的document全局click监听，改成监听输入框自己的`blur`(失焦)事件——手机上点输入框以外的地方，第一下往往只是把输入法收起(输入框失焦)，不一定会同时让那个位置本身的点击生效，用document click判断"点了外面"这个思路在真机上容易出现"点一下没反应，要点第二下才收起"的体验问题；失焦这个信号更直接，不管是点了别的地方、点了输入法自带的"完成"、还是手动下滑收起输入法，只要输入框不再聚焦这件事本身就该触发收起，不用再判断具体点在了哪。为了不跟"清空"按钮(✕)打架——点它会让输入框先失焦一瞬间、按钮自己的click逻辑处理完后又马上把焦点还给输入框——加了个150ms可取消的延迟：失焦后先订个定时器，如果150ms内重新聚焦(比如点清空按钮触发的这种"焦点瞬间移开又移回")就把这次收起取消掉，避免出现"收起了又立刻弹回来"的抽搐感。用Playwright验证：点清空按钮后依然保持展开态且重新聚焦(定时器被正确取消)；直接调用`blur()`模拟输入法收起，60ms时还没到延迟、仍是展开态，约260ms后确认已收起且清空了搜索词、列表恢复全部；手动再点搜索图标收起(不走blur这条路径)依然是立即生效不用等延迟，无报错 | accountbook-20260804 |
| 2026-08-09 | 反馈搜索出结果时视窗会上下跳动，影响查看检索结果 | 已完成，根因有两层：①`.page.active`本来就有`justify-content:center`(内容比一屏矮时垂直居中，是给"空账本"/内容很少这种场景准备的)——搜索把结果过滤到只剩几条时，内容高度骤减到比一屏矮，就会突然被居中，每敲一个字符结果集变化，居中的位置跟着抖动；加了`#pageLedger.ledger-searching{justify-content:flex-start}`，搜索展开期间固定靠顶部对齐，不参与"矮了就居中"这套逻辑。②光改居中还不够，用Playwright实测发现哪怕改成flex-start依然有100+px的位置偏移——根因是过滤结果变少时列表容器变矮，页面总高度跟着变矮，如果当时页面已经往下滚了一截，浏览器会强制把滚动位置收紧到不超过新的(更矮的)可滚动范围，表现出来就是内容突然往下"弹"一截；修法是展开搜索的那一刻(还没输入、显示的是完整未过滤列表，是可能出现的最高高度)先量出`#listArea`当时的`scrollHeight`锁成它的`min-height`，之后不管过滤剩几条，列表容器高度都不会矮过这个基准，页面总高度全程保持不变，也就没有"滚动位置被收紧"这回事了；收起搜索时把这个`min-height`锁定去掉，恢复正常内容自适应高度，不会在平时(没有搜索)的账本页面留下多余空白。用Playwright验证：展开时`justify-content`确认是`flex-start`；从16条结果过滤到只剩1条，搜索框在视口里的位置像素级零漂移(改之前是100+px的跳动)；再从1条清空搜索词恢复回16条，位置同样零漂移；收起搜索后确认`min-height`清空、恢复居中逻辑不影响正常使用，无报错 | accountbook-20260804 |
| 2026-08-09 | 要求所有图表的图例统一限制视窗最多显示5条，多的靠拖拽进度条查看，图例其它逻辑(点击筛选等)不要动 | 已完成：用Playwright实测出两种图例容器各自的单行实际渲染高度——首页环状图/积分内訳环状图共用的`.legend-row`单行37px，集計页收支趋势图下面的`.cat-legend-row .item`单行23px(条目间还有10px间距)；据此给两个图例容器分别加`max-height`(前者185px=5×37，后者155px≈5×23+4×10)+`overflow-y:auto`+`-webkit-overflow-scrolling:touch`(跟已有的横向图表滚动`.chart-scroll`同款iOS触摸滚动优化)，超出5条的部分靠原生滚动条拖拽/触摸滑动查看，第5条之后干净截断不会露出半行。三处图例(首页环状图`#homeLegendList`、积分内訳`#pointsDonutLegendList`、收支趋势图`#trendCatLegend`)全部覆盖，只加了这一条CSS规则，没有动任何JS——图例的点击筛选(`clickable-legend`跳转月度明细)、颜色/百分比/文案渲染逻辑原封不动。用Playwright验证：8条图例的分类数据下，容器`clientHeight`精确对应5行、`scrollHeight`包含全部8行、确认可滚动(`scrollHeight>clientHeight`)；手动把`scrollTop`设到底部确认能滚动到看不见的后3条；点击一条图例(哪怕是要滚动才能看到的)依然能正确弹出月度明细弹层，证明点击逻辑完全没受影响；截图确认5行截断处干净利落，没有半行露出的违和感 | accountbook-20260804 |
| 2026-08-09 | 要求执行过一次搜索后(包括结果为空)，关闭输入法不应该把搜索框收起，改成点击其它位置才收起 | 已完成：`blur`(输入法消失)时是否自动收起，改成看输入框内容是否为空来分两种情况——还没输入任何东西就把键盘收起(大概率是打开了搜索又反悔)，跟之前一样直接收起；已经输入过内容(哪怕搜索结果是0条)后收起键盘，不再自动收起搜索框，很可能是想腾出屏幕空间看筛选结果，这时候自动收起会连搜索词一起清空，体验很差。判断依据是"输入框内容是否为空"而不是"搜索结果是否为空"，两者是两回事(明确要求过结果为空也算"已经搜索过")。这种情况下改成要点搜索框以外的地方(重新加回`document`全局click监听，跟更早之前撤掉的那版逻辑一样)才会真正关闭并清空搜索词。用Playwright验证四种场景：①空输入时关闭键盘→立即收起；②有输入且有结果时关闭键盘→保持展开、搜索词不变；③保持展开状态下点击页面其它位置(标题文字)→收起并清空；④有输入但结果为0条时关闭键盘→同样保持展开(验证了"结果为空也不自动收起"这一条)；另外确认清空按钮(✕)依然不会被误判成"关闭键盘"触发收起，无报错 | accountbook-20260804 |
| 2026-08-09 | 反馈图例的滚动条要显示在右侧让用户看得到，左右(横向)不需要滚动，固定住即可 | 已完成：三处图例容器(`#homeLegendList`/`#pointsDonutLegendList`/`.cat-legend-row`)显式加`overflow-x:hidden`锁死横向滚动(内容本来就不会横向溢出，这条是明确保证不会横向滚动)；竖向滚动条用`::-webkit-scrollbar`系列伪元素自定义成右侧常驻可见的4px细条(配`scrollbar-width:thin`+`scrollbar-color`兼容Firefox写法)，颜色用现成的`--ink-faint`token跟画风统一，不再依赖iOS默认的"只有滑动时才短暂闪现"的隐藏滚动条，用户一眼就能看出"这里还能往下滚"。用Playwright验证`overflow-x`确认是`hidden`、`overflow-y`确认是`auto`，无报错 | accountbook-20260804 |
| 2026-08-09 | 要求点击搜索结果区域不要关闭搜索框 | 已完成：`document`那个"点外面收起"的监听里，除了原来的`#ledgerSearch`(输入框/清空按钮)算"内部"，新增`#listArea`(搜索结果列表本身)也算"内部"，点结果列表里的任何东西(某一天的日期分组、某一条账目、点开编辑/复制/删除)都不会触发收起——用户点结果列表是在继续跟筛选出来的这些账目打交道，不该被顺带关掉搜索框丢了筛选状态。只有点列表和搜索框以外的地方(页头、图表卡片、标签栏等)才真正算"点别处了"，触发收起+清空。用Playwright验证：点结果列表里的日期分组头、点账目行本身，搜索框都保持展开、搜索词不变；点页头标题这种真正的"外部"位置，依然会正确收起并清空，无报错 | accountbook-20260804 |
| 2026-08-09 | 截图反馈：更新提示条点了"更新"后显示"更新中..."时，左边"›"收起按钮和右边卡片高度对不上，参差不齐 | 已完成，根因是左边`.update-collapse-handle`写死了`height:41px`，右边`.update-card`没写高度、跟着内容走——正常态(标题+说明文字+按钮)内容比较高，天然比41px高；点"更新"后内容换成单行"更新中..."文字，一下矮了不少，比41px还矮，两边就露出一截参差不齐的边缘。改成`.update-expand-wrap`的`align-items`从`flex-start`换成`stretch`，同时去掉`.update-collapse-handle`写死的`height:41px`，两边高度都跟着"两者之间较高的那个"自动拉伸对齐——不管右边卡片当前是"标题+说明"的正常态还是"更新中..."的单行态，左边按钮永远自动跟着一样高，不用针对某个特定状态单独写死一个数字。用Playwright注入了跟`showUpdateTab()`/`performUpdate()`实际生成的一模一样的DOM结构分别测了两种状态：正常态两边高度都是55.7px(完全一致)，"更新中"单行态两边都是35px(同样完全一致)，无缝隙无报错 | accountbook-20260804 |
| 2026-08-09 | 反馈滚动条要一直显示，目前滚动条位置遮盖了金额数字，再改一版 | 已完成，两个问题一起换了个更彻底的方案：①"一直显示"这件事光靠CSS做不到——iOS上`overflow:auto`的原生滚动指示条是系统级行为，松手就自动淡出，`::-webkit-scrollbar`这套自定义在iOS的触摸滚动容器上很不可靠，没法强制常驻。改成自己画一条进度条(`.legend-scroll-thumb`)：三个图例容器各自外面包一层`.legend-scroll-wrap`(`position:relative`)，进度条是绝对定位在这层里的3px细条，高度/位置由新增的`updateLegendScrollThumb(containerId, thumbId)`函数按`scrollTop/scrollHeight/clientHeight`实时算(超过5条能滚动才显示，否则直接隐藏)，每次图例重新渲染、以及用户实际滑动图例的`scroll`事件都会触发重算，不依赖任何系统原生滚动条行为，也就不存在"松手就消失"的问题。②遮盖金额的根因是滚动条紧贴在容器最右边、跟金额数字的`text-align:right`右对齐位置正好撞在一起；改法是三个图例容器都加了`padding-right:10px`，把内容整体往左让出10px空间，进度条摆在这段空白区域里，不再压在数字上面。原来的`::-webkit-scrollbar`系列规则和`scrollbar-width`/`scrollbar-color`一并删掉，不再需要。用Playwright验证：8条分类数据下首页图例进度条正确显示(高度116px，跟5/8比例吻合)、跟第一行金额文字的包围盒确认零重叠(进度条x393-396，金额文字止于x386，留了7px空隙)、手动滚到底进度条位置从0移动到69px(精确等于185-116的可滚动范围)；集計页趋势图图例同样验证滚动条可见，点柱子切到单日明细时图例和进度条一起正确隐藏；积分页因为没有积分数据只有空状态，进度条也正确保持隐藏，无报错 | accountbook-20260804 |
| 2026-08-09 | 要求百分比和金额再往左移一点，给滚动条留更多位置 | 已完成：三处图例容器的`padding-right`从10px加到18px，内容(百分比/金额数字)整体往左让出更多空间，进度条本身位置不变(还是贴着容器最右边`right:0`)，相当于加宽了两者之间的空白间隙。用Playwright量了金额/百分比文字右边界到进度条左边界的实际距离，从原来7px的间隙拉大到15px，视觉上更宽松、不会显得局促 | accountbook-20260804 |
| 2026-08-09 | 纠正更新提示条的高度修法：不是要两边动态互相拉伸对齐，是"更新中"单行状态时两边固定一致、正常态(标题+说明两行)两边高度不一样没关系 | 已完成：撤掉上一版`align-items:stretch`那套"跟着较高一侧动态拉伸"的方案，改回`align-items:flex-start`，`.update-collapse-handle`重新写死高度，但这次写死的目标值不是原来的41px(那是按正常两行态估的)，而是精确量出"更新中..."单行态(带真实的旋转刷新图标，不是随便找个字符占位)的实际高度——40px。这样点"更新"之后进入的单行态两边严丝合缝，没点更新前的正常两行态两边高度对不上是预期效果、不用处理。用Playwright注入了完整还原`performUpdate()`真实DOM结构(含真实`<svg class="hd-icon"><use href="#ic-refresh"/></svg>`旋转图标，不是占位字符)的测试：单行"更新中"态两边都精确是40px(delta=0)，正常两行态卡片55.7px、按钮固定40px(符合预期，不要求一致) | accountbook-20260804 |
| 2026-08-09 | 要求设置页加个版本号显示，非主分支推送时要告诉版本号，定一套规则：正式版沿用MAJOR.MINOR.PATCH(当前1.0.0)只在合并主分支正式发布时改；测试分支(accountbook-20260804)每次推送带递增开发号，格式`1.0.0-dev.N`，方案经用户确认后实装 | 已完成：新增两个JS常量`APP_VERSION`(手动跟仓库根目录`VERSION`文件保持一致)和`DEV_BUILD`(测试分支迭代计数，这次从1开始、以后每推送一次测试分支就手动+1)，拼成`v{APP_VERSION}-dev.{DEV_BUILD}`显示在设置页底部新增的`#devBuildVersionRow`——这一行不区分网页/PWA/原生App，只要打开设置就能看到，跟原来只在原生App里才显示的`#nativeAppVersionRow`(CI打包ipa时才写入真实ipa版本号)是两码事、互不影响，分别解决"我是不是原生App"和"我现在看的是第几次网页测试改动"这两个不同的问题。用Playwright验证：非原生(网页)环境下`devBuildVersionRow`正确显示`v1.0.0-dev.1`且可见，`nativeAppVersionRow`按预期保持隐藏，无报错。之后每次推送测试分支，会在推送后的消息里告知这次对应的`dev.N`是多少 | accountbook-20260804 |
| 2026-08-10 | 上传真机录屏反馈："上滑时会动不动闪跳回第一条" | 已完成，用ffmpeg把录屏拆成逐帧截图定位到问题现场：帧序列显示首页环状图下面的图例(`#homeLegendList`)被用户手指拖拽滚动到第3~7条，其中一条(通信费)还带着`:active`按压高亮说明手指正压在图例上，但紧接着下一帧图例整个弹回第1~5条、高亮也消失——不是原生滚动的"回弹"效果(那种只在拖到边界外才会发生，正常拖动中途松手不会自己弹回去)，说明是JS强行把`scrollTop`重置了。查代码发现根因：`renderHomeDonut()`/`renderPointsDonut()`/`renderTrendChart()`这三处图例渲染函数，每次重画都无条件执行`legendEl.scrollTop=0`；而这三个函数不止用户点了月份切换/维度下拉/支出收入tab这类主动操作时会调用，还会被完全跟用户操作无关的后台刷新连带触发——比如App启动时后台补历史汇率(`ensureRatesForEntries()`，网络请求，结果什么时候回来不确定)、云端Realtime订阅收到变动、切回前台时的`resyncCloudOnResume()`，这几个都会走到`renderAll()`→连带调用这三个渲染函数。只要用户正巧在图例上滑动的这几百毫秒内，后台请求前后脚返回触发了一次重渲染，图例就会被强制打回顶部，跟用户手上的拖动动作完全对不上，看起来就是"莫名其妙跳回第一条"。修法：新增公共函数`setLegendHtml(legendEl, html, resetScroll)`统一接管三处图例的"换内容"逻辑，换内容前先记下当前`scrollTop`，换完后默认(`resetScroll`为假)按新内容的最大可滚动范围夹取后恢复原位置，不再无脑清零；只有明确是"用户主动切换了要看哪份数据"这几个入口——月份前进/后退、支出↔收入tab切换、标签维度下拉框、`goToHomeDonut`跳转——才显式传`resetScroll=true`让它真正回到顶部(这几个场景本来就是换了一批全新数据，回顶符合直觉)。顺带把首页环状图卡片上专门用来"横滑切月"的`touchstart`/`touchend`监听也加固了一下：原来这个监听是绑在整张卡片(donut+图例都在里面)上、只看横向位移`dx`，完全没排除图例区域，理论上在图例里上下滑动时手指的水平抖动也可能被误判成">40px横滑"进而意外切月；改成`touchstart`时如果落点在`.legend-scroll-wrap`(图例)范围内直接不记录起点，且判定横滑额外要求`|dx|>|dy|`(确实以横向为主的手势才算)，避免图例的竖直滚动手势被误当成横滑切月处理。验证：①抽离`setLegendHtml`函数用真实DOM单测——后台刷新场景(`resetScroll`不传)滚动位置从50px重渲染后依然是50px(没被清零)，用户主动切换场景(`resetScroll=true`)确认清零，列表变短的场景确认新位置被正确夹取在有效范围内不会溢出；②起本地server用Playwright跑完整App：图例有7条数据(可滚动)，手动滚动到40px后，点击支出↔收入tab切换(这是`resetScroll=true`的场景)图例正确回到顶部，再滚动40px点回原tab同样正确回到顶部，全程无报错(仅有的报错是沙盒环境没有外网导致的汇率/节假日接口请求失败，跟这次改动无关)；DEV_BUILD从1提到2 | accountbook-20260804 |
