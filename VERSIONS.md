# 版本记录

记录记账本的正式版本节点。每个版本对应一个同名快照分支（不会被删除/覆盖），可用于回滚：

```
git checkout v1.0.0 -- index.html
```

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0.0 | 2026-07-31 | 首个正式版本。手绘彩铅风格 UI + 自定义 SVG 图标；中日双语；GitHub Gist 云备份；修复存储型 XSS；统计页改名「集計」并修复空白布局；新建/编辑/复制/设置弹窗统一加关闭按钮；删除加二次确认；桌面图标 v35（放大猪+"家計簿"字样，纯黑背景）。 |

## 命名规则

- 正式版本：`vMAJOR.MINOR.PATCH`（语义化版本），对应同名分支，例如 `v1.0.0`。
- 日常快照（非正式版本节点）：`accountbook-YYYYMMDD`，同一天多次则加 `-2`、`-3`……
