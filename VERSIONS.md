# 版本记录

记录记账本的版本节点，分两张表：**正式版本**是发布给用户的版本号（`vX.Y.Z`）；**开发版本**是开发分支上每次推送的迭代号（`{DEV_BUILD_DATE}-dev.{DEV_BUILD}`），还没发布、只在测试分支上跑，最终会归到某一个正式版本里。

## 正式版本

版本号只是发布时写进`VERSION`/`index.html`里`APP_VERSION`常量/这份文档的一个记录值，不额外拉分支——需要回滚某个版本时，直接在主分支的提交历史里找到对应的`release: vX.Y.Z`提交回滚：

```
git log --oneline --grep="release: v"
git checkout <对应提交> -- index.html
```

（`v1.0.0`~`v2.0.4`这几个是旧规则下留的快照分支，之后不会再新增同类分支。）

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0.0 | 2026-07-31 | 首个正式版本。手绘彩铅风格 UI + 自定义 SVG 图标；中日双语；GitHub Gist 云备份；修复存储型 XSS；统计页改名「集計」并修复空白布局；新建/编辑/复制/设置弹窗统一加关闭按钮；删除加二次确认；桌面图标 v35（放大猪+"家計簿"字样，纯黑背景）。 |
| v1.0.1 | 2026-08-10 | 云同步大改：接入 Supabase 邮箱密码认证+多端 Realtime 实时同步，取代原来的 GitHub Gist 手动备份；分类/细分/支付方式/标签全部改成 id+code 规范化存储；新增记账标签(tags)功能，首页环状图/本月明细支持按标签维度筛选统计。功能新增：明细区加模糊搜索框(备注/分类/支付方式)；支付方式换上 Amazon/楽天/メルペイ/ペイディ/Suica 等真实品牌图标；集計页新增区间累计结余卡片、日别/月别对比合并卡片，趋势图分类图例支持点击查看明细；App 内更新检测改成整段源码比对+可折叠边缘提示条；设置页新增版本号显示。原生 App（iOS，AltStore 侧载）改名 PigBang，换用储蓄罐线稿图标。修了一批体验问题：图例统一限高 5 行+自画常驻滚动条、图例后台重渲染导致的"闪跳回顶部"、图例按压高亮在深色皮肤下糊住文字、搜索框横向溢出/键盘顶飞/视窗跳动、更新提示条高度对不齐等。 |
| v1.1.0 | 2026-08-10 | 修复图例自画滚动条在iOS真机上跟系统原生滚动指示条叠加出现"重影"的问题（补上`::-webkit-scrollbar{display:none}`彻底关闭原生渲染）。设置页版本号显示逻辑新增`IS_DEV_BUILD`标记：正式发布版本只显示干净的`v{APP_VERSION}`，不再跟测试分支迭代版一样带`-dev.N`后缀，两者一眼区分开。 |
| v2.0.0 | 2026-08-13 | 新增闹钟提醒功能：跟"闹钟""主题""设置"平级的独立入口，支持增删改和列表，仅一次/周期两种重复模式（周期模式下每天/平日/周末预设+自定义星期+"祝日不响"，复用现成的日本法定节假日基础设施），铃声上传（Supabase Storage跨设备同步，原生端本机缓存离线可用；超过2分钟不直接拒绝，改成弹裁剪页面选一段2分钟以内区间再上传）+震动开关；删除闹钟前二次确认。持续响铃走后台音频保活方案（自研Capacitor原生插件`capacitor-alarm-ringer`，声明`UIBackgroundModes:audio`+`AVAudioPlayer`后台循环播放防止进程被系统挂起，到点切换成真实铃声/震动，不需要付费Apple开发者账号），本地通知作为兜底。首个包含原生自定义插件的版本，版本号跳到2.0.0。 |
| v2.0.1 | 2026-08-13 | 修复v2.0.0真机实测暴露的问题：①选铃声选不了——去掉`<input accept="audio/*">`这个在WKWebView里不可靠的前置过滤，交给已有的后置时长校验兜底；②响铃不够可靠——App被系统/用户杀掉导致后台音频保活失效时，兜底本地通知从原来单条一次性提示音改成密集连续通知（隔5秒一条，连续约1分钟），静音模式下震动依然生效，并加了"停止"按钮可以不点开App直接撤掉整批通知；③新建/编辑闹钟的时间选择器换成真正的iOS风格滚轮选择器（纯CSS scroll-snap实现），替换掉一直没换上、在WKWebView里样式和操作都别扭的系统`<input type="time">`。 |
| v2.0.2 | 2026-08-13 | 继续修复v2.0.1真机反馈：①时间滚轮选中态改成按离中心距离连续算缩放/透明度，不再是二值硬切换，数字更大、滑动过渡更顺滑；②闹钟列表加左滑删除手势（仿邮件App），不用非得进编辑页才能删；③裁剪铃声弹层从光秃秃的原生滑块重新设计成时间轴卡片+高亮选中区间，视觉跟App其它页面统一；④修复铃声上传偶发失败——上传时补上显式`contentType`，不再依赖浏览器自己猜文件类型（部分设备猜出来的类型是空值/不对，被Storage桶白名单直接拒收）。 |
| v2.0.3 | 2026-08-14 | 继续修复真机反馈：①裁剪铃声弹层重新设计成参考iOS原生视频裁剪的交互——裁剪头/尾是两个独立可拖拽手柄只管裁哪一段，播放头单独一条竖线只管预览进度，播放到裁剪尾无缝循环回裁剪头，拖裁剪头/尾甩出当前播放位置会自动接回并播放；②时间滚轮补上纵向锁定，不会再被误判成横向滑动；③铃声上传失败提示改成显示Supabase返回的原始报错文本，不再只是笼统的"上传失败"，方便诊断（顺带排查确认是Storage桶/权限策略之前没建出来导致的，已修）；④闹钟新建"+"按钮从弹层头部右上角改成页面居中，跟随列表内容走——列表为空时显示在提示文字下方，有数据时显示在最后一条闹钟下方。 |
| v2.0.4 | 2026-08-14 | 继续修复真机反馈：①铃声上传体积超过Storage桶10MB上限导致的失败——原来裁剪只限制时长不限制体积，未压缩wav在立体声+高采样率下2分钟能到20MB+；改成统一降到单声道+22050Hz(电话音质水准，铃声场景够用)，120秒封顶输出约5MB，直接上传(不经过裁剪)的短音频也统一走这套标准化流程，不再有两条体积表现不一致的路径；②裁剪页播放头拖动(快进/快退)不再自动暂停播放，改成跳到新位置后自动开始播放(暂停状态下)或自然接着播放(播放中)，两个方向共用同一套逻辑。 |
| v2.1.0 | 2026-08-15 | 三个新功能：①闹钟铃声从"每次上传只属于当前这条闹钟"改成可复用的"我的铃声"资源池——新建`alarm_sounds`表记录每个上传对象的显示名字，铃声选择改成独立列表页(系统默认+我的铃声逐条列出，支持试听/改名/删除)，上传成功后弹窗要求命名(不填落"未命名铃声"默认名，之后可改)；②闹钟时间滚轮划过整格时加iOS原生风格的"哒"声反馈(Web Audio API现场合成，不打包音频文件)；③记账表单新增支付方式还元率(积分)自动计算——メルペイ/楽天ペイ默认还元率1%，选中后按金额自动算出获得积分并可手动改，切换支付方式/改金额/改日期都会跟着重新计算，新增`payment_method_point_rules`表支持"メルペイ每月8日还元率变9%"这类特殊日期规则(存数据库而非写死代码，运营规则变动不需要重新发版)。 |
| v2.1.1 | 2026-08-16 | 花火皮肤视觉+交互修复批次(汇总自accountbook-20260816分支dev.1~dev.9)：①闹钟图标(ic-alarm-fw)找回真实定版路径、补齐缺失的填充渐变、外轮廓补描边+放大、"Zzz"从重叠改成分开摆位再整体换成参考图那种粗体块状"Z"；②底部标签栏补上液态玻璃气泡指示器(之前漏做)，并修复气泡动画的跳变(读实时渲染位置替代内联样式)和缓动曲线(标准ease，避免长尾"凝滯感"或前段"瞬移感"两个极端)；③闹钟列表：删除键改成跟iOS原生一致的独立圆形/药丸、左侧留白修复、时间数字自适应宽度不再被挤、新建/编辑标题文案修正；④铃声选择页去掉播放按钮填充色和选中星标装饰，保存按钮文字改成纯"保存"；⑤新建`design-assets/`仓库内设计资产归档目录(final/history两级结构)和`CLAUDE.md`长期规则文件(每次会话自动加载)。 |
| v2.1.2 | 2026-08-16 | 闹钟功能暂时封存：真机反馈"响铃没真正生效、只有点开通知才震动"，定位到原生插件两个真bug并修复(自定义铃声加载失败会连累内置蜂鸣音兜底一起哑掉；点击兜底通知本体原来靠15秒轮询窄窗口"看运气"才触发真响铃，改成直接用通知携带的`alarmId`+目标时间戳精确匹配)。修复之后重新评估：免费Apple ID侧载没有AlarmKit/Push/Critical Alert这些系统级权限，现有"后台音频保活+本地通知兜底"方案终究模拟不出系统闹钟那种真正可靠的持续响铃，决定暂时封存这个功能——新增`ALARM_FEATURE_ENABLED`开关常量，头部三个闹钟入口按钮隐藏、原生调度和响铃状态监听整体停用，闹钟相关代码本身原样保留不删，以后方案成熟(或者AlarmKit权限门槛有变化)可以直接把开关打开恢复。 |

## 开发版本

开发分支(`accountbook-YYYYMMDD`)上每推送一次改动，设置页显示的测试版本号`{DEV_BUILD_DATE}-dev.{DEV_BUILD}`就跟着变一次，这张表记录每次迭代对应的分支和改动内容。这个版本号格式是 2026-08-16 才引入的，之前的开发分支迭代没有留这张表的记录，只能查 `DEVLOG.md`。

| 版本号 | 日期 | 说明 | 分支 |
|---|---|---|---|
| 20260816-dev.1 | 2026-08-16 | 闹钟功能补齐花火主题视觉(图标/列表/铃声选择/表单页)；底部导航改成悬浮毛玻璃胶囊样式；闹钟重复设置去掉页签切换改成"不重复"预设选项；闹钟保存按钮统一红色、开关改绿色；闹钟列表滑动删除改成"滑到底"手势；合入主分支v2.1.0发布内容+版本号显示规则修正(测试版本号从借用未定版发布号改成独立的日期格式) | accountbook-20260816 |
| 20260816-dev.2 | 2026-08-16 | 找回花火皮肤闹钟图标(ic-alarm-fw)的原始定版路径数据并接入，补上钟身/表盘之间缺失的填充渐变(fw-icon-grad-soft)；design-assets/资产管理规则新增(设计产出物不许覆盖、必须同步存进仓库) | accountbook-20260816 |
| 20260816-dev.3 | 2026-08-16 | 闹钟图标外轮廓补描边(跟其他花火图标同款处理)+单独放大图标框到33px，解决轮廓不够明显、图标比头部另外两个按钮小的问题；design-assets/icons改成final/history两级归档；新建CLAUDE.md固化长期规则(推送前确认/设计产出物存储/汇报格式/发版流程)，每次会话自动加载 | accountbook-20260816 |
| 20260816-dev.4 | 2026-08-16 | 补上底部标签栏漏掉的液态玻璃气泡指示器——之前落地"底部导航改毛玻璃胶囊"时只做了图标/文字变色，没做气泡元素本体，跟已确认的原型效果不一致 | accountbook-20260816 |
| 20260816-dev.5 | 2026-08-16 | 闹钟列表三处问题：删除键改成跟iOS原生一致的独立圆形/药丸(不再是贴满整行高度的直角矩形)；新建/编辑标题文案改成"アラームを追加"/"アラームを編集"；.alarm-time改成按内容自适应宽度，修复时间数字被挤 | accountbook-20260816 |
| 20260816-dev.6 | 2026-08-16 | .alarm-row左侧留白修复(12px 2px→12px 14px)；ic-alarm-fw的"Zzz"两轮迭代——先重新摆位解决三个字重叠，再按参考图整体换成粗体块状"Z" | accountbook-20260816 |
| 20260816-dev.7 | 2026-08-16 | 修复标签栏气泡快速切换时的跳变——动画起点原来读的是内联样式(上一次点击已写死的终点值)，改成读气泡实时渲染位置(getBoundingClientRect)，并在新动画开始前显式cancel上一个还没播完的动画 | accountbook-20260816 |
| 20260816-dev.8 | 2026-08-16 | 气泡缓动曲线换掉长尾曲线(原曲线75%时间点已完成96%位移，剩余25%时长只爬不到4%，读起来像卡住)；铃声选择页播放按钮去掉填充色、选中行去掉星标、保存按钮文字改成纯"保存" | accountbook-20260816 |
| 20260816-dev.9 | 2026-08-16 | 气泡缓动曲线再调——上一版矫枉过正变成瞬移感，换成标准CSS ease，位移在时间窗口内均匀推进 | accountbook-20260816 |
| 20260816-dev.10 | 2026-08-16 | 修两个真机闹钟响铃原生bug(自定义铃声失败连累内置蜂鸣音兜底一起哑掉、点通知因15秒轮询窄窗口而"看运气"生效)；讨论后决定后台音频保活方案模拟不出真正可靠的持续响铃，加`ALARM_FEATURE_ENABLED`开关整体暂时封存——三个头部入口隐藏、原生调度/监听整体停用，代码保留 | accountbook-20260816(合并自accountbook-20260816-2) |

## Washi Ledger重写

独立版本号体系，跟上面`accountbook-YYYYMMDD`那条线（`v1.0.0`~`v2.1.2`）是两回事，但**沿用同一套"开发分支不用干净语义化版本号"的命名规则**（见下面「命名规则」一节）：`washi-ledger-rewrite`分支还没合并/正式发布过，现在处于开发阶段，版本号格式是`{DEV_BUILD_DATE}-dev.{DEV_BUILD}`，记在`washi-ledger/package.json`的`version`字段里(用semver的prerelease写法`0.0.0-{DEV_BUILD_DATE}-dev.{DEV_BUILD}`承载，保证仍是合法semver)。等这条分支真正合并/发布，才会跳到干净的`vMAJOR.MINOR.PATCH`并记进「正式版本」表。详细开发过程见`DEVLOG.md`里`washi-ledger-rewrite`分支那几行，架构背景/设计决策见`HANDOFF-washi-ledger-rewrite.md`。

（曾经在2026-08-18当天短暂把这个字段写成过`0.1.0`这种"干净"格式，是记错了规则——开发分支不该用这种格式，已改回`dev.N`格式。）

### 正式版本

还没有——这条线目前还没合并/正式发布过，等真正合并时再在这里加第一行记录。

### 开发版本

| 版本号 | 日期 | 说明 | 分支 |
|---|---|---|---|
| 20260818-dev.1 | 2026-08-18 | 十个正式页面全部落地：仪表盘、明细、统计(收支/积分双栏+趋势柱状图)、记一笔(两级分类+支付方式+标签+积分自动计算)、汇率换算(多货币)、设置、我的账户、登录、注册、关于；另加分类明细钻取、确认删除弹窗、自定义细分/标签内联新增改名删除、web端CI(`washi-ledger-ci.yml`)、iOS测试包打包workflow(`washi-ledger-ios-test-build.yml`)。技术栈Vite+React+TypeScript+Tailwind CSS v4，数据库复用旧App同一个Supabase项目(不迁移数据)。已知简化：趋势图纵轴刻度按固定范围算、不跟随横向滚动视口实时重算；汇率只接单一数据源(frankfurter.dev)，没做旧App那套多源兜底；跨月份自定义区间选择器简化成单月/单年导航；原生打包还没有真实App图标，用Capacitor默认占位图标 | washi-ledger-rewrite |
| 20260818-dev.2 | 2026-08-18 | 定稿App图标：从Stitch项目拉取资产"Washi_Ledger_Final_App_Icon"（M-pig剪影复古红渐变+米色纸质背景，1024×1024），存档进`design-assets/icons/washi-ledger-app-icon/final/`，源文件放`washi-ledger/assets/icon.jpg`；`washi-ledger-ios-test-build.yml`新增`capacitor-assets generate`步骤，CI里现生成iOS全套尺寸图标，不再用Capacitor默认占位图标 | washi-ledger-rewrite |
| 20260818-dev.3 | 2026-08-18 | 修复真机装上打开白屏——`supabase.ts`模块加载时读不到`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`就直接throw，而这两个CI workflow(`washi-ledger-ci.yml`/`washi-ledger-ios-test-build.yml`)构建前都没有补上这两个环境变量(`.env`按规矩不进仓库)，导致CI出的`dist`里读到的是undefined，整个React在挂载前就崩溃，只剩原生层背景色。两个workflow各加一步`cp .env.example .env`(该文件本来就存了这个Supabase项目的真实URL+anon key，公开的客户端key，靠RLS兜底) | washi-ledger-rewrite |
| 20260818-dev.4 | 2026-08-18 | 修复真机上顶部区域点不到(汉堡菜单/个人中心入口)——`capacitor.config.json`把iOS的`contentInset`设成`"never"`，webview不会自动让出状态栏/灵动岛区域，而全项目没有任何地方用`env(safe-area-inset-top)`让出空间，导致页面顶部内容被压在状态栏底下(视觉上像有间距，实际是被压住点不到，只有真机会复现，浏览器/模拟器预览看不出来)。`index.css`给`body`加`padding-top: env(safe-area-inset-top)`(对sticky/普通流内容生效)；`NavDrawer`的`<aside>`是`fixed`定位不受body padding影响，单独加了同样的padding。另外`AppLayout`头部右侧一直是个空占位`div`，没有个人中心/登录注册入口(此前只能靠抽屉导航进，抽屉又点不开，等于完全进不去)，补上了跳转`/account`的人像图标按钮 | washi-ledger-rewrite |
| 20260818-dev.5 | 2026-08-18 | 上一版(dev.4)真机验证过白屏/安全区问题后，连续几轮反馈攒在一起改、没有逐次单独推送验证，统一收在这一版里：①记一笔分类选择器改成旧App真实的4列grid左对齐+图标常态按分类色着色(不是只有选中才上色)，新增`lib/color.ts`；仪表盘/明细`EntryCard`图标配色同步从"收入绿/支出红"改成按分类色。②`index.html`viewport meta补上`maximum-scale=1`，修复真机登录后input聚焦触发WKWebView自动放大、且SPA路由切换不会重置、导致整个App变成能到处拖动缩放的问题。③新增`ScrollLegendList`组件(照旧App`updateLegendScrollThumb()`)，仪表盘/统计页donut图例、趋势图图例超过5行出现内部滚动+常驻细滚动条；`EntryCard`/`BalanceCard`/`HistoryEntryList`收支金额颜色统一改成跟标签一致的红(支出)/绿(收入)，不再是纯黑。④记账数据缓存/同步整个重做——新增`lib/entriesCache.ts`+重写`useEntries.ts`，照旧App`handleCloudSignedIn()`/`cloudSignOutBtn`真实逻辑：退出登录不清空本地缓存、登录时云端有数据就覆盖本地否则把本地缓存推上去当种子数据、App回前台补一次同步；我的账户页去掉加入天数/记录数、邮箱改小改淡、退出登录按钮改成居中小胶囊+接确认弹窗(`ConfirmDialog`新增`icon`/`tone`参数)；底部导航补点击特效(`active:opacity-60`)并把居中方式从`left-1/2 -translate-x-1/2`(半像素边界，图标发糊的一个可查成因)改成`inset-x-0 mx-auto`；左侧抽屉加顶部标题栏(真实数据：App图标+登录邮箱，不是设计稿里虚构的"Artisan User"人设)；设置页语言/货币从按钮组改成原生`<select>`，货币选项从2个扩到`data/rate.ts`的CURRENCIES完整11种；主题项改成跳转`/theme`子页面(照Bento卡片布局，但只有真实存在的"Washi Ledger"一张主题卡，设计稿里另外三套配色代码没有实现，没有编造) | washi-ledger-rewrite |
| 20260818-dev.6 | 2026-08-18 | 修复dev.5真机测试反馈的新问题——"页面可以滚动，一旦滚动变成白屏"。根因是`body`没有显式`height`(只靠内容撑开)、也没有`html`层级的背景色兜底，真机WKWebView做rubber-band回弹时会露出`html`/`body`未着色的原生画布(默认白色)；再叠加上一版把`padding-top: env(safe-area-inset-top)`直接加在`body`上，导致body必然比一屏高、每个页面不管内容多少都能触发这个回弹。照旧仓库index.html`html,body{height:100%}`的真实写法改：`html`/`body`都钉死`height:100%`+背景色，`body`加`overscroll-behavior-y:none`防回弹；`safe-area-inset-top`的让空处理从`body`挪到各页面自己的根容器(`AppLayout`及`AddTransactionPage`/`SignInPage`/`SignUpPage`/`ThemePage`这几个不套`AppLayout`的独立页面)，不再整体撑高`body` | washi-ledger-rewrite |
| 20260818-dev.7 | 2026-08-18 | 用户明确"页面是要固定的，不应该上下左右滚动"——之前几版一直在给body/html的滚动+回弹打补丁(背景色兜底、锁缩放、height:100%)，这版改成更根本的架构：外壳整体`position:fixed;inset:0`钉死在视口上，不再靠body/html的文档流滚动，真正可滚动的只有页面内部的`<main>`(`overflow-y-auto`+`overscroll-y-contain`)。`AppLayout`及`AddTransactionPage`/`SignInPage`/`SignUpPage`/`ThemePage`这几个页面根容器都改成`fixed inset-0`，header从`sticky`改成普通流内的`shrink-0`(不需要sticky了，外壳本身已经固定)；`index.css`里`html`/`body`/`#root`统一钉死`height:100%`+`overflow:hidden`，body从根上就不可滚动，不再需要靠背景色兜底猜"回弹露出的颜色对不对" | washi-ledger-rewrite |
| 20260818-dev.8 | 2026-08-18 | 修复dev.7真机反馈"闪现一下数据之后还是白屏，分类之类的突然闪现英文了，还全都一个颜色"，定位到两个真实bug：①`ScrollLegendList`用`useState`存滚动条位置，每次重算都new一个新对象传`setState`(哪怕数值没变)，而算滚动条这段代码又被一个没有依赖数组、每次渲染后都执行的`useLayoutEffect`包着——图例条目一旦超过5行(触发滚动条分支)就会陷入"渲染→算滚动条→setState→触发渲染"的死循环，React很快报`Maximum update depth exceeded`直接崩溃卸载整棵树，这是真机上"数据闪一下又白屏"的真实根因，改成用ref直接写DOM style、不经过React state，从根上消掉这个循环。②`useEntries`改成缓存优先后，entries能在`useCatalog`(纯网络请求、没有缓存)返回之前就先渲染出来，这段时间窗口里分类名/颜色/图标全部找不到对应的catalog数据、会兜底成"英文图标名(Material Symbols的图标名本来就是英文单词，比如`category`)+统一灰色"这种半成品画面——`DashboardPage`/`HistoryPage`/`StatsPage`/`AddTransactionPage`这四个用到entries+catalog的页面统一加了`if(!catalog)return null`，等catalog真正到位再渲染正文 | washi-ledger-rewrite |
| 20260818-dev.9 | 2026-08-18 | 用户提出12项问题大修，拆三版推，这是第一版(#1/#3/#10/#12/#11/#5/#2)：①修复底部导航切页先白一下才出数据——之前每个页面各自独立调`fetchCatalog()`，改成`CatalogProvider`(新`hooks/useCatalog.tsx`)在App根节点只拉一次、Context全局共享，导航不再重新拉取。②中日文i18n拆成独立模块：新增`lib/i18n/legacy.ts`原样搬旧App`I18N.zh`/`I18N.ja`完整词典(按旧key名存档，供后续功能复用查阅)，`lib/i18n/dict.ts`是新App实际用的词典(按新App现有key名，逐条从legacy词典取值，没有对应旧概念的先保留新App原文案，注释标明来源)，`lib/i18n.tsx`拆成`lib/i18n/index.tsx`只留Context逻辑；应用两处文案修正：明细→"出入金"，仪表盘中文"概览"/日文"家計簿"(旧App`tabLedger`)。③仪表盘顶部时间范围选择条(`DateRangeBar`)顶到header——外层加`mt-md`。④补齐横向拖动锁定：`AppLayout`/`AddTransactionPage`/`ThemePage`的`<main>`、`SignInPage`/`SignUpPage`的外层容器、`NavDrawer`的`<aside>`、`CategoryDetailSheet`的滚动区，统一补上`overflow-x-hidden`，只保留竖向滚动。⑤我的账户页头部左上角改成小房子图标(点击回仪表盘)，不再显示汉堡菜单——`AppLayout`新增`leftButton`prop(`'menu'`\|`'home'`)。⑥底部导航栏三个tab改成`flex-1`等宽分配，消除中日文文案宽度不同导致的间距不均。⑦Google Fonts导入的字重列表改回旧App原始配置(Noto Serif SC 500/600/700，Noto Sans SC 400/500/600，之前多带了个700/400) | washi-ledger-rewrite |
| 20260818-dev.10 | 2026-08-18 | 真机测过dev.9后又提了6个新问题，逐一定位真实根因修复：①"进App必定白屏一段时间"——`DashboardPage`/`HistoryPage`/`StatsPage`/`AddTransactionPage`之前用`if(!catalog)return null`整页提前返回，连header/底部导航壳子都出不来；改成只在数据区域挡一个轻量loading占位，外壳立刻可见。②"默认没有设置在日文"——查到旧App真实默认值是`{lang:'ja',currency:'JPY'}`(不是想当然的中文/人民币)，新App`DEFAULT_SETTINGS`一直定的是`zh`/`CNY`，改回来对齐。③"语言设置不生效、切页会变"——根因是`I18nProvider`的`lang`state和`useSettings`完全脱节，且旧的`useSettings(userId)`是每次挂载都从头开始的普通hook、不是全局共享状态，未登录时改语言连页面内存都留不住；重做成`hooks/useSettings.tsx`的`SettingsProvider`(照`useEntries.ts`同一套"本地缓存优先+登录同步"模式，新增`lib/localSettings.ts`本地持久化，key仿旧App`ledger_settings`)，`I18nProvider`内部改成消费这个Context，`setLang`本质是`update({lang})`，语言真正落地持久化。④"中日文混乱翻译"——排查到是大量页面文案压根没走`t()`、是直接写死的中文字面量(此前默认语言一直是zh，从没被暴露过)，切到日语只有小部分文案跟着变，看起来"混乱"；全量扫了一遍`src/features`+`src/design-system`把写死文案换成`t()`，`dict.ts`新增64个词条(优先复用`legacy.ts`里旧App的真实翻译，没有对应旧概念的才新写，逐条注释标来源)。⑤明细金额没跟着货币设置换算——新增`data/currencyDisplay.ts`(照旧App`toBase()`/`convForDisplay()`/`fmtCur()`真实逻辑，但汇率来源改用`RatePage.tsx`已有的11种货币frankfurter快照、不做旧App那套历史每日汇率缓存，这个简化跟`RatePage.tsx`本身的已知简化程度对齐)+`hooks/useDisplayRates.ts`，`toDisplayEntries()`在"先换算每条记录、再求和"这一层统一处理，`summary.ts`的聚合函数完全不用改；`EntryCard`/`BalanceCard`/`CategoryDonutCard`/`CategoryDetailSheet`/`TrendLegend`/`HistoryEntryList`的货币符号从写死的`¥`改成跟着`settings.currency`走；顺带修了记一笔新建记录时`entry.currency`写死`'CNY'`的bug，改成跟`settings.currency`一致。⑥明细条目格式补上"分类·子分类"——照旧App`renderEntry()`的`catLine`真实格式(`${分类} · ${子分类}`，没有子分类只显示分类)，`EntryCard`第二行同时改成只显示备注(原来会在没备注时重复显示一遍分类名，是明显的占位bug)。⑦额外处理用户追加反馈"字号粗细太粗"——整理`design-system/typography.ts`记录旧App各真实CSS选择器的字体/字号/字重对照表，修正5处偏粗/偏细的地方：底部导航标签、记一笔收支切换、统计页收支/积分切换(旧App均无weight override即400，之前误套了font-semibold/font-bold)、汇率换算页货币选择pill(旧App600，之前用了font-medium即500)、`EntryCard`分类标题(旧App500，之前没设置即默认400)。此轮"中日文混乱翻译"的大范围`t()`替换由后台subagent完成扫描+改写，本次会话核对了改动内容并做了收尾的货币符号统一 | washi-ledger-rewrite |
| 20260818-dev.11 | 2026-08-18 | 装真机测dev.11前的反馈("明细"日语没变成"出入金"、分类/子分类/支付方式没跟着日语切换、首页转圈圈)，定位到两类各自独立的真实bug：①`dict.ts`里`tabHistory`的ja值一直写的是`'明細'`，但翻查旧App`index.html`第2419行`I18N.ja.ledgerSectionTitle`同样是`'出入金'`(中日通用汉字词，不是`detailWord`的"明細")，改成跟zh一样取`LEGACY_JA.ledgerSectionTitle`。②分类/子分类/支付方式/标签这些目录数据(`Category`/`Subcategory`/`PaymentMethod`/`Tag`)全App所有用到名字的地方之前都硬编码读`.zh`字段，从来没实现旧App`catLabel()`/`subLabel()`/`payLabel()`/`tagLabel()`那套按`settings.lang`动态取值(`obj[lang]||obj.zh`)的逻辑，日语模式下这些名字理所当然全部还是中文；新增`lib/catalogLabel.ts`统一收口这四个函数，替换了`EntryCard`/`CategoryDetailSheet`/`CategoryPicker`/`TagPicker`/`AddTransactionPage`/`PaymentMethodIcon`里的`.zh`硬编码，`data/summary.ts`的`categoryBreakdown`/`pointsBreakdownByPaymentMethod`/`dailyTrendBuckets`/`monthlyTrendBuckets`/`dailyPointsTrendBuckets`/`monthlyPointsTrendBuckets`这几个聚合函数(仪表盘/统计页donut图例、趋势图legend用的分类/支付方式名字来源)也加了`lang`参数改用这套函数，不然分类明细钻取以外的图表标签会漏掉。③"首页转圈圈"——翻查旧App`index.html`第2515行`loadCatalogCacheFromLocal()`确认分类/支付方式这套目录数据旧App同样有本地缓存(不是纯网络请求)，新App`CatalogProvider`之前每次冷启动都要等一次真实网络请求才有数据(dev.10刚把"白屏"改成"转圈圈"，但等待网络请求这件事本身没解决，只是从白屏变成有个图标转)；新增`lib/catalogCache.ts`(照`entriesCache.ts`同款模式)，`CatalogProvider`初始state直接读本地缓存、有缓存就立即可用不转圈，同时仍会在后台发一次真实请求悄悄刷新最新数据，跟entries那套"缓存优先+后台刷新"完全对齐。版本号推进到`0.0.0-20260818-dev.11` | washi-ledger-rewrite |
| 20260819-dev.1 | 2026-08-19 | 装真机测dev.11反馈"这版本开始转不出数据了"(转圈圈转个不停，一直进不去)，随后又反馈dev.9做过的"#11我的账户页左上角改小房子图标"一直没生效——这两轮都是当天(0818)夜里在还没推送验证的状态下连着改的，日期跨到0819才正式收尾推送，按规则版本号归到新一天的dev.1：①转圈圈问题用Playwright在沙盒里跑built的应用复现，确认了一个真实的架构缺陷——但沙盒本身连不上Supabase(代理白名单限制)，没法百分百判定真机上失败的具体原因，只能确认并修复缺陷本身：`DashboardPage`/`HistoryPage`/`StatsPage`/`AddTransactionPage`这几处"catalog没就绪就转圈圈"的判断只看`!catalog`，不看`useCatalog()`的`loading`状态——`fetchCatalog()`请求失败时`loading`会变`false`但`catalog`因为没缓存仍是`null`，转圈圈的条件`!catalog`一直成立，界面会永远停在转圈圈、没有任何报错提示也没有重试入口。新增`design-system/components/CatalogLoadState.tsx`拆成"请求中显示转圈圈/请求失败显示错误提示+重试按钮"两种真实状态(重试按钮调`useCatalog()`的`reload()`)，`hooks/useCatalog.tsx`请求失败分支补上`console.error`方便下次复现时查日志；这版修的是"卡死无反馈"这个真实缺陷本身，不保证解决网络本身连不连得上Supabase这个更底层的问题。②`AccountPage.tsx`登录态分支的`<AppLayout>`调用缺了`leftButton="home"`——这个prop确实在dev.9加过，但只加在了未登录态那个`<AppLayout>`调用上，登录态那个后来被处理"中日文混乱翻译"的subagent把`title="我的账户"`字面量改写成`title={t('accountTitle')}`时没保留住这个prop，补上。版本号推进到`0.0.0-20260819-dev.1` | washi-ledger-rewrite |
| 20260819-dev.2 | 2026-08-19 | 用户要求把V2/V3剩下的5项一次性做完、不再分版推："全都放在一版上修正完毕"：①底部导航气泡液态玻璃特效(#4)——之前只落地了图标/文字变色，气泡元素本体一直没做；照旧仓库`index.html`的`tabBubble`真实实现搬：`design-system/components/BottomNav.tsx`用Web Animations API手动摆位/动画(不是CSS transition)，切换tab时先算出"起点∪终点"的并集区域、动画中途先拉伸盖住经过的中间tab再收回到目标tab宽度制造"液态挤压"观感；`index.css`新增`.tab-bubble`(毛玻璃背景+`backdrop-filter: ... url(#glassLens)`)，`index.html`新增`#glassLens`这个SVG位移滤镜定义(照旧仓库原样搬，透过玻璃看被盖住的图标会有轻微折射变形)；气泡宽度跟旧App硬编码78px不同，改成读目标tab按钮的真实渲染宽度(新App的tab是`flex-1`等宽平分，不是旧App那种内容自适应宽度，硬编码常量会跟tab本身对不上)。②仪表盘顶部时间范围选择器(#8)——之前的`DateRangeBar`只是"点左右字段切换上/下月"的视觉占位，这次换成真正的起止日期选择器：`design-system/components/DateRangeBar.tsx`(从`features/ledger/`挪到共享目录)用两个原生`<input type="date">`+`min`/`max`互相约束(起始日的`max`=当前结束日、结束日的`min`=当前起始日，不合规的日期在原生选择器里直接选不到，不用等选完了再弹提示拦截)，`onChange`里再兜底一次"不可为空"+范围校验。原生日期选择器跟随设备系统语言显示，不会是写死的英文月份/星期缩写。③仪表盘"最近明细"跟随顶部时间范围(#6)——`data/summary.ts`新增`summarizeRange`/`categoryBreakdownRange`(跟原有`summarizeMonth`/`categoryBreakdown`共享同一段聚合逻辑，只是判断"在不在范围内"的条件从"是否同一日历月"换成"起止日期区间"，统计页仍用月份版本，不受影响)，`DashboardPage.tsx`状态从`monthAnchor`(单一日历月)换成`startDate`/`endDate`(默认当月1日到今天)，结余/环状图/最近明细全部跟着这个区间走；新增`groupByDayPinned`——照旧App`buildDayGroupedHtml()`真实排序逻辑，今天及之前的记录按日期倒序置顶，晚于今天的(比如提前生成的下月房租)统一沉底按日期升序排，明细页(History)保持`groupByDay`纯降序不变。④明细页复用同一套日期区间选择器(#9)——`HistoryPage.tsx`原来的`startMonth`/`endMonth`(月份粒度)换成`startDate`/`endDate`(日期粒度)，直接用②新增的同一个`DateRangeBar`组件，逻辑和样式跟仪表盘完全一致。⑤环状图右上角标签维度切换(#7)——照旧App`renderHomeDonut()`的`homeDonutDimSelect`真实逻辑：`data/summary.ts`新增`tagBreakdownRange`，选中某个标签后把entries分成"匹配这个标签"/"其他"两组(配色固定用旧App同款的金色/灰色强调色，不跟分类各自的颜色混在一起)，默认(不选)还是原来的分类维度；`CategoryDonutCard.tsx`加`tagDimension`可选prop(只有仪表盘传，统计页donut卡片不受影响)，右上角绝对定位的下拉框照旧App`.dim-select`的真实定位方式(浮在donut右侧本来空着的地方，不挤占布局)；切换支出/收入tab时如果当前选中的标签在新tab类型下不存在，自动退回分类维度(照旧App`renderDonutDimOptions()`的容错)；标签维度下图例行的钻取点击暂时禁用(`CategoryDetailSheet`目前只支持按分类筛选，不支持按标签筛选，这部分不在这次范围内，没有强行拼凑一个不完整的功能)。全部改完用Playwright在沙盒里跑built产物做了smoke test(沙盒连不上Supabase，看不到真实数据，但能验证组件不崩溃)：确认气泡能正确定位+跟随tab切换滑动、明细页日期选择器渲染出正确的默认值(当月1日~今天)。版本号推进到`0.0.0-20260819-dev.2` | washi-ledger-rewrite |
| 20260819-dev.3 | 2026-08-19 | 用户指出标签维度下图例点击钻取被临时禁用是错的："旧app逻辑应该是有写这块,之前旧app也可以点击啊,按照tag检索出明细呀"，要求补上。翻查旧仓库`index.html`第4417行`openMonthDetail(filter, monthKey)`确认filter参数本来就同时支持`{catCode, type}`(分类)和`{tagCode, tagExclude, tagLabelText, type}`(标签，`tagExclude`区分点的是"匹配这个标签"还是"其他(没打这个标签)"那组)两种维度，第4441行`renderMonthDetailList()`里`tagExclude ? list.filter(e=>e.tagCode!==tagCode) : list.filter(e=>e.tagCode===tagCode)`就是标签维度的真实筛选逻辑，之前理解成"这功能本来就没做"是错的，直接禁用点击是偷懒。补上：`CategoryDonutCard.tsx`新增`onSelectTagGroup`回调(标签维度下点图例行不再disabled)，`CategoryDetailSheet.tsx`的props从只认`category: Category`改成通用的`header: {icon,color,label}`+额外传入完整`categories`列表(标签维度钻取出来的记录可能横跨好几个不同分类，之前直接把筛选用的单一category塞给列表里每一条EntryCard、假设"列表里所有记录都是同一个分类"，这个假设在分类维度下碰巧成立、标签维度下是错的，改成每条记录自己按`entry.catCode`去`categories`里查)，`DashboardPage.tsx`的`detailSelection`state改成`{kind:'category',...} | {kind:'tag',tagCode,tagExclude,tagLabelText,type}`联合类型，筛选/表头(图标+配色+文字)分两条分支各自计算——标签维度用`sell`(Material Symbols里最接近旧App自绘"tag"图标的现成图标)+`TAG_MATCHED_COLOR`/`TAG_OTHER_COLOR`(从`data/summary.ts`导出复用，跟点进来之前图例行看到的颜色保持一致)。`StatsPage.tsx`用到的同一个`CategoryDetailSheet`(只有分类维度)同步改成传`header`+`categories`。版本号推进到`0.0.0-20260819-dev.3` | washi-ledger-rewrite |
| 20260819-dev.4 | 2026-08-19 | 装真机测dev.3后反馈dev.2/dev.3做的几项(#4/#6/#8/#9)"压根没改"，先排查确认代码逻辑本来就都在——真正原因是这两版之前一直没人手动点过iOS打包workflow(`workflow_dispatch`纯手动触发)，手机上装的一直是更早的dev.1包，跟这次改动无关。触发打包验证后又追加了4条具体反馈，逐一定位到4个真实bug：①底部导航气泡切tab瞬间跳过去、不滑动(录屏复现确认)——根因是React Router切tab会把整个页面组件树换掉，`BottomNav`跟着被卸载重建，组件内部"是否首次渲染"的判断标记(原存在`useRef`里)每次都被重置，动画分支永远走"瞬间摆位"那条路径；改成把"上一次停在哪个tab"记在模块级变量里，不会跟着组件销毁丢失，重新挂载后能正确判断该不该播放滑动动画。②仪表盘/明细页起止日期默认值从"1日到今天"改成"1日到当月最后一天"——月中打开App时后半个月的数据(含预先录入的未来日期记录)会被默认范围挡在外面，看起来像数据缺失/搜索失效。③`DateRangeBar`两个日期框收起状态改成显示自己生成的"Y年M月D日"文字、不再依赖原生`<input type="date">`收起态渲染——那是跟设备系统语言走的，跟App内切换的中/日语言设置是两回事，会出现"App设置中文，选择器显示英文"。④`RecentEntriesList`去掉硬编码的"最多显示6条"上限(旧版"仪表盘小预览"设计遗留)——顶部日期范围现在是真正可调的筛选器，卡6条会导致调节范围时列表看起来没反应；同时给一直没绑定点击事件的"查看全部"按钮接上跳转明细页。版本号推进到`0.0.0-20260819-dev.4` | washi-ledger-rewrite |
| 20260822-dev.1 | 2026-08-22 | 用户开始通过Smartsheet提需求/bug（B-02 + R-02/R-03/R-04/R-05/R-06/R-07/R-08），一次性处理完：①entries表新增Supabase Realtime订阅(`data/catalog.ts` `subscribeEntriesRealtime`)，登录后常驻+回前台重启，修复B-02"旧App持续加数据、新App好几天都拉不到"——根因是之前只有"登录同步"+"回前台visibilitychange/pageshow补拉"两个时机会去请求，没有常驻订阅兜底，iOS上这两个事件不总是可靠触发。②`CategoryDonutCard`/`PointsDonutCard`图例的百分比/金额字号从`text-stat-figure`(18px)改成12px/13px并固定宽度右对齐，照旧App`.legend-row`真实样式；金额颜色按支出/收入(tab)着色，不再用分类色。③新增`.entry-card`类(2px实线+不对称圆角)统一`EntryCard`外框(仪表盘/明细页共用同一组件)。④新增`.ledger-card`/`.points-chip`类，`BalanceCard`("今月の残高")外框+points显示改用旧App同款虚线朱红边+绿色胶囊样式。⑤记一笔页面`CategoryPicker`(分类细分)/`TagPicker`(标签)/`AddTransactionPage`(支付方式)三处选中态配色统一改成旧App`.sub-pill.selected`真实样式(粉底`--color-primary-fixed`+红字`--color-primary`)，之前各写各的(深色实心填充/绿色淡底)。⑥新增`.app-header`类(半透明+backdrop-filter模糊+柔和阴影)替换`AppLayout`/`AddTransactionPage`/`ThemePage`三处头部原来的实色底+虚线分割线，照旧App`.header`"悬浮在内容上方"效果复用。⑦汇率换算页(`RatePage.tsx`)按Stitch项目「汇率换算(全新重构版)」最新设计稿重做：从多货币搜索+热门汇率列表，换成单一货币对换算卡片(和纸胶带装饰+图章式互换按钮)+走势图(1D/1W/1M/1Y)，走势图数据源新增`data/rate.ts`的`fetchRateHistory`(frankfurter.dev同一个API真实存在的时间序列接口，不是编的假数据)；旧的搜索框/热门汇率列表这版设计稿里没有，未保留。本次改动的B-02和R-04都涉及外部网络请求(Supabase/frankfurter.dev)，沙盒连不上，没能实际联网验证，需真机/能联网环境确认。推送前发现远程已有另一会话推送的`20260819-dev.4`，rebase解决了`package.json`版本号这一处冲突(保留本次的日期重置)，其余7个文件无冲突 | washi-ledger-rewrite |
| 20260829-dev.1 | 2026-08-29 | 一次性做完R-27自绘日期区间日历+Smartsheet待处理需求R-22~R-26，另附怀旧主题定色收尾：①**R-27自绘日历**——新增`RangeCalendarPicker.tsx`替换原生`<input type=date>`方案，单一触发框弹出弹层日历：点选起止日(同一天点两次=单日区间、已选完再点=重新起算、第二次点比第一次早自动交换起止)；顶部年月标题可点开二级"选年→选月"分页(年份页十年翻页)，抽屉式跳转配Windows日历风格的"下钻缩小进入/回退放大退出"过渡动画；去掉左右切月箭头和年月标题旁多余的下拉小箭头，改成在日历格子区域直接左右滑动切月；年/月选择页格子去掉网格线，只用背景色标注当前/区间内状态；弹层加毛玻璃(`backdrop-filter: blur(18px) saturate(150%)`)；新增"本月/过去一个月/过去一年"三个预设，点选即时生效并关闭弹层；长区间(如"过去一年")重新打开时默认定位到区间**末尾**月份而不是起始月份；顺带修了`formatDateRangeLabel`对非整月对齐区间会显示原始`YYYY-MM-DD`而不是本地化日期的bug。②**R-22**——仪表盘"最近记录"标题下新增可折叠搜索框(照旧App`.detail-search`真实交互：30×30圆形虚线图标按钮点击展开成圆角矩形输入框，失焦无内容自动收起)，复用明细页同款搜索匹配逻辑(新增`data/summary.ts`的`matchesEntrySearch`共享函数)；搜索词状态从组件内部提升到`DashboardPage`+`dashboardFocusMemory.ts`新增`search`字段/`saveDashboardSearch()`，解决新建/编辑/复制跳转再返回时搜索词丢失的问题(用户确认方案B：搜索词无条件保留，搜不出也不自动清空，用户自己清)；"查看全部"按钮改成`max-width`+`opacity`+`margin`过渡收缩(不是瞬间消失)让出空间给搜索框平滑占满整行。明细页/仪表盘搜索框样式统一改成圆角胶囊+虚线边框(参照日文版参考截图)。③**R-23**——个人中心页(`/account`)头部左右按钮全部去掉只留返回按钮，隐藏底部导航，复用R-18"子页面"模式(`leftButton="back"`)返回上一页而不总是回仪表盘(该页依赖`useAuth()`真实Supabase会话，沙盒连不上网络没能截图验证，已在Smartsheet备注注明)。④**R-24**——左侧抽屉菜单去掉"个人中心"入口(跟头部账户图标重复)。⑤**R-25**——日期区间选择器补上"过去一个月/过去一年"预设(随R-27一并实现)，"过去一个月"是滚动的1个月窗口而不是上个自然月。⑥**R-26**——明细页默认日期区间从"当月"改成"全部数据范围"(`data/summary.ts`新增`fullDataRange`，取最早~最晚一条记录日期；全新用户没有数据时退回当月这个合理默认)。⑦**怀旧主题定色**——主色定为`#FF9459`；按用户要求把"深浅衍生色"和"对比色"两类token拆开：`--color-primary-container`/`--color-inverse-primary`/`--color-primary-fixed`/`--color-primary-fixed-dim`/`--color-app-title`改用`color-mix(in srgb, var(--color-primary) X%, black/white Y%)`公式从主色衍生，以后只改一行主色就能联动这些深浅色阶，不用每次改色相都手动重调；`--color-on-primary`系(对比色，需要人工判断黑白哪个更清晰)保持手选；`--color-app-title`的黑色混合比例从45%(太浑浊发褐)调到25%。⑧新增`--color-expense`/`--color-income`两个跟主题解耦的固定色token(支出恒定红`#D1665A`，收入沿用`--color-secondary`)——用户明确"数字的颜色不要变，标签文字可以跟主题"，审查`BalanceCard`/`CategoryDonutCard`等改成金额数字用固定token、标签/Tab文字仍用跟主题的`text-primary`/`text-secondary`。全部改动均用Playwright对built产物做了真实截图验证(R-23除外，见上)。版本号推进到`0.0.0-20260829-dev.1` | washi-ledger-ui-fixes |
| 20260903-dev.1 | 2026-09-03 | 新一天，日期重置：①**R-20汇率悬浮快捷入口**——新增`RateShortcutFab.tsx`，挂在`AppLayout`非子页面分支，仪表盘/明细/统计/我的账户四个主页面右下角常驻一个48px悬浮按钮(主"记一笔"FAB正上方20px)，点击直达`/rate`；样式取用户确认稿"方案A加方案C"(位置/`currency_exchange`图标取方案A，呼吸光晕取方案C)，确认稿存进`design-assets/icons/rate-shortcut-fab/`。②**汇率走势图默认滚动到最新数据**——`RatePage.tsx`图表点数多、超出容器宽度时默认停在最左边(起始点)，看不到最新汇率；给横向滚动容器加ref，`chartGeometry`变化(切换货币对/时间范围/下拉刷新)时`scrollLeft = scrollWidth`滚到最右，照旧App`renderRateTrendChart()`同一个思路。均用Playwright对built产物做了真实验证(FAB在四个主页面正确显示/汇率页本身正确隐藏，图表mock长时间序列数据后scrollLeft+clientWidth===scrollWidth确认滚到底)；`/account`页面因sandbox连不上Supabase出现一个预置的、与本次改动无关的既有bug(信号态不一致导致渲染崩溃)，未在此次改动范围内处理 | washi-ledger-ui-fixes |
| 20260903-dev.2 | 2026-09-03 | **R-29新增"夏 · 花火"主题**——移植旧App`index.html`的`summerB`主题：`ThemeSkin`新增`'summer'`(深藏青底+朱红/金黄/翡翠绿，色值照抄旧仓库`:root[data-skin="summerB"]`)，`ThemePage.tsx`新增第三张卡；新增`FireworksBackground.tsx`完整移植旧App`starSkySvg()`+`startFireworksCanvas()`的星空+canvas实时烟花粒子引擎(牡丹/菊花/柳枝/十字爆/闪烁星5种类型，滚动时自动暂停)，挂在`App.tsx`根部(不随路由重新初始化)，只在`themeSkin==='summer'`时渲染；`index.css`新增发光描边(`.entry-card`/`.ledger-card`/主FAB/h1标题)；新增`ThemeIcon`组件+`public/icons/fw/`下6个从旧App potrace SVG符号原样导出的专属图标，接入`BottomNav`仪表盘&统计tab、`NavDrawer`汇率&设置行、`SettingsPage`主题行、`RateShortcutFab` | washi-ledger-ui-fixes |
| 20260903-dev.3 | 2026-09-03 | **B-39头像外框跑偏/漏页面**——12张预设头像源图从`preset-avatars-v1-stitch.png`拼版图按网格坐标+质心法(centroid+95分位半径，规避单像素噪声干扰)重新精裁：原来单张裁切留的白边大小不一(最多到19px/160px)且部分图案中心跟画布中心没对齐，clip成圆形后露出不对称的"外框"、看起来像圆是偏的；稀疏线条/留白设计(山水/朱印/开片纹)单独用同行相邻图案的网格坐标校准。非编辑态头像展示(我的账户主头像/头部小头像/抽屉头像/更换头像页顶部预览)去掉多余的`border-primary`装饰环，只留`ChangeAvatarPage`选择网格里表示"当前选中哪一个"的指示环。补上`SettingsPage`"我的账户"行漏接的头像(R-23那批只接了`NavDrawer`/`AppLayout`头部图标，这一行当时漏了)。"从相册选择"仍按之前约定押后，不在本次范围。均用Playwright对built产物做了真实验证(mock登录态截图全部avatar-bearing页面：仪表盘/抽屉/明细/统计/我的账户/设置/更换头像) | washi-ledger-ui-fixes |
| 20260904-dev.1~4 | 2026-09-04 | **夏·花火主题补3个专属图标**——旧App`applyThemeIcons()`只换6处(设置/着せ替え/闹钟/记账tab/统计tab/汇率tab)，这6处R-29已照搬；剩下"明细"tab/"关于App"/"我的账户"(未登录兜底态)3处是新App专属、旧App没有对应图案可复用，另开Stitch项目("夏·花火主题补充图标")重新设计，用pip装的`potracer`(纯Python版potrace)把选中的参考图描成矢量path：①明细→捞金鱼网+游动金鱼(`ic-history-fw.svg`)②关于App→系着蝴蝶结的信息卡片(`ic-about-fw.svg`)③我的账户→维持用户确认的第一版实心填色狐面(きつね)半身像(`ic-account-fw.svg`)，中途一度按要求改过线稿版但用户确认要保留原样实心版，改动已撤回、线稿尝试归档进`design-assets/icons/fw-supplement-icons/history/`标注未使用。明细/关于两个线稿图标描边粗细经过实测校准(用面积/周长比估算描边宽度，对齐"设置"图标约8.9px的粗细，不是最初误判的加粗方向)。接入`BottomNav`明细tab、`NavDrawer`/`SettingsPage`的关于行、`AppLayout`/`SettingsPage`的账户图标(未登录态) | washi-ledger-ui-fixes |
| 20260904-dev.11~12 | 2026-09-04 | **B-40/B-41汇率走势图修复**——横向滚动时Y轴刻度跟着滚走(改成Y轴单独一个不滚动的固定`<svg>`，跟可滚动的主图区拆开，`CHART_LEFT`从40收窄到10，新增`AXIS_W`常量)；1W时间窗从7个自然日拓宽到9个日历日(交易日数据本身有空档，7天窗口经常凑不够足够的真实数据点)；`loadHistory()`补上用走势图最新一个真实数据点回填顶部卡片`snapshot`的兜底逻辑，修正两处偶发对不上的"最新汇率"数字。**B-47冷启动头像闪烁**——`useAuth()`内部异步确认登录态期间(`loading`为true)之前统一按"未登录"分支渲染，已经真登录过的用户会看到头像先闪一下默认人形图标、等异步结果出来才换回真头像；`AppLayout.tsx`/`NavDrawer.tsx`改成`loading`期间只要`hasEverSignedIn()`(本机曾经真登录过)为true就乐观展示头像，跟`CloudDisconnectBanner.tsx`已有的同款模式对齐 | washi-ledger-ui-fixes |
| 20260909-dev.1 | 2026-09-09 | 新一天，日期重置：①**页面切换硬切"跳一跳"改平滑过渡**——`App.tsx`所有页面路由切换之前是瞬间卸载旧页面/挂载新页面，中间没有任何过渡；新增`AnimatedRoutes`用`location.pathname`当key包一层`.route-fade`，路由变化时逼React重新挂载这层div触发一次200ms渐显(纯opacity，不用transform——包裹层若加transform会变成内部fixed定位后代如BottomNav/RateShortcutFab/NavDrawer的containing block，宽屏预览下跟视口尺寸不一致有位置错位风险，测过这个坑，opacity没有这个副作用)。②**"夏 · 花火"图标光效系统正式接入真实代码**(此前多轮设计稿定稿于`design-assets/icons/summer-fireworks-icon-effects/`)——新增`lib/iconGlyphs.ts`(从Material Symbols字体用fontTools提取的9个图标path+Stitch设计稿potrace描出的新书本图标，共10个，含视觉居中viewBox修正)、`AppIcon.tsx`(纯形状渲染)、`IconGradientDefs.tsx`(全局唯一共享渐变色源，挂`App.tsx`根部)、`IconEffects.tsx`(`RingIconEffect`贴真实常驻圆形容器发光/`BareIconEffect`没有常驻圆形容器时贴图标轮廓本身发光，逐个位置核对过真实UI是否存在常驻圆形背景才定的)——图标形状/颜色/发光效果三层分开维护；`ThemeIcon.tsx`新增`effect`prop接入这套新系统，逐步替换掉旧的"旧App专属插画图标"(`fw`prop)方案。接入位置：`SettingsPage`语言/货币行(有圈)、`AppLayout`菜单/返回/账户按钮+`NavDrawer`关于/汇率行(裸图标)、`ThemePage`/`AddTransactionPage`/`SignUpPage`三处独立返回按钮(裸图标)、`BottomNav`三个tab(裸图标，仪表盘tab换成新画的书本图标且全主题通用——不只是summer——因为Material Symbols没有合适的"账本"图标)、`RateShortcutFab`(有圈，顺带把呼吸光晕颜色源从跟新色板不搭的`--color-secondary`薄荷绿改成`--color-primary`珊瑚红，呼吸节奏不变)、头像光晕(`AppLayout`头部28px+`NavDrawer`44px，贴照片外沿，不放进`overflow-hidden`的圆形裁切容器内部避免被自己裁掉)。清理8个不再使用的旧App专属插画(`ic-book-fw`/`ic-history-fw`/`ic-lantern-fw`/`ic-exchange-fw`/`ic-account-fw`/`ic-lang-fw`/`ic-currency-fw`/`ic-about-fw`)。均用Playwright对built产物做了真实截图验证(summer/default两套主题下逐个接入位置的截图对比，确认default主题下除仪表盘书本图标外零回归) | washi-ledger-ui-fixes |
| 20260909-dev.2 | 2026-09-09 | **顶部安全区与header出现视觉接缝**——用户真机截图反馈：状态栏那一条(`env(safe-area-inset-top)`留白)是`FireworksBackground`原始清晰画面，header那一条是`backdrop-filter:blur(1px)`模糊过的，中间一条明显分界线。`AppLayout.tsx`改成把padding-top和header的背景/blur一起挪到包住safe-area+header的同一层div上，两段统一成同一层玻璃质感 | washi-ledger-ui-fixes |
| 20260909-dev.3 | 2026-09-09 | 用户连续反馈的一批真机问题，逐一定位修复：①**汇率悬浮按钮虚线圆环颜色跟确认稿对不上**——上次只改了呼吸光晕颜色源，虚线边框(`border`)/`color`忘了一起改，真机上圆环还是薄荷绿(`--color-secondary`)，确认稿"改成珊瑚红"那版虚线圆环本身也是珊瑚红；summer下`border`/`color`一并改成`--color-primary`。②**有圈图标发光真机上糊成"一片红"**——两处独立根因都修了：(a)`.icon-ring-glow`/`.rate-fab-glow`径向渐变末端之前用字面量`transparent`(=透明黑，不是"这个颜色但透明")，插值经过一段RGB被拉向黑色的浑浊过渡色，叠加`blur()`更明显，这是渐变的通病不是某浏览器专属bug；改成显式写同色相、alpha渐变到0，规避浑浊过渡。(b)`RingIconEffect`之前自己又包了一层跟图标同尺寸(24px)的容器当发光定位基准，`inset:-9px`没有相对调用方真正的圆形容器(40px)算，发光几乎全糊在纯色圆内部没有真正探出圆外；改成发光层不再自己包裹，直接作为调用方容器的直接子节点渲染，`inset:-9px`才是相对真实圆算的。③**主题选择页summer下背景还是方格纸**——`ThemePage.tsx`是独立整屏子页面不走`AppLayout`，R-29那批summer背景透明化只改了`AppLayout`，这页当时漏了；照同一套处理搬过来。④**图标光效范围遗漏主题/设置两处**——`SettingsPage`"着せ替え"(`palette`)行和`NavDrawer`"设置"(`settings`)行当时没纳入9+1个图标的覆盖范围，两个图标都是独立命名的glyph、bbox本身严格居中，直接从字体提取接入(着せ替え接ring效果、设置接bare效果)，清理2个不再用的旧插画。⑤**补上确认稿里的萤火虫装饰**(此前接入真实代码时跳过了、没跟用户说清楚)——语言/货币两个最早定稿的ring图标补上暖色光点游走效果，固定3组预设(游走轨迹+发光节奏各自独立的时长/负delay组合)而非运行时随机生成，负delay让每个萤火虫挂载时已处于周期中间某状态，不会全部从第0帧同步起跳。均用Playwright对built产物做了真实截图验证，真机层面的视觉判定(①②③)以用户提供的真机截图为准 | washi-ledger-ui-fixes |
| 20260909-dev.4 | 2026-09-09 | ①**汇率换算卡片下方数字支持双向互算(R-30)**——之前只有上方数字(源货币)是输入框，下方(目标货币)只是纯展示的计算结果。改成两栏都能编辑：新增`lastEditedField`记"哪一栏最后被手动编辑过"，没被编辑的那一栏在渲染时用汇率从被编辑的那一栏实时推算显示，不是各自维护一份互不同步的状态——这样切货币对/汇率刷新时，没在编辑的那一栏也会自动跟着重新算对。互换按钮不动，还是只换货币种类不换数值。"保留结果"维持现状：汇率页离开即清空(组件卸载，本来就没做持久化)，这次不新增持久化逻辑。Playwright验证双向计算：下方输入10(CNY)，上方正确算出228.31(JPY，10/0.0438)；上方输入500(JPY)，下方正确算出21.90(CNY，500×0.0438)。②**底部导航图标呼吸光效切tab后"跳"一下(B-48)**——`BottomNav`每次切tab整个组件都会销毁重建(`.tab-bubble`滑动动画当初也踩过的架构限制)，`icon-glow-breathe`呼吸动画不传delay时新挂载的实例永远从0%关键帧重新开始，看起来像"跳"一下。`BareIconEffect`/`ThemeIcon`新增`glowDelayMs` prop，`BottomNav`用`useState(()=>Date.now()%5500)`(惰性初始值，只在挂载这一刻算一次)算出一个基于真实时间戳的负delay传下去，把动画相位锚定在真实时间轴上而不是"这个实例何时诞生"。Playwright验证：紧接remount后立即采样的`animation-delay`跟`-(挂载时刻wall-clock % 5500)`预测值接近 | washi-ledger-ui-fixes |
| 20260910-dev.1 | 2026-09-10 | 新一天，日期重置。用户真机反馈B-46/B-48上一版修复没有彻底解决，继续深挖两处更深层的根因：①**有圈图标真机上还是"空的"(B-46续)**——之前所有图标共用一份渐变(`IconGradientDefs.tsx`，挂在`App.tsx`根部一个`width=0 height=0`的`<svg>`里)，图标自己的`<svg>`用`url(#icon-fill-grad)`跨SVG引用它——这种写法在部分WebKit/Safari版本上有已知的不稳定表现(引用解析不到，图形直接没有填充，只剩背后的光晕可见)，不是z-index层级问题(图标本身层级确实在光晕之上，没错，只是内容没画出来)。改成`AppIcon.tsx`每个实例自带一份本地`<defs>`(用`useId()`生成独立id)，彻底不再有跨SVG引用；删除不再使用的`IconGradientDefs.tsx`。②**底部导航图标气泡切换凝滞感(B-48续)**——路由切换的入场渐显(`.route-fade`)之前包在`App.tsx`整个`<Routes>`外面，连`RateShortcutFab`/`BottomNav`/`NavDrawer`这些"应该感觉持续存在、不随页面切换重新出现"的常驻元素也被一起罩进去——每次切tab，这些元素的opacity从0淡入，跟`BottomNav`自己`.tab-bubble`的Web Animations API滑动动画同时跑，两个动画叠在一起，真机上就是"切换有凝滞感"(这是B-48上一版只修了呼吸光效本身的delay、没发现的另一层根因)。挪到`AppLayout.tsx`内部，只包住header+main这两块真正算"页面内容"的部分，`RateShortcutFab`/`BottomNav`/`NavDrawer`/`CloudDisconnectBanner`留在这层渐显外面。Playwright验证：每个图标渲染出独立的gradient id；`.route-fade`容器结构检查确认不再包含`BottomNav`/`RateShortcutFab`节点 | washi-ledger-ui-fixes |
| 20260910-dev.2 | 2026-09-10 | 用户反馈"切换到各个页面、还有返回按钮还是太快了，不够平缓"，两处改：①**`.route-fade`时长/缓动曲线**——`200ms ease-out`改成`320ms cubic-bezier(0.4,0,0.2,1)`(standard material ease-out，前段快、收尾放缓，比线性ease-out更有"落定"感)。②**补齐4个未接入`RouteFade`的独立整屏子页面**——`ThemePage`/`AddTransactionPage`/`SignInPage`/`SignUpPage`不走`AppLayout.tsx`，dev.1那批把渐显逻辑从`App.tsx`挪进`AppLayout.tsx`内部时，这4个独立页面漏了覆盖，切入/退出(含返回按钮)一直是硬切、零动画，用户反馈的"返回按钮太快"主要就是这几页。新建可复用的`RouteFade.tsx`组件(用`location.pathname`当key强制重挂载触发CSS入场动画)，`AppLayout.tsx`原有内联逻辑也改用它(去重)；4个独立页面各自补上，包裹范围只到"页面内容"本身，`AddTransactionPage`底部`fixed`保存条严格留在包裹层外面(同一条transform-containing-block限制，纯opacity不受影响)。Playwright验证：`.route-fade`计算样式确认`animation-duration:0.32s`/`animation-timing-function:cubic-bezier(0.4, 0, 0.2, 1)`已生效；`/add`/`/signin`/`/register`/`/theme`四页截图确认布局无回归，`fixed`保存条/FAB/底部导航坐标不受影响 | washi-ledger-ui-fixes |
| 20260910-dev.3 | 2026-09-10 | **汇率换算页"实时汇率"胶囊条新增一键清除+刷新按钮(R-31)**——点击后①把上下两个金额输入框清空恢复默认(上方100/下方空)②重新拉取一次最新汇率数据，不影响货币对选择和走势图时间范围。设计过程(Stitch多轮出图+用户反馈)：第一版位置放在标题栏、图标看起来像刷新但当时只有清空功能，用户反馈两处都不对；改成位置挪到胶囊条上、图标语义加上"刷新最新汇率"这层真实功能使其名实相符；用户进一步反馈默认/怀旧主题下这类小按钮应该有阴影+悬浮反馈(查证`AppLayout.tsx`头部图标的`hover:-translate-y-0.5`+`active:scale-90`惯例、以及互换按钮的`stamp-shadow`+`boxShadow`惯例后补上，缩小尺寸配合胶囊条高度)。用fontTools从Material Symbols字体提取`restart_alt`字形加入`iconGlyphs.ts`(bbox本身接近居中，不需要偏移校正)，接入`ThemeIcon`(`effect="bare"`)而不是裸的`material-symbols-outlined`——默认/怀旧主题走素色+阴影悬浮，夏·花火主题自动走`BareIconEffect`发光，三个主题的差异化效果不用各写一套。新增i18n词条`rateResetAria`(中/日)。Playwright验证：默认主题下按钮正确渲染在胶囊条右侧；切到summer主题后图标确认呈现发光效果(区别于默认主题的纯色)，无控制台报错 | washi-ledger-ui-fixes |
| 20260910-dev.4 | 2026-09-10 | **R-31续排：按钮位置改到换算卡片右上角**——用户对dev.3那版位置(胶囊条里)不满意，来回确认了好几轮才对齐："放胶囊条里"→"应该在卡片自己右上角，卡片没有右上角吗"→(中途误判成参照旧App`index.html`的`#refreshRateBtn`整段搬成通栏大按钮，被用户否定，承认理解错了)→最终确认：小图标按钮，位置在卡片右上角、跟"JPY·日元"这行对齐在同一水平线上，功能保持二合一(清空金额+刷新汇率)不变，胶囊条本身恢复成原来的纯文字展示(不放按钮)。实现上改成按钮和货币选择行共用一个`flex justify-between`容器，不是绝对定位猜偏移量——两者垂直中心用Playwright实测完全对齐(121px=121px)。另外解决了按钮跟右上角和纸胶带装饰"挤在一起"的间距问题：给这一行加`mt-1.5`跟胶带拉开距离，同时把下面"0.00"到"取得中"胶囊之间`mt-6`收紧成`mt-3`(用户截图标出的多余留白)，卡片整体上下重新分配，不是单纯堆叠。中途误删又补回`iconGlyphs.ts`的`restart_alt`字形/`rateResetAria`词条(git revert后从头对齐一遍)。Playwright验证：typecheck通过、按钮与货币行对齐、真实截图确认间距 | washi-ledger-ui-fixes |
| 20260911-dev.1 | 2026-09-11 | 新一天，日期重置。两处修复：①**汇率页"实时汇率"胶囊补回更新日期(B-43衍生反馈)**——R-04(2026-08-22)重构成Stitch卡片时这行日期被新设计稿拿掉了，用户要求恢复；照走势图选中点"日期 · 数值"同一种拼接格式加回胶囊同一行，不单独起一行。②**B-46第10轮：设置页有圈图标"一片红"真正解决**——这轮排查揪出了此前9轮修复一直没抓到的两处硬伤：(a) `radial-gradient(circle, ...)`不写显式size关键字时默认`farthest-corner`(到正方形box对角线的距离，是半宽的√2≈1.41倍)，不是想当然的`closest-side`(半宽本身)——之前算的每一个百分比stop都被这个隐藏默认值拉长了41%，这是光晕和图标真实边缘之间一直有空白、大小怎么调都不对的根本原因；(b) 单靠"渐变透明终点留够余量"来保证"图标圆圈内部零渗色"，会跟"光晕紧贴边缘无空白"这两个要求互相矛盾(留的安全垫越大越没有空白问题但缝隙也越大)，改用`mask-image`/`-webkit-mask-image`做真正的硬边界裁切(mask在background的blur渲染结果算出来后才生效，边界不受blur影响)，background渐变可以放心从靠近圆心处就给亮色，mask统一在图标真实边缘(60.6%)裁掉多余部分，两个要求不再矛盾。用Python逐像素采样验证(不是肉眼判断)：真实图标半径范围内(r=12~19px)颜色完全不变，r=20px(真实边缘)立即跳变到亮色，中间无空白无渗色 | washi-ledger-ui-fixes |
| 20260911-dev.2 | 2026-09-11 | 同一天连续4处小修：①**统计页趋势图例选中态日期恢复不了**——收支/积分趋势图点开某根柱子后图例头部显示的日期，用的`TrendBucket.label`字段按日视图只是裸数字(比如"2")，因为同一字段还兼职柱状图下方横轴刻度("1 2 3...11"那排)；改用已有的`.key`字段(本来就是完整"YYYY-MM-DD"/"YYYY-MM")，不用新增字段，对B-49的补充修复。②**B-46第11轮：头像光晕留白**——头像(`AppLayout.tsx`，28px)复用了SettingsPage(40px)那套`.icon-ring-glow`后出现光晕跟头像之间有留白，根因是上一轮mask硬边界写死的60.6%/60.8%只对40px场景成立(`inset:-13px`是固定像素，容器尺寸一变，"真实边缘占光晕盒子半径的比例"就跟着变)；改成`--icon-size`自定义属性+`calc(var(--icon-size)/2)`绝对像素裁切，不再依赖百分比换算，头像28px/设置页40px两种尺寸通用一份CSS。逐像素采样验证零留白零渗色，40px场景无回归。③**言語/通貨两行文字真机上基线没对齐**——原生`<select>`可见文字由系统表单控件外壳渲染，纯CJK文本("日本語")和CJK+英文括号混排文本("日元 (JPY)")在iOS上可能不是同一套基线算法(桌面Chromium测不出这个差异，量出来是对齐的)；改成自己画`<span>`显示文字，`<select>`保留但降级成透明可点击交互层(功能不变，仍会弹出系统原生选择器)，三行渲染方式统一。箭头间距顺带统一成跟着せ替え同款`flex items-center gap-1`(之前沿用原生select时代`absolute right-0`贴边的旧写法，比着せ替え那行紧)。④**图标到文字间距偏窄**——夏·花火主题下图标光晕视觉边界有13px(66px发光盒子`closest-side`半径33px减图标自身20px半径)，比`SettingsRow`原有`gap-sm`(12px)间距还宽，肉眼看紧贴文字；提到`gap-md`(16px)，只改这一处class，不动全局`gap-sm`token | washi-ledger-ui-fixes |
| 20260911-dev.3 | 2026-09-11 | **页面跳转"一跳一跳"根因修复**——用户拿参考App(另一款真实存在的日本记账App)录屏对比，指出我们的页面切换不够平缓。定位到真根因：`RouteFade.tsx`用`location.pathname`当key逼React重新挂载，路由一变旧页面这个节点在同一帧里被直接卸载销毁，新页面节点单独从opacity:0淡入——旧页面是"瞬间消失"不是"淡出"，新旧动画完全没有重叠，效果是"瞬间切到背景→再淡入新页面"，感觉上就是硬跳一下再补一个淡入，不是真正的交叉淡出淡入。改用浏览器原生View Transitions API(`document.startViewTransition`)：新增`hooks/useAppNavigate.ts`统一包一层所有程序化跳转(`useNavigate`→`useAppNavigate`，返回函数签名完全不变，全站10个文件的调用点只改了import，没有改任何调用代码本身)，用`flushSync`保证路由状态变化在浏览器截取"旧/新DOM快照"之间同步完成；`<Link>`/`<NavLink>`这类声明式跳转新增`viewTransitionLinkClick`辅助函数拦截普通左键点击走同一套包装(`AccountPage`/`SignInPage`/`SignUpPage`/`AppLayout`头像/`BottomNav`/`RateShortcutFab`共6处)。不支持这个API的机型(较老iOS WebKit)自动降级成跟以前一样的直接跳转，不会报错；`RouteFade.tsx`改成检测到浏览器支持View Transitions时完全跳过自己的key remount+`.route-fade`渐显(避免跟浏览器原生动画打架)，不支持时保留原有兜底渐显。`index.css`新增`::view-transition-old(root)`/`::view-transition-new(root)`时长缓动，跟`.route-fade`对齐同一条320ms/material ease-out曲线，不引入新的动效语言(用户明确要求不改成滑动/底部弹出卡片这类新样式，只是要把现有的淡入淡出做平滑)；`BottomNav`/`RateShortcutFab`这两个"应该感觉持续存在"的常驻元素各自加了`view-transition-name`(`bottom-nav-vt`/`rate-fab-vt`)，避免被默认的整页交叉淡出淡入卷进去、跟自己的Web Animations气泡滑动动画打架。沙盒Chromium(不支持外部网络，用不了真机，但View Transitions API本身沙盒版本支持)验证：`document.startViewTransition`确认被每次路由跳转真实调用；`document.getAnimations()`采样到浏览器原生的`-ua-view-transition-fade-in`/`-ua-view-transition-fade-out`成对同时运行(证明新旧内容真的在同一时间段交叉淡出淡入，不是先后顺序)，且`bottom-nav-vt`/`rate-fab-vt`被浏览器识别成独立于`root`的过渡组(不会被整页交叉淡出淡入误伤)；控制台确认无新增报错(仅有的报错是沙盒连不上Supabase/汇率API这类既有的网络不可达问题，跟这次改动无关)。真机上View Transitions是否达到预期的平缓观感仍需真机验证 | washi-ledger-ui-fixes |
| 20260912-dev.1 | 2026-09-12 | 新一天，日期重置。两处修复：①**仪表盘"最近の记录"补上当日净额**——明细页(`HistoryEntryList.tsx`)每天标题旁边一直有净额显示(收入绿/支出红)，仪表盘的`RecentEntriesList.tsx`当初做成没有这个(有意的差异化设计，不是漏做)，这次按用户要求补齐，直接复用明细页已经验证过的同款算法(`reduce`按收入+/支出-求和)和样式；`DashboardPage.tsx`新增传入`currency={settings.currency}`。净额按分组内`group.entries`现算，这组entries本来就是搜索关键词过滤后的结果，搜索词一变净额自动跟着重算，不用额外处理。②**统计页"支出/収入"切换看不出是可点的**——之前是裸`<select>`(`bg-transparent border-none`)，视觉上跟纯文字没区别；改成跟同一张卡片里"日別/月別"完全同款的分段胶囊按钮样式(灰底圆角框+选中态白底珊瑚字+阴影)，两处视觉语言统一，不用另外设计一套。两处改动沙盒里都因为连不上Supabase没有真实数据可截图验证，只做了`tsc`/`oxlint`检查，样式改动是直接复用同一页面里已经上线验证过的现成class | washi-ledger-ui-fixes |
| 20260913-dev.1 | 2026-09-13 | 新一天，日期重置。**R-32：新增レシート(小票)扫描存档功能**——新建/编辑/复制记账页新增"扫描小票"入口：拍照/选图(`@capacitor/camera`，Web端自动降级成`<input type=file capture>`)→用Tesseract.js(免费、纯客户端OCR，用户明确要求不用付费云端方案)识别文字→弹层里可编辑校对识别结果→确认后**不保存原始照片**(只是识别过程的临时输入，识别完即丢弃)，把校对后的纯文字渲染到canvas(借WebView系统字体解决jsPDF不内置CJK字体的限制)生成一份文字版PDF→上传到Supabase Storage的`receipts`私有bucket(路径`<用户id>/<记录id>.pdf`，重新扫描是upsert覆盖同一路径，不会越攒越多孤儿文件)→每条记录(仪表盘+明细页共用的`EntryCard.tsx`操作抽屉)新增"查看凭证"按钮，点击现签一个1小时有效期的签名URL打开(private bucket没有永久公开链接)。新增`Entry.receiptPath`字段(Supabase一列`receipt_path`，`worker/src/entries/types.ts`同步镜像更新)。OCR服务商刻意解耦成单一入口函数`lib/receiptOcr.ts`的`recognizeReceiptText()`，其余代码都不直接碰Tesseract.js的API，以后要换服务商只改这一个文件。自动读取金额填入表单**这次不做**(用户明确说可以滞后到以后)。前端设计稿经Stitch生成并已获用户确认(沿用"Washi Ledger & Liquid Glass"design system，不是另起一套)。新增依赖`@capacitor/camera@^6.1.3`(锁定v6避免跟本项目`@capacitor/core@^6.1.2`冲突)、`tesseract.js@^7.0.0`、`jspdf@^4.2.1`。Supabase端需要用户手动执行的SQL(加`entries.receipt_path`列)和Storage bucket/RLS设置步骤见新增的`worker/RECEIPT_STORAGE_SETUP.md`，这次代码改动本身不依赖用户先做完这两步就能编译，但缺了这两步实际运行时上传/加列会失败。沙盒连不上Supabase，只做了`tsc -b`(前端)+`tsc --noEmit`(worker)+`oxlint`验证，没有真机/真实网络环境下的端到端验证 | washi-ledger-ui-fixes |
| 20260913-dev.2 | 2026-09-13 | **R-32真机反馈修复三处**：①**扫描按钮真机上完全点不出反应**——根因是`washi-ledger-ios-test-build.yml`的Info.plist每次CI都是`npx cap add ios`全新生成，没有patch进`@capacitor/camera`要求的三个隐私用途说明键(`NSCameraUsageDescription`/`NSPhotoLibraryUsageDescription`/`NSPhotoLibraryAddUsageDescription`)；iOS原生`CameraPlugin.checkUsageDescriptions()`在调用一开始就会因为缺键直接reject，连系统权限弹窗都不会弹出，表现就是"点了跟没点一样"。在已有的"锁定竖屏+写版本号"那个PlistBuddy patch步骤里一并加上这三个键。②**扫描入口位置太靠下**——从备注区下面(要滚到底才能看到)挪到支出/収入页签下面，进页面第一屏就能看到，不用再往下滚。③**收入页签也显示扫描入口**——レシート只对应支出场景，收入不该有；改成`type === 'expense' &&`条件渲染；另外新建模式下(编辑/复制锁定类型切不了，不受影响)如果先在支出页签扫描存了凭证、又切到收入页签，新增一个effect把本地`receiptPath`状态清掉，防止这份隐藏起来的凭证被悄悄带着存进收入记录——不去删Storage里已经传上去的文件，留成孤儿文件(跟放弃整个新建流程不保存是同一种可接受的取舍)。三处都是根据用户提供的真机录屏定位，不是猜测；Info.plist那处沙盒连不上Xcode/真机没法二次验证，前端两处过了`tsc -b`+`oxlint` | washi-ledger-ui-fixes |
| 20260913-dev.3 | 2026-09-13 | **R-32扫描方案推倒重做：从OCR文字识别改成"全能扫描王"式图像扫描**——用户实测反馈Tesseract.js对日文小票识别出来是大段乱码，且重新排版丢失了小票原本的表格对齐版面；改成不做文字识别，直接对拍摄的照片做自动边缘检测+透视纠偏(参考用户明确要求的全能扫描王效果)，保留小票原始视觉样式。新增`lib/receiptEdgeDetect.ts`：用OpenCV.js(`@techstark/opencv-js`，Apache-2.0开源免费，纯WASM本地跑不联网不需要账号)——灰度化→高斯模糊→Canny边缘检测→找轮廓→挑最大四边形轮廓当小票边界→透视变换拉正，再做一次基于直方图1%/99%分位数的自动对比度拉伸(auto-levels)让画面更清晰锐利接近扫描件；检测不到合适四边形时不报错，兜底返回未裁剪的原图(仍做对比度增强)，不阻塞整个流程。`lib/receiptPdf.ts`从"把OCR文字画到canvas再存图"改成直接把处理后的图像整张嵌入PDF(`doc.addImage`)，日期信息改写进PDF文档属性(标题)而不是画在图面上。`ReceiptScanSheet.tsx`弹层流程从"拍照→OCR识别→可编辑文字确认"简化成"拍照→自动处理→预览确认(带"已自动裁边纠偏"/"未检测到边缘使用原图"提示)"，去掉了文字编辑框。`lib/receiptOcr.ts`(Tesseract.js封装)保留在代码里但不再被这条流程调用——留着是因为以后"自动读取金额填入字段"这个滞后需求如果要做，这套OCR解耦接口还用得上。文件体积方面：裁边后的JPEG通常几十到两三百KB，比直接存手机原图小一个数量级，没有违背"不要存很大图片"的初衷(只是从"存文字"变成"存裁剪增强后的图"，这次是用户看到OCR效果后明确要求换回保留图片样式)。新增依赖`@techstark/opencv-js@^4.12.0-release.1`——单文件WASM构建约11MB(gzip后约3.5MB)，用动态import()懒加载成独立chunk(实测`vite build`验证过，不会拖慢App其他页面的加载)，只有进入扫描弹层时才会被加载，且是随App包一起打进iOS原生工程的本地资源(不是运行时从CDN下载)，离线可用。`npx tsc -b`+`npx oxlint`+完整`npm run build`(验证opencv.js真能被Vite正确打包成独立chunk，不只是类型检查)全部跑过 | washi-ledger-ui-fixes |
| 20260913-dev.4 | 2026-09-13 | **R-32真机反馈：扫描卡在"処理中…"出不来结果**——用户实测发现处理阶段会一直转圈卡住，具体根因(WASM编译慢/其他环境因素)在沙盒里无法复现确认，先补两处防御性修复而不是瞎猜：①`lib/receiptEdgeDetect.ts`给OpenCV.js加载环节加20秒超时，超时就明确抛错，不再无限等下去(超时不影响后台真实加载过程继续跑，只是这次调用会先拿到超时错误走兜底，下次进扫描弹层能直接复用已经跑完的加载结果，不用重新触发一次完整下载/编译)；②真实报错信息从只打`console.error`(用户看不到)改成同时显示在弹层界面上(处理失败会退回原图继续可用，同时用红字显示技术性报错原文；拍照阶段失败的错误也会把原始报错文本一起显示)，方便用户截图把具体报错反馈回来，不用再靠猜测排查。`tsc -b`+`oxlint`+完整`npm run build`全部跑过 | washi-ledger-ui-fixes |
| 20260913-dev.5 | 2026-09-13 | **R-32真机排查：开启Safari Web Inspector调试**——dev.4加的20秒超时上真机测试(确认是重新sideload过的新包)依然卡住一分钟以上毫无反应，连超时报错都没弹出，说明卡的不是"等待时间不够"，而是超时机制本身没机会触发——`setTimeout`靠事件循环调度，如果主线程正卡在一段很长的同步代码里(比如OpenCV.js把内嵌的约8MB base64数据同步解码成二进制)，定时器必须等这段同步代码跑完才能执行；也可能是真机上请求这个约11MB的chunk文件本身在Capacitor本地资源加载方式下就卡住了，没抛错也没触发超时。两种可能光猜测分不清，需要真实的Console/Network信息才能判断，而`capacitor.config.json`此前没有显式声明`ios.webContentsDebuggingEnabled`，Capacitor默认只在Debug编译下才打开WKWebView的Safari调试开关(`#if DEBUG`)，CI这边打的是Release配置的未签名测试包，导致Safari Develop菜单连不上这个WKWebView——不是操作方式的问题，是包本身没开调试开关。这次在`ios`配置块里显式加`"webContentsDebuggingEnabled": true`，让Release测试包也能被Safari Web Inspector检查(只影响能不能连调试器，不影响App本身行为/性能)。改完之后用户需要iPhone设置→Safari→高级→打开"网页检查器"，数据线连Mac，Safari菜单栏"开发"里找到设备上这个App的WKWebView，直接看真实Console输出和Network请求状态排查这次卡住的真正原因。`tsc -b`+`npm run build`跑过；这一行配置本身沙盒里没法验证Safari真的连得上(需要真机+Mac+数据线的组合环境)。**补充(同一批次，还没推送前追加)**：用户反馈没有Mac，Safari Web Inspector这条路完全走不通，改成不依赖任何外部设备/工具的方案——新增`lib/appLog.ts`(纯localStorage本地日志，不联网、不经任何后端，逐条持久化"时间戳+等级+内容"，条数上限2000条防止无限增长撑爆localStorage)，在レシート扫描全链路(`receiptCamera.ts`拍照→`receiptEdgeDetect.ts`的OpenCV.js加载/edge detection各阶段→`ReceiptScanSheet.tsx`整体流程)埋点记录关键步骤耗时和结果，包括之前"卡住看不到任何信息"这个空白区间具体会记到哪一步。新增`features/settings/DebugLogPage.tsx`诊断日志查看页(设置页新增"诊断日志"入口跳转)：记录开关(默认开，因为现在正需要它排查问题)、时间范围筛选(全部/最近5分钟/30分钟/1小时/今天)、日志列表(等宽字体，最新在最上面)、复制全部(clipboard API+execCommand兜底)、下载(.txt文件，Blob+`<a download>`)、清空(带确认弹窗)。Stitch前两次`generate_screen_from_text`调用超时失败(`list_screens`确认没有新画面生成)，先直接照App现有设计token手写了一版；第三次重试Stitch成功生成设计稿(`design-assets/prototypes/washi-ledger-debug-log-viewer/final/`存了screen.png+code.html)，用户确认后按设计稿补了两处调整：①记录开关行加"实时捕获中/已暂停"状态文字+绿色呼吸点(`animate-pulse`)+日志总条数；②日志等级从纯文字变色改成三档明确底色徽标(ERROR朱红淡粉底/WARN琥珀底/INFO纸墨灰底)。`tsc -b`+`oxlint`+完整`npm run build`全部跑过 | washi-ledger-ui-fixes |
| 20260913-dev.6 | 2026-09-13 | **R-32真机根因确认+核心修复：OpenCV.js改到Web Worker线程跑**——用户装了带诊断日志的新包实测，日志显示卡在"开始动态import(@techstark/opencv-js)"这一条之后再无任何输出，包括我们自己加的20秒超时报错也没触发。这条证据直接定位到真根因：`import('@techstark/opencv-js')`本身(模块内嵌约8MB base64编码WASM数据，加载时要同步解码成二进制)会把执行它的那个线程整个阻塞住；这段代码之前跑在主线程，`setTimeout`定时器要等主线程闲下来才能触发回调，而主线程一直被这段同步解码占着，所以连超时保护都排不上队——不是"等待时间不够"，是压根没机会检查有没有超时。**核心修复**：把OpenCV.js的加载+边缘检测整个流程搬进独立的Web Worker线程(新增`lib/receiptEdgeDetectWorker.ts`)，`lib/receiptEdgeDetect.ts`改成主线程这边的薄封装——创建/复用Worker、发照片过去、等结果回来、处理超时(超时后`worker.terminate()`销毁掉可能还卡着的实例，下次重新起一个干净的)。这样不管Worker内部那段解码实际卡多久，主线程(UI线程)完全不受影响，用户能一直正常操作App，超时保护这次也才是真的会生效。Worker里没有`document`/`Image()`这些主线程专属DOM API，改用`createImageBitmap()`+`OffscreenCanvas`+`ImageData`重写了原来基于`HTMLCanvasElement`/`cv.imread(imgElement)`的边缘检测逻辑，算法本身(灰度化→高斯模糊→Canny边缘检测→找四边形轮廓→透视变换→自动对比度拉伸)完全不变，只是输入输出的数据载体换了一套Worker全局作用域也支持的标准Web API。用Vite的`new Worker(new URL(...), {type:'module'})`写法接入，实测`npm run build`确认Vite正确识别并把OpenCV.js整个打进了独立的`receiptEdgeDetectWorker-*.js`这一个chunk里(不再混在主bundle的模块图里)。`tsc -b`+`oxlint`+完整`npm run build`全部跑过；Worker内真实卡多久、超时保护这次是否真的能救回主线程，仍需真机验证——这是这次R-32卡住问题排查以来第一次有真实根因证据支撑的修复，不是又一次照经验加超时/加日志的试探。**补充(同一批次追加)**：用户真机截图反馈诊断日志页"记录中"开关的圆形滑块跑到了胶囊轨道外面——`DebugLogPage.tsx`里滑块`<span>`只写了`top-0.5`没写`left`，水平方向没有显式锚点(依赖CSS"static position"兜底，不同引擎/沙盒桌面Chromium跟真机WebKit表现不一致，沙盒里测不出这个问题)，`translate-x`是在这个不确定的基准上叠加位移，真机上就跑出轨道了；改成显式加`left-0.5`当基准锚点，`translate-x-0`/`translate-x-[22px]`两个状态改成在这个固定基准上位移，不再依赖浏览器兜底行为 | washi-ledger-ui-fixes |
| 20260913-dev.7 | 2026-09-13 | **R-32性能优化：检测阶段用缩小图跑，20秒超时问题的真正解法**——dev.6把OpenCV.js搬进Worker后真机验证确认主线程不再卡死、20秒超时能正常触发了(真实进步)，但20秒内检测本身还是没跑完，最后退回未裁剪原图。根因：手机相机拍的照片常是十几MP(比如3024×4032)，直接在全分辨率图上跑Canny边缘检测+找轮廓，这几步计算量随像素数近似线性甚至更高增长，这才是真正跑不完的原因，不是加载OpenCV.js库本身的固定成本。参考真实扫描类App的通用做法：`receiptEdgeDetectWorker.ts`新增`resizeImageData()`，检测阶段(找小票四个角点)先把图缩到最长边1200px(`DETECT_MAX_DIM`)再跑，像素量降一个数量级以上，检测速度大幅提升；`findQuad()`独立拆出来只负责在(缩小后的)图上找四个角点坐标，`detectAndWarp()`拿到缩小图上的坐标后除以缩放比例换算回全分辨率坐标系，**真正的透视裁剪(`warpPerspective`)还是在原图上做**，裁出来的成片清晰度完全不受影响，检测准确度也不受分辨率下降影响(角点位置定位不需要看清文字细节)。顺带把`receiptEdgeDetect.ts`的超时从20秒放宽到35秒——因为现在跑在Worker里完全不占用主线程，放宽超时不会让App在等待期间变得没反应，只是给OpenCV.js库加载这个一次性固定成本多留一点余量。`tsc -b`+`oxlint`+完整`npm run build`全部跑过。**补充(同一批次追加)**：用户真机截图发现超时提示"小票扫描处理超时(20秒)"是中文，App语言明明设成了日语——排查发现`receiptEdgeDetect.ts`/`receiptEdgeDetectWorker.ts`这几个`throw new Error('...')`图省事直接写死了中文字符串，没走i18n(这几个文件不是React组件，拿不到`useI18n()`)。新增`lib/i18nSync.ts`的`tSync(key, vars?)`——非React模块用这个同步查字典(读`localStorage`缓存的当前语言，跟`I18nProvider`同一份数据源)，`dict.ts`补了`receiptScanTimeoutError`/`receiptWorkerErrorPrefix`/`receiptWorkerUnknownError`/`receiptCanvasContextError`/`receiptOffscreenCanvasError`/`receiptImageLoadError`这几个zh/ja词条。Worker线程内没有`localStorage`访问权限(Web Storage API只挂在`Window`接口上)，`receiptEdgeDetectWorker.ts`那几处改成抛语言无关的错误码(`ERR_OFFSCREEN_CANVAS_CONTEXT`)，翻译成用户可见文字的活挪到主线程的`receiptEdgeDetect.ts`做(`translateWorkerError()`查表)。这条"所有用户可见文字必须走i18n，非React模块也不例外"的规则已经补进`HANDOFF-washi-ledger-rewrite.md`"已确认的设计原则"一节，以后接手/做新页面时会先读到 | washi-ledger-ui-fixes |
| 20260913-dev.8 | 2026-09-13 | **R-32诊断：Worker内部执行进度埋点**——用户真机再测dev.7，仍在35秒处超时、未检出轮廓。但从日志上看"向Worker发送照片，开始处理"和"超时"这两条中间完全没有任何输出——主线程侧的日志(`appLog`基于`localStorage`)覆盖不到Worker内部，不知道这35秒实际卡在OpenCV.js库加载还是边缘检测计算，无法判断该往哪个方向继续优化(缩小检测分辨率已经不够，但不确定是不是这个方向本身就错了)。这版不改任何算法/超时参数，只加可观测性：`receiptEdgeDetectWorker.ts`在关键阶段边界(收到照片/开始加载OpenCV.js/加载完成/图片解码完成/开始缩图/缩图完成/找角点完成(含是否找到)/透视裁剪完成/对比度增强完成)各`postMessage`一条`{kind:'progress', stage}`消息回主线程，跟最终结果消息(`{kind:'done', ok, ...}`)用`kind`字段区分；`receiptEdgeDetect.ts`收到`progress`消息直接写入`appLog`(不`resolve`/`reject`，只有`done`才会)。下次真机测试超时时，日志里就能看到卡在哪一步、各步骤耗时，才能对症下药而不是继续猜参数。`tsc -b`+`oxlint`+完整`npm run build`全部跑过 | washi-ledger-ui-fixes |
| 20260913-dev.9 | 2026-09-13 | **R-32诊断：Worker模块加载埋点+messageerror监听**——用户确认装了dev.8新包再测，仍是从"向Worker发送照片"直接跳到35秒超时，**连理论上应该同步打出的第一条"Worker进度: worker-received-photo"都没有**，说明问题比"检测计算哪步慢"更前置：不知道是Worker模块脚本本身没加载起来，还是加载了但收不到主线程发来的照片消息。iOS WKWebView对`type:'module'`的Worker历史上兼容性一直不稳定，是这次怀疑的方向之一。这版加两处更前置的埋点，不改任何算法/超时逻辑：①`receiptEdgeDetectWorker.ts`脚本**最顶层**(不在`onmessage`里，模块一加载执行就会跑到)发一条`worker-module-loaded`进度消息——如果连这条都收不到，说明Worker模块压根没执行起来，问题出在Worker本身的加载机制，不是照片消息传输或OpenCV.js；②`receiptEdgeDetect.ts`新增监听`messageerror`事件(结构化克隆失败等消息传输层面的错误，跟`error`事件是两回事，之前完全没监听，这类失败之前会被完全吞掉、只能等到超时)。`tsc -b`+`oxlint`+完整`npm run build`全部跑过 | washi-ledger-ui-fixes |
| 20260913-dev.10 | 2026-09-13 | **R-32：确认瓶颈是OpenCV.js库加载本身+加预加载优化**——dev.9真机日志确认Worker模块加载/收到照片消息都在60毫秒内完成，真正卡住的是`cv-load-start`之后：`import('@techstark/opencv-js')`这一步在Worker里也要花很久(至少35秒没完成)，排除了"Worker兼容性有问题"和"检测计算慢"这两个此前怀疑的方向。这版做两件事：①`getCv()`内部把`import()`本身resolve和WASM的`onRuntimeInitialized`拆成`cv-import-start`/`cv-import-resolved`/`cv-runtime-init-wait-start`/`cv-runtime-ready`几个更细的进度点，能看清具体卡在下载解析这个约11MB的chunk文件，还是WASM实例化；②真正的优化：`receiptEdgeDetectWorker.ts`模块顶层(Worker一创建就跑)主动调一次`getCv()`预加载，不再等用户拍完照片、真的发消息过来才触发——`getCv()`内部`cvReadyPromise`是单例，`onmessage`里再调用拿到的是同一个Promise。`receiptEdgeDetect.ts`新增`preloadReceiptScanWorker()`导出，`ReceiptScanSheet.tsx`在弹层打开(用户还在原生拍照/选图界面操作，还没拍完)的`useEffect`里调用这个预加载函数，把OpenCV.js加载这段固定成本尽量藏在用户拍照/选图的这几秒里。同时把`kind:'progress'`消息的监听挪到`getWorker()`里统一常驻监听(而不是只在`scanReceiptDocument`调用时临时监听)，避免预加载阶段发出的进度消息因为那会儿还没人监听而丢失。`tsc -b`+`oxlint`+完整`npm run build`全部跑过 | washi-ledger-ui-fixes |
| 20260914-dev.1 | 2026-09-14 | **设置页"诊断ログ"行图标补光晕效果**——用户截图反馈这行图标没有跟语言/货币/着せ替え这几行一样的"夏 · 花火"主题光晕效果，排查到`SettingsRow`调用没传`effect="ring"`；但即使补上这个prop，`ThemeIcon`还是不会生效——`bug_report`没有对应的自定义形状数据(`lib/iconGlyphs.ts`的`ICON_GLYPHS`)，`effect`只在`ICON_GLYPHS[icon]`存在时才起作用，没数据会静默走回普通Material图标。用`fontTools`(Python)从`src/assets/fonts/material-symbols-outlined.woff2`里实际提取`bug_report`这个字形的路径数据(`BoundsPen`量出边界是(160,120,800,840)，属于文件头注释里"数学居中不用额外修正"的那一类标准图标，用跟`settings`/`restart_alt`一样的`viewBox:'0 0 960 960'`)，补进`ICON_GLYPHS`；`SettingsPage.tsx`这行补上`effect="ring"`(不带`fireflies`，跟`palette`那行同规格)。用Playwright截图渲染验证过提取出来的形状是正确居中的bug图标，不是瞎猜的坐标。`tsc -b`+`oxlint`+完整`npm run build`全部跑过 | washi-ledger-ui-fixes |
| 20260914-dev.2 | 2026-09-14 | **R-32诊断：Worker心跳日志，验证"Worker线程自己被同步代码占住"这个新假设**——dev.1(0914)真机日志出现新现象：预加载阶段`cv-import-start`正常打出，但用户拍完照片、真的发消息给Worker之后，连`onmessage`最开头两行同步代码(`worker-received-photo`/`cv-load-start`，收到消息该立刻执行)都没有输出。怀疑不只是"OpenCV.js加载慢"，而是`import('@techstark/opencv-js')`这段同步解码内嵌WASM数据的代码，把Worker自己这条线程也整个占住了，导致这条线程连自己的消息队列(`postMessage`触发的`onmessage`)都处理不了——这条线程从"卡住主线程"变成"卡住自己"。这版加一个每秒打一次的心跳日志(`setInterval`，从Worker模块顶层`cv-import-start`前后开始跑，`getCv()`成功或失败后停止)：如果心跳也跟着停了，说明Worker线程确实被同步代码占住(事件循环连定时器都排不上)；如果心跳还在正常跳，说明是别的原因，需要重新分析。不改任何算法/超时逻辑，纯诊断。`tsc -b`+`oxlint`+完整`npm run build`全部跑过 | washi-ledger-ui-fixes |
| 20260914-dev.3 | 2026-09-14 | **R-32核心修复：接管OpenCV.js的WASM实例化方式，绕开导致Worker线程卡死的同步base64解码**——dev.2心跳日志实锤`import('@techstark/opencv-js')`这段同步解码内嵌约8MB base64 WASM数据的代码会把Worker自己这条线程完全占住(45秒里连一次心跳定时器都插不进去)。查证OpenCV.js官方发布默认也是这种`SINGLE_FILE`内嵌base64格式(`build_js.py`确认)，没有现成的"独立.wasm异步加载"版本可以直接替换；重新编译官方分离版需要完整Emscripten工具链，这个环境做不了。找到折中方案：这个npm包内部其实支持Emscripten标准的`Module.instantiateWasm`钩子(能完全接管WASM实例化方式)，只是包作者把`Module`写死成局部空对象、没留外部注入配置的口子。新增`scripts/patch-opencv-js.mjs`(字符串替换式补丁，不用`patch-package`，改动点单一、diff直观)，接进`package.json`的`postinstall`(每次`npm install`/`npm ci`含CI里都会自动重新应用，不用手动记)，把文件末尾`var Module = {}`改成优先读取`self.__CV_MODULE_OVERRIDE__`；用Python脚本从这同一版本的`opencv.js`里一次性提取出原始WASM二进制(校验过`\0asm`魔数)，存成`public/opencv.wasm`(随npm包版本走，升级版本要重新提取)。`receiptEdgeDetectWorker.ts`在`import()`之前设置这个全局覆盖，`instantiateWasm`实现改成`fetch('/opencv.wasm')+WebAssembly.instantiateStreaming()`真异步加载(streaming失败时兜底降级成完整下载后`WebAssembly.instantiate()`，应对部分环境wasm响应`Content-Type`没设对的情况)，不再经过内嵌base64那条同步解码路径。**Node本地验证**：写了个模拟Worker环境(伪造`self`/`importScripts`/`location`)的验证脚本，确认补丁生效后`instantiateWasm`钩子被正确调用、`WebAssembly.instantiate`本身成功——这条修复路径的核心机制没问题；但脚本卡在OpenCV自己的运行时初始化(embind给几百个C++类注册类型)超过3分钟没跑完，判断是这个沙盒环境CPU太受限(容器/无JIT优势)，不是真机的可靠计时代理，具体在真机上初始化要多久，只能靠真机测试验证。`tsc -b`+`oxlint`+完整`npm run build`全部跑过，`dist/opencv.wasm`确认生成正确 | washi-ledger-ui-fixes |
| 20260915-dev.1 | 2026-09-15 | **汇率走势图卡片重设计落地**——设计稿经Stitch多轮修正后确认(先截了真机截图核对，Stitch第一版凭空加了底部导航栏等真实页面没有的元素，改了两版才对齐)，存进`design-assets/prototypes/washi-ledger-rate-trend-chart/final/`。涨跌幅/区间最高/区间最低/波动区间这三项新增统计数据，用户明确要求不能前端各自算(担心多端以后各算各的对不上)，新增后端层：`worker/src/rate/`(`router.ts`+`handlers.ts`+`types.ts`)加`GET /rate/history-stats?base=X&target=Y&days=N`路由，服务端调`frankfurter.dev`同一个真实时间序列接口(数据源不变，只是从前端直连挪到后端)，顺带算好这三项统计：涨跌幅=区间内最新点相对最早点的百分比、区间最高/最低=区间内极值、波动区间=`(最高-最低)/最低`的百分比——都是"当前选中时间范围内"，不是固定按自然周算。这个接口不碰Supabase/不需要用户身份(汇率不是用户私有数据)，但前端仍走现有`apiClient`(App有匿名登录兜底，实际调用时永远有token，不用为这一个接口单独开鉴权例外)。前端`data/rate.ts`的`fetchRateHistory`(直连frankfurter.dev)整个替换成`fetchRateHistoryStats`(调新后端接口，返回`points`+四项统计)。`RatePage.tsx`走势图卡片：折线从逐点直线连接改成`smoothLinePath()`(Catmull-Rom转三次贝塞尔，张力1/6)画的平滑曲线；纵坐标网格线从通栏均匀虚线改成`rateGridFade`渐变stroke两端渐隐；时间范围从三个独立按钮改成共享底槽的分段控件；标题旁新增涨跌幅徽标(`pctChange`为`null`时不显示，不展示假数据)；卡片底部新增三栏统计条，"最高/最低"标签带`周`/`月内`/`年内`前缀(跟着当前选中的时间范围变，dict.ts新增`rateStatsPrefix1W/1M/1Y`等zh/ja词条)，数据不足时(`high`/`low`为`null`)整条不显示。选中点提示改成胶囊底色样式，但保留"固定在图表上方一行文字"的原实现方式，没有做成跟着选中点在SVG里浮动的tooltip(设计稿是那样画的，但那种做法要额外处理tooltip贴近图表边缘被裁切的问题，这版先用更简单可靠的实现)。`tsc -b`(前端+`worker/`两处都跑过)+`oxlint`+完整`npm run build`全部通过；沙盒里用Playwright确认页面能正常渲染不崩溃(新分段控件/无数据态都对)，但新后端接口还没部署(`worker/`部署要用户自己在本机跑`wrangler deploy`)，真实数据下的曲线/统计数值这次没法在沙盒里端到端验证，需要用户部署后端后自己测。**补充(同一批次追加)**：用户提醒要考虑主题差异——排查发现色调本身没问题(全程用的是`var(--color-primary)`这类主题token，不是写死颜色值，Playwright强制切到summer主题验证过色调正确切换)，但汇率换算页(换算卡片+走势图卡片)从设计之初就没接入过"夏 · 花火"主题的液态玻璃/发光特效(`.ledger-card`/`.entry-card`那套)，用户确认要一起补上。没有直接复用`.ledger-card`/`.entry-card`这两个类——它们各自的非summer基础定义带着专属于结余卡片/记账条目的不对称圆角，套到汇率页卡片上会在默认/怀旧主题下也跟着变形；改成新开一个只在`[data-theme='summer']`下生效的`.rate-card`类(`index.css`)，发光公式照抄`.ledger-card`的summer版本(毛玻璃blur+半透明底+主色光晕)，`RatePage.tsx`两张卡片的className里加上这个class。Playwright分别截了默认主题(确认无变化)和summer主题(确认发光效果生效)两张图核对过 | washi-ledger-ui-fixes |
| 20260915-dev.2 | 2026-09-15 | **R-32：拍照页面改成自建取景界面(CamScanner式实时预览的前置步骤)**——用户明确要求预览页面就要能画框识别，不接受"拍完之后再处理"或纯手动拖角裁剪；排查发现现有`captureReceiptPhoto()`用`@capacitor/camera`的`CameraSource.Prompt`会弹起系统原生相机UI，JS层完全拿不到实时画面，这是做任何实时检测框功能的硬性前置阻塞——不管最终检测算法用不用OpenCV都一样。这版先做这一个前置步骤：`getUserMedia({video:{facingMode:'environment'}})`+`<video>`自建取景器替换原生相机弹窗，**这一步本身不带实时检测框**，检测框是否可行留到这一步验证完成后再单独评估决定。`receiptCamera.ts`：`captureReceiptPhoto()`拆成`openCameraStream()`/`stopCameraStream()`/`captureFrameFromVideo()`(video帧画到canvas转Blob)+保留独立的`pickFromGallery()`(改用`CameraSource.Photos`，不再跟拍照共用一个系统弹窗)。`ReceiptScanSheet.tsx`：`Stage`类型从单一的`capturing`拆成`camera-starting`/`camera-live`/`camera-denied`三态，取景界面里左下角相册图标(点击释放摄像头改走`pickFromGallery()`，用户取消选图会自动回到取景界面而不是关闭整个弹层——这跟原来"系统统一弹窗、取消即关闭弹层"的行为不同，是两个入口拆开后的必然结果)+底部快门圆按钮(点击`captureFrameFromVideo()`拿到Blob后走原有`scanReceiptDocument()`边缘检测流水线，这段完全没动)；`getUserMedia`被拒绝/设备不可用时进入`camera-denied`态，给"重试"+"改用相册选择"两个按钮兜底，不会卡在黑屏。开发过程中用真实Chromium(fake-device-for-media-stream参数模拟摄像头)+React StrictMode跑出一个真实竞态bug：`startCameraPreview()`是异步的，StrictMode开发模式下的挂载→卸载→重新挂载连续调用会让一次已经"过期"的`openCameraStream()`请求在组件重新走完一轮之后才resolve，把过期的流错误地当成当前流赋值上去(`AbortError: play() interrupted`)——加了个"取景请求代次号"(`cameraGenerationRef`)修掉，每次`startCameraPreview()`领一个新代次，`await`结束后代次对不上就直接把拿到的流关掉、不更新任何state/ref，`releaseStream()`统一负责让在途的旧请求作废。**沙盒验证**：由于沙盒到Supabase的网络不通(证书链问题)，`/add`页面的分类数据一直卡在加载中进不去真实入口，改用临时的独立Vite入口(`test-camera.html`+`.tsx`，验证完已删除，未提交)单独挂载`ReceiptScanSheet`+`I18nProvider`+`SettingsProvider`(不依赖catalog/entries)，配合Playwright的`--use-fake-device-for-media-stream`参数验证：取景界面确认拿到真实视频帧(`video.videoWidth/videoHeight`非零、画面是Chromium内置的动态测试图案)、点快门后正确走到既有处理流水线并在35秒超时后按原有逻辑降级成原图进入review态、`getUserMedia`权限拒绝时`camera-denied`态的错误提示和两个兜底按钮都正确渲染。`tsc -b`+`oxlint`+完整`npm run build`全部通过。**未决问题(留到这一步验证完成后再定)**：实时检测框在这个取景界面上是否可行还没有验证，`@techstark/opencv-js`+`patch-opencv-js.mjs`+`public/opencv.wasm`这套WASM加载修复(dev.3)去留也还没定——取决于后续实时检测方案是否还需要OpenCV | washi-ledger-ui-fixes |
| 20260915-dev.3 | 2026-09-15 | **Capacitor 6→7升级(未完成，会话中途叫停，仅推送已验证的部分供本地继续)**——R-32调研发现真正的扫描App在iOS上都用系统自带VisionKit(`VNDocumentCameraViewController`)而不是OpenCV.js，找到现成Capacitor插件`@capgo/capacitor-document-scanner`封装了这个能力，但要求`@capacitor/core>=7.0.0`(项目当时是6.1.2)。这版把`@capacitor/app`/`camera`/`core`/`ios`/`cli`五个包升到`^7.0.0`，`.github/workflows/washi-ledger-ios-test-build.yml`的`runs-on`从`macos-14`改成`macos-15`(GitHub已宣布`macos-14`年底前停用，独立于这次升级也需要换)。调研确认过：Capacitor 7要求Node20+/Xcode16+/iOS部署目标14.0，`macos-15`runner默认Xcode就是16.4够用；被删的`bundledWebRuntime`/`cordova.staticPlugins`/`.platform`/`.isNative`这几项，项目代码里grep过没有用到；另一个自定义原生插件`capacitor-alarm-ringer`只被根目录旧App依赖，跟washi-ledger无关。`npm install`+`npm run build`(tsc+vite)本地跑通无报错。**没有验证的**：真正的iOS原生编译(沙盒没有macOS/Xcode，只能等推上去后跑CI的`macos-15` runner验证)；那个原生扫描插件本身**还没安装**，这只是升级Capacitor这一个前置步骤。详细交接见根目录`HANDOFF-session-20260915.md` | washi-ledger-ui-fixes |
| 20260915-dev.4 | 2026-09-15 | **修复汇率走势图"底部空一大块"bug**——根因：`RatePage.tsx`的`chartGeometry`之前按"每个数据点固定24px(`POINT_GAP`)撑宽图表像素宽度+外层横向滚动到最右"(B-38引入)，1Y档365个点撑出的宽度只有最后一屏(~13点/两三周)会被滚动显示出来，但纵坐标`min`/`max`是按*全部*365个点算的，可视窗口内真实波动相对全年range小得多，折线被压扁在中间、底部空一大截。改成图表宽度=容器实际可用像素宽度(`ResizeObserver`实测，不再写死320或按点数撑宽)，不再横向滚动，全部数据点摊平显示在同一屏，min/max始终跟"当前显示的点集合"是同一份数据。开发过程中自己的第一版实现有个bug：用`useRef`+空依赖数组的`useEffect`绑定`ResizeObserver`，但图表容器只在`chartGeometry`非null(有数据)时才挂载，组件首次挂载那一刻容器通常还没渲染出来(数据在loading)，导致`ResizeObserver`永远没绑上、宽度卡在初始默认值320，在比320px窄的手机屏幕上图表实际比容器宽、右侧被裁切——用浏览器模拟375px手机宽度实测才发现，改成回调ref(容器DOM节点出现/消失时自动重新绑定/解绑)修好。这次沙盒网络能连通真实Worker/Supabase(不同于之前几次session)，直接用本地dev server + 真实数据在桌面宽度和375px手机宽度分别验证了"过去1个月"/"过去1年"两档，折线都能用满纵向range、横坐标不再截断也不再滚动。`tsc -b`+`vite build`+`oxlint`全部通过 | washi-ledger-rewrite |
| 20260915-dev.5 | 2026-09-15 | **R-32收尾：接入原生文档扫描插件，彻底取代OpenCV.js整条链路**——Capacitor 6→7升级(dev.3)经CI(`macos-15` runner)验证编译成功后，正式装上`@capgo/capacitor-document-scanner@7.2.5`(与Capacitor 7兼容的版本线，`latest`标签是要求Capacitor>=8的8.x版本，装的时候要显式指定7.x)，调用iOS原生VisionKit(`VNDocumentCameraViewController`)/Android ML Kit——硬件加速、零加载时间、自带实时边缘检测+透视校正的系统级全屏扫描界面，彻底解决了R-32从一开始就没解决过的"扫描超时"问题(OpenCV.js运行时初始化本身在这台设备上就卡死超过35秒，判定这条技术路径在Worker环境里跑不动，不是加载方式问题)。`receiptCamera.ts`：删掉`openCameraStream`/`stopCameraStream`/`captureFrameFromVideo`(昨天dev.2刚做的自建webcam取景界面，现在被原生扫描器的系统级全屏UI整个取代，不再需要)，新增`scanDocumentNative()`调插件；保留`pickFromGallery()`(相册选图，作为原生扫描器在Web端/权限被拒绝时的兜底路径——插件的web fallback在浏览器里会直接抛"not supported on the web"，这是插件本身的设计，不是bug)。`ReceiptScanSheet.tsx`大幅简化：`Stage`从原来6态(camera-starting/camera-live/camera-denied/processing/review/error)砍到3态(scanning/error/review)，因为原生扫描器自己就是完整的权限/实时预览/裁剪流程，不需要自建这些中间状态；review阶段也不再需要"未检测到边缘用原图兜底"的提示文案，因为原生扫描器要么成功返回已裁剪校正的图，要么直接报错，没有中间态。**确认后清理死代码**：`receiptEdgeDetectWorker.ts`(421行)、`receiptEdgeDetect.ts`里的`scanReceiptDocument`/`preloadReceiptScanWorker`/Worker通信逻辑、`@techstark/opencv-js`依赖、`scripts/patch-opencv-js.mjs`补丁脚本、`public/opencv.wasm`(8MB二进制)全部删除——构建产物里那个10.7MB的`receiptEdgeDetectWorker`chunk确认已经从`dist/`里消失。`dict.ts`同步清理了11个只服务于这条被删链路的孤儿i18n词条(zh/ja各一份)。`tsc -b`+`oxlint`+完整`npm run build`全部通过，构建产物体积明显下降。**Smartsheet R-32行**已同步更新备注记录这次收尾。**还没验证的**：这个插件的实际扫描效果/真实设备上的表现，只能等新ipa出来后真机测试才知道 | washi-ledger-ui-fixes |
| 20260915-dev.6 | 2026-09-15 | **清理更早遗留的OCR死代码**——`src/lib/receiptOcr.ts`(Tesseract.js文字识别封装)+`tesseract.js`依赖，是更早一轮会话就已经放弃的方案(日文小票OCR识别质量不理想，改成保留原版面直接嵌图的"全能扫描王"式扫描)，当时刻意留着没删(注释写明是为以后"自动读取金额填入字段"这个滞后需求预留)，但从未被任何代码真正`import`过，一直是纯死代码。这次dev.5清OpenCV死代码时顺手确认过这个文件跟今天的改动无关，用户确认后一并删除。`receiptPdf.ts`/`types/index.ts`里指向`receiptOcr.ts`的过时注释(还提到"经过Canny边缘检测"/"存的是OCR识别文字生成的PDF"这类跟现在原生扫描器实现完全对不上的旧描述)一并改成准确描述当前实现。`tsc -b`+`oxlint`+`npm run build`全部通过 | washi-ledger-ui-fixes |
| 20260916-dev.1 | 2026-09-16 | **新增支付方式PayPay(ペイペイ)**，还元率0.5%。前端：图标存成`src/assets/payment-brands/pm-paypay.png`(用户提供的红底白"P"标志)，`PaymentMethodIcon.tsx`的`BRAND_LOGOS`里注册`'pm-paypay'`映射；`payment_methods`表是全局预设参考数据，不走客户端写入，改用新增的`sql/2026-09-16_payment_method_paypay.sql`迁移脚本(幂等，用户手动在Supabase SQL Editor执行)插入一行(`code='paypay', zh='PayPay', ja='ペイペイ', icon='pm-paypay', default_point_rate=0.005, sort_order`接在现有预设最后)。过程中发现`payment_methods`表比之前已知的字段多出`is_preset`/`user_id`两列(通过用户截图确认)，且没有随仓库带DDL建表脚本(default_point_rate/sort_order等列都是运营期间陆续手动加的)；用`.env`里前端本来就有的匿名key直接查PostgREST接口核对了线上真实字段和现有8条预设数据的`sort_order`排列，比单纯读用户截图更可靠。用户在Supabase里手动执行插入时首次遗漏了`is_preset=true`，导致新行插入成功但被RLS策略挡住、前端匿名key读不到，追加一条UPDATE补上`is_preset=true, user_id=null`后复测确认能正常读取 | washi-ledger-ui-fixes |
| 20260916-dev.2 | 2026-09-16 | **账目卡片レシート凭证常驻角标按钮**(R-32续，接续之前"点击展开抽屉里第4个按钮"方案的改造)：`EntryCard.tsx`卡片右上角新增一个圆形"骑缝章"角标——只在这条记录有`receiptPath`时显示，一半压在卡片圆角边框内、一半探出卡片外(Stitch设计稿确认稿No.06图标：右上45°折角纸张+底部三齿锯齿+3条横线，viewBox手工从设计稿坐标换算成干净的`0 0 36 49`)，单独点击直接弹出レシート预览，跟卡片整体展开(编辑/复制/删除抽屉)用`stopPropagation()`彻底互不干扰；原来"展开后在抽屉里第4个按钮查看凭证"的入口整个删掉，`hasActions`不再把`hasReceipt`算进去(明细页只靠这个撑起展开抽屉已经没有意义)。角标颜色用`color-mix(in srgb, var(--color-primary) ...)`跟随当前主题的`--color-primary`，default/nostalgia/summer三套主题自动适配不用分别写死颜色。这版经历了两轮用户验收反馈才定稿：①角标改成44×44px透明热区(外层)包32px视觉圆章(内层)，补上遗漏的iOS HIG最小触控尺寸要求；②角标探出卡片顶部导致跟"日期分组标题"/上一张卡片相撞，`.entry-card`自身`mb-2`→`mb-4`，连带`RecentEntriesList.tsx`/`HistoryEntryList.tsx`/两处`CategoryDetailSheet.tsx`的日期标题间距一并调大，按角标最大探出量(16px)反推验证过零碰撞(专门截图做了"当天第一条记录就带凭证"的压力测试场景)；③补上遗漏的悬浮阴影+summer主题呼吸光晕——没有直接套用`IconEffects.tsx`的`RingIconEffect`(那套是给`ICON_GLYPHS`里登记过形状数据的图标用的，这个角标是自定义线条图标没登记)，改为照抄结构更接近的`RateShortcutFab.tsx`(同属"小圆形常驻浮标")，新增`.receipt-badge-glow`类复用其`rate-fab-breathe`关键帧。因为沙盒连不上真机/Supabase登录，用`npm run build`产出的真实编译CSS+Playwright离屏渲染三套主题+当天首条记录压力测试场景做的视觉验证，不是走完整App流程截图 | washi-ledger-ui-fixes |
| 20260917-dev.1 | 2026-09-17 | **汇率换算卡片数据源+缓存bug修复**：用户真机反馈换算卡片显示的日期(2026-09-15)跟走势图最新点(2026-09-16)对不上，排查出`data/rate.ts`的`fetchRates()`直连frankfurter.dev时没加`cache: 'no-store'`——frankfurter.dev响应头是`cache-control: public, max-age=86400`，浏览器/WebView本地缓存了前一天的响应，24小时内不会真的重新发请求，走势图那边走自建Worker后端转发(服务器对服务器请求，不受浏览器本地缓存影响)所以能拿到真实最新数据，两边因此对不上；用`curl`直接打frankfurter.dev官方接口实测确认了服务器端当时数据其实已经更新，纯粹是客户端缓存导致的显示滞后。顺带调研了"为什么拿不到当天(09-17)数据"——查证ECB官方参考汇率每个工作日约16:00 CET才发布，发布前全球所有镜像/派生数据源(包括frankfurter.dev)都拿不到当天数据，不是bug、没有绕过办法；调研了Open Exchange Rates/Fixer.io/CurrencyLayer/CurrencyFreaks/currencyapi.com等多个"更新更频繁"的替代源，无一例外要么锁死base货币(USD/EUR)要么阉割掉历史区间查询接口(走势图需要)，才决定改用`api.exchangerate.fun`(个人维护的免费第三方服务，每小时更新，任意base货币，CORS开放，用`curl`实测过响应确实是当天最新数据)作为换算卡片的主数据源，请求失败时自动降级回退到frankfurter.dev(也补了`cache: 'no-store'`)，用户已知晓这是个人维护、无官方SLA的服务，接受这层风险；走势图数据源(`fetchRateHistoryStats`，走Worker+frankfurter.dev)本次不动。同时排查到`RatePage.tsx`第462行走势图外层容器`h-52`(208px)跟图表实际高度常量`CHART_H`(160px)对不上，导致图表下方多出48px真实存在的空白bug(用户真机截图确认)，这处诊断已确认但用户还没明确要求修改，暂未改动 | washi-ledger-ui-fixes |
| 20260917-dev.2 | 2026-09-17 | **汇率走势图底部空白bug修复**(接续dev.1里诊断的问题)：`RatePage.tsx`第462行走势图外层容器`h-52`改成`style={{ height: CHART_H }}`，直接读同一个高度常量而不是另开一个容易再次对不上的写死数字，图表下方多出的48px空白消失 | washi-ledger-ui-fixes |
| 20260917-dev.3 | 2026-09-17 | **汇率走势图坐标轴+横向滚动+底部统计条重设计**(任务#34收尾)。用户拿真机截图证实纵坐标数值贴左边被裁(`AXIS_W=44`太窄)，要求走Stitch重新设计而不是直接改数字——新建Stitch项目`washi-ledger-rate-chart-redesign`，一次生成方案A(纵轴浮动内嵌标签)/方案B(独立固定列拓宽)两版对比，用户选方案B：`AXIS_W`从44拓宽到54px。同一轮讨论里用户还要求底部三栏统计条(週間最高値/週間最安値/変動幅)"変動幅"换成两国央行法定利率——排查发现这类数据没有免费实时API(`unirateapi.com`自己在FAQ里说明"利率一年就变几次，不值得做接口，给源链接就够"，且实测其页面数据本身已经过期6个月)，于是联网核实了11个货币当前真实利率(逐个搜索验证，非单一信源)，新增`worker/src/rate/centralBankRates.ts`：静态兜底表(2026-09-17核实，含来源/截至日期)+解析`unirateapi.com`表格HTML的抓取函数，按`asOf`日期"谁新用谁"合并，抓取失败/更旧都不影响展示；新增`GET /rate/central-bank-rates`路由，Cache API缓存14天(用户明确要求"定时拉取、不用很频繁")。新加坡(SGD)特殊处理——MAS不设利率目标、用汇率政策(S$NEER浮动区间)调控，`rate`字段为`null`，前端显示"无利率目标"文字而非当0%展示。底部统计条改成左右两栏(方案A同款布局)：左边週間最高値(primary印章红)/週間最安値(secondary草木绿)堆叠；右边"利率"(tertiary琥珀)两行，各自显示base/target两国国家名+利率%。同一轮用户还提到"趋势图之前的bug又浮现"，要求横向滚动重新带回来(不必全部点位摊平挤进一屏)，但这正是B-38当初引入、又被B-40撤掉的方案——原因是纵轴min/max当时按全部数据点算，1Y档滚到最新一小段时因为全年range太大导致折线被压扁看起来像直线。这次重新实现横向滚动(按数据点数`POINT_GAP=18px`撑宽图表)的同时把这个根因一并修掉：纵轴min/max改成只按**当前可见滚动窗口**内的点动态算，随滚动实时重算(节流到每帧最多一次)，默认滚动到最右(最新数据)。`tsc -b`+`oxlint`+完整`npm run build`(前端+`worker/`两处)全部通过；沙盒连不到已部署的Worker域名(`washi-ledger-api.pigbang.workers.dev`)，新接口还没上线，需要用户本机`wrangler deploy`后在真机验证横向滚动手感/纵轴不再截断/利率数据正常显示。推送时特意把这批改动跟同一工作区里另一项仍在等待确认的"小票App内预览弹窗"(`ReceiptPreviewSheet.tsx`)改动分开提交——用`git stash push --keep-index -u`只把已确认的这批暂存起来验证+提交，避免把还没经过确认的那部分一起带上去 | washi-ledger-ui-fixes |
| 20260917-dev.4 | 2026-09-17 | **レシート凭证预览改成App内弹层**(R-32续)：用户反馈之前点击查看凭证用`window.open(url,'_blank')`直接跳出系统浏览器"外跳不好"，要求做成App内弹窗；同时明确提醒"小票有可能很长，注意滚动"。新增`ReceiptPreviewSheet.tsx`，跟`ReceiptScanSheet.tsx`同一套底部弹层视觉规范(遮罩+从底部滑入的rounded-t卡片)，内容区用`<iframe src={signedUrl}>`内嵌渲染PDF(WKWebView原生支持在iframe里直接显示PDF，不需要额外接入pdf.js)，`flex-1 min-h-0`保证内容过长时iframe内部可以滚动；底部保留"在浏览器中打开"的`<a target="_blank">`兜底链接，防止个别WebView环境iframe内嵌PDF渲染有问题时用户还有路径能看到内容。`EntryCard.tsx`的`handleViewReceipt()`从`window.open()`改成`setPreviewOpen(true)`弹出这个新组件；`dict.ts`新增`receiptPreviewTitle`/`receiptOpenExternalLabel`两条zh/ja词条，删掉不再使用的`receiptViewLabel`。`tsc -b`+`oxlint`+完整`npm run build`全部通过 | washi-ledger-ui-fixes |
| 20260918-dev.1 | 2026-09-18 | 新一天，日期重置。**底部利率统计条JPY接入日银官方实时API**：起因是用户问"能不能像TradingView一样拿到日本央行利率"，调研确认日本银行2026-02新上线了官方时系列数据API(`stat-search.boj.or.jp/api/v1/getDataCode`)，免key/免注册、JSON格式，实测可用(`curl`直接验证返回真实数据)。新增`worker/src/rate/centralBankRates.ts`的`fetchBojCallRate()`，查`db=FM02`/`code=STRECLUCON`(無担保コールO/N物レート月度值)最近4个月窗口，取最后一个非null值(当月数据要月末才发布，查询窗口太窄容易只查到null)；`handlers.ts`的`getCentralBankRates()`在原有unirateapi抓取结果基础上，用日银官方数据按"更新才覆盖"口径覆盖JPY这一项，日银API请求失败不影响其余10个货币，仍回退到原有静态表/抓取结果。顺带发现`CENTRAL_BANK_RATES_SEED`里JPY静态兜底值(0.75，2025-12-19)已经过期，同步核实更新成0.977/2026-08-31(与日银API当前返回值一致)。CNY同轮调研过FRED(China利率序列已停更在2023年)、chinamoney.com.cn的`lpr.json`接口(返回2021年占位数据)、东方财富猜测的几个报表接口(均不存在)、shibor.org(网络不通测不了)，没找到同等可靠的免费官方源，本次不改动，静态值核实过是当前值(3.0%/2026-08-20)。缓存key从`central-bank-rates-cache-v1`bump到`v2`，避免用户端还缓存着旧的JPY静态值要等满14天才刷新。`tsc --noEmit`(worker)通过；额外写了一段独立Node脚本直接调真实日银API验证`fetchBojCallRate()`同款逻辑，确认能正确挑出最新已发布月份(2026-08)、跳过还未发布的当月(2026-09，值为null) | washi-ledger-rewrite |
| 20260918-dev.2 | 2026-09-18 | **底部统计条补全方案A设计稿细节+レシート预览iframe定位修复**。用户拿设计稿逐像素核对dev.3那版底部统计条实现，发现四处跟方案A不符：①每个标签前漏了色点；②数字下方漏了"XX峰值/XX谷值"副标题(跟着`statsPrefixKey`动态变前缀，不是写死"本周")；③利率区块漏了虚线边框卡片+"政策利率"小标签；④**布局本身看错**——设计稿实际是週間最高値｜週間最安値｜央行利率3列横排+2条虚线分割线，不是"最高/最低堆叠成左半、利率占右半"的2栏布局，这个是第一次实现时看图看错了，用户逐像素纠正后用`grid-cols-[1fr_1fr_1.3fr]`(第三列稍宽，装得下两行利率数据)重做成3列。这几处不符不是用户授权的简化——之前用户唯一明确要求去掉的是Stitch效果图自己加的顶部说明文字(那处去掉是对的)，圆点/副标题/边框卡片这几处是这次实现时自己偷懒简化的，没有跟用户确认过，这次逐条补全，不再自行简化。`ReceiptPreviewSheet.tsx`：用户反馈真机上内容撑爆没有标题栏/圆角/遮罩，改成`position:absolute; inset:0`贴满`position:relative`父容器，不再依赖`flex-1 min-h-0`+`h-full`这套在WebKit下对PDF-iframe不太可靠的百分比高度链条。`tsc -b`+`oxlint`+完整`npm run build`全部通过；用Playwright配合`npm run build`产出的真实编译CSS离屏渲染了默认/summer两套主题的独立静态预览验证过布局和配色，iframe定位那处沙盒里没法用真实WebView复现，需要真机验证 | washi-ledger-ui-fixes |

## 命名规则

- 正式版本：`vMAJOR.MINOR.PATCH`（语义化版本），只是主分支上一次`release: vX.Y.Z`提交里写的版本号，不再额外拉同名分支（`v1.0.0`~`v2.0.4`是旧规则留下的快照分支，之后不会再新增）。
- 开发分支（主分支以外，日常迭代用）：`accountbook-YYYYMMDD`，年月日命名，例如 `accountbook-20260731`；同一天需要另开一个的话加 `-2`/`-3` 后缀，例如 `accountbook-20260810-2`。这是从 2026-07-31 起最早定下、一直沿用的规则。
  - 2026-08-13～2026-08-14 之间的几个分支（`YYYYMMDD-dev.N` 格式，不带 `accountbook-` 前缀）是中途误改的命名，已经改回来，不再沿用。
- **核心原则(两条线都适用)：开发分支上还没合并/发布的版本，一律不用干净的`vMAJOR.MINOR.PATCH`格式**，那个格式专属于"已经合并/正式发布"的版本；开发中的版本号长相是`{DEV_BUILD_DATE}-dev.{DEV_BUILD}`(日期+当天第几次)，跟正式版本号一眼就能分清谁是谁。
  - 旧App(`accountbook-YYYYMMDD`分支)：这两个值是`index.html`里的`DEV_BUILD_DATE`/`DEV_BUILD`两个JS常量。
  - washi-ledger(`washi-ledger-rewrite`分支)：这条线还没有`accountbook-YYYYMMDD`那样按天开新分支的习惯，长期在同一个`washi-ledger-rewrite`分支上开发，所以版本号只把`{DEV_BUILD_DATE}-dev.{DEV_BUILD}`这串写进`washi-ledger/package.json`的`version`字段（用semver prerelease语法`0.0.0-{DEV_BUILD_DATE}-dev.{DEV_BUILD}`承载）——同一天再推一次，`DEV_BUILD`在当前值上+1；换了新的一天，`DEV_BUILD_DATE`改成当天、`DEV_BUILD`重置成1。等这条分支真正合并/发布，才跳到干净的`vMAJOR.MINOR.PATCH`。
